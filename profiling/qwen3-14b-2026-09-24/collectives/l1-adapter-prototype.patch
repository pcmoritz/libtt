diff --git ttnn/cpp/ttnn/operations/ccl/all_reduce/all_reduce.cpp ttnn/cpp/ttnn/operations/ccl/all_reduce/all_reduce.cpp
--- ttnn/cpp/ttnn/operations/ccl/all_reduce/all_reduce.cpp
+++ ttnn/cpp/ttnn/operations/ccl/all_reduce/all_reduce.cpp
@@ -3,6 +3,11 @@
 // SPDX-License-Identifier: Apache-2.0
 
 #include "all_reduce.hpp"
+#include <cstdlib>
+#include "ttnn/operations/ccl/common/host/moe_utils.hpp"
+#include "ttnn/operations/core/to_memory_config/to_memory_config_op.hpp"
+#include "ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation.hpp"
+#include "ttnn/tensor/tensor_ops.hpp"
 
 #include <tt-metalium/experimental/fabric/fabric.hpp>
 
@@ -41,6 +46,38 @@
     // Determine topology
     tt::tt_fabric::Topology topology_ = ::ttnn::ccl::get_usable_topology(input_tensor, topology, cluster_axis);
     topology_ = ::ttnn::ccl::convert_2d_to_1d_topology(topology_);
+    // Experimental two-rank adapter to the existing fused L1 exchange/reduce kernel.
+    const auto& shape = input_tensor.logical_shape();
+    const char* enabled = std::getenv("LIBTT_TP2_MINIMAL_ALL_REDUCE");
+    if (enabled && std::string(enabled) == "1" && cluster_axis &&
+        mesh_device->arch() == tt::ARCH::BLACKHOLE && mesh_device->num_devices() == 2 &&
+        ttnn::ccl::get_topological_dimension(input_tensor, cluster_axis) == 2 &&
+        input_tensor.dtype() == DataType::BFLOAT16 && input_tensor.layout() == Layout::TILE &&
+        input_tensor.memory_config() == DRAM_MEMORY_CONFIG &&
+        memory_config.value_or(DRAM_MEMORY_CONFIG) == DRAM_MEMORY_CONFIG &&
+        shape.size() >= 2 && shape[-1] >= 1024 && shape[-1] <= 8192 && shape[-1] % 1024 == 0 &&
+        input_tensor.logical_volume() / shape[-1] <= 2) {
+        constexpr uint32_t workers = 32;
+        const uint32_t shard_width = shape[-1] / workers;
+        const CoreRangeSet cores(CoreRange(CoreCoord(0, 0), CoreCoord(7, 3)));
+        const MemoryConfig local_config(
+            TensorMemoryLayout::WIDTH_SHARDED, BufferType::L1,
+            tt::tt_metal::ShardSpec(cores, {32, shard_width}, ShardOrientation::ROW_MAJOR));
+        const MemoryConfig scratch_config(
+            TensorMemoryLayout::WIDTH_SHARDED, BufferType::L1,
+            tt::tt_metal::ShardSpec(cores, {32, 2 * shard_width}, ShardOrientation::ROW_MAJOR));
+        auto local = ttnn::to_memory_config(input_tensor, local_config);
+        ttsl::SmallVector<uint32_t> scratch_shape(shape.cbegin(), shape.cend());
+        scratch_shape.back() *= 2;
+        auto scratch = ttnn::create_device_tensor(
+            tt::tt_metal::TensorSpec(Shape(scratch_shape), tt::tt_metal::TensorLayout(input_tensor.dtype(), input_tensor.tensor_spec().page_config(), scratch_config)),
+            mesh_device, input_tensor.tensor_topology());
+        const auto links = num_links.value_or(ttnn::operations::ccl::common::get_num_links(*mesh_device, cluster_axis));
+        auto reduced = ttnn::prim::all_reduce_async(
+            local, scratch, *cluster_axis, *mesh_device, topology_, std::nullopt,
+            input_tensor.dtype(), local_config, links, subdevice_id, false, false, true);
+        return ttnn::to_memory_config(reduced, DRAM_MEMORY_CONFIG);
+    }
     // Call the experimental all_reduce_async with Sum operation
     return ::ttnn::experimental::all_reduce_async(
         input_tensor,
diff --git ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation.cpp ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation.cpp
--- ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation.cpp
+++ ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation.cpp
@@ -258,7 +258,7 @@
     uint32_t cluster_axis,
     MeshDevice& mesh_device,
     ttnn::ccl::Topology topology,
-    const GlobalSemaphore& multi_device_global_semaphore,
+    const std::optional<GlobalSemaphore>& multi_device_global_semaphore,
     std::optional<DataType> dtype,
     const std::optional<MemoryConfig>& memory_config,
     std::optional<size_t> num_preferred_links,
diff --git ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation.hpp ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation.hpp
--- ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation.hpp
+++ ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation.hpp
@@ -39,7 +39,7 @@
     uint32_t cluster_axis,
     MeshDevice& mesh_device,
     ttnn::ccl::Topology topology,
-    const GlobalSemaphore& multi_device_global_semaphore,
+    const std::optional<GlobalSemaphore>& multi_device_global_semaphore,
     std::optional<DataType> dtype,
     const std::optional<MemoryConfig>& memory_config,
     std::optional<size_t> num_preferred_links,
diff --git ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation_types.hpp ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation_types.hpp
--- ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation_types.hpp
+++ ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_device_operation_types.hpp
@@ -24,7 +24,7 @@
     DataType dtype = DataType::BFLOAT16;
     MemoryConfig output_mem_config;
     ttnn::ccl::Topology topology;
-    GlobalSemaphore semaphore;
+    std::optional<GlobalSemaphore> semaphore;
     std::optional<tt::tt_metal::SubDeviceId> sub_device_id;
     bool use_noc1_only = false;
     bool use_optimal_ccl_for_llama = false;
@@ -41,7 +41,7 @@
         DataType dtype,
         MemoryConfig output_mem_config,
         ttnn::ccl::Topology topology,
-        GlobalSemaphore semaphore,
+        std::optional<GlobalSemaphore> semaphore,
         std::optional<tt::tt_metal::SubDeviceId>& sub_device_id,
         bool use_noc1_only,
         bool use_optimal_ccl_for_llama,
diff --git ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_program_factory.cpp ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_program_factory.cpp
--- ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_program_factory.cpp
@@ -93,8 +93,17 @@
     Tensor& tensor_return_value) {
     tt::tt_metal::distributed::MeshWorkload workload;
     std::unordered_map<ttnn::MeshCoordinateRange, shared_variables_t> shared_variables;
+    auto attributes = operation_attributes;
+    if (!attributes.semaphore) {
+        auto* device = tensor_args.input_tensor.device();
+        auto sd = attributes.sub_device_id.value_or(device->get_sub_device_ids().at(0));
+        auto cores = device->worker_cores(tt::tt_metal::HalProgrammableCoreType::TENSIX, sd);
+        attributes.semaphore = ttnn::global_semaphore::create_global_semaphore(device, cores, 0);
+        tt::tt_metal::distributed::Synchronize(device, std::nullopt, {});
+    }
     for (const auto& coord : tensor_coords.coords()) {
-        auto cached_program = create_at(operation_attributes, coord, tensor_args, tensor_return_value);
+        auto cached_program = create_at(attributes, coord, tensor_args, tensor_return_value);
+        cached_program.shared_variables.owned_semaphore = attributes.semaphore;
         workload.add_program(ttnn::MeshCoordinateRange(coord), std::move(cached_program.program));
         shared_variables.emplace(ttnn::MeshCoordinateRange(coord), std::move(cached_program.shared_variables));
     }
@@ -147,7 +156,7 @@
     const auto& num_links = operation_attributes.num_links;
     const auto& ring_size = operation_attributes.ring_size;
     const auto& topology = operation_attributes.topology;
-    const auto& semaphore = operation_attributes.semaphore;
+    const auto& semaphore = operation_attributes.semaphore.value();
     const auto& sub_device_id = operation_attributes.sub_device_id;
     const auto& use_noc1_only = operation_attributes.use_noc1_only;
     const auto& use_optimal_ccl_for_llama = operation_attributes.use_optimal_ccl_for_llama;
@@ -614,7 +623,7 @@
         const auto& input_tensor = tensor_args.input_tensor;
         const auto& buffer_tensor = tensor_args.buffer_tensor;
 
-        auto semaphore = operation_attributes.semaphore;
+        const auto& semaphore = operation_attributes.semaphore ? *operation_attributes.semaphore : *shared_vars.owned_semaphore;
 
         // update senders
         auto& worker_reader_sender_runtime_args_by_core =
diff --git ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_program_factory.hpp ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_program_factory.hpp
--- ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_program_factory.hpp
+++ ttnn/cpp/ttnn/operations/experimental/ccl/all_reduce_async/device/all_reduce_async_program_factory.hpp
@@ -10,6 +10,7 @@
 namespace ttnn::experimental::prim {
 
 struct AllReduceAsyncSharedVariables {
+    std::optional<GlobalSemaphore> owned_semaphore;
     tt::tt_metal::KernelHandle worker_sender_reader_kernel_id{};
     tt::tt_metal::KernelHandle worker_sender_writer_kernel_id{};
     tt::tt_metal::KernelHandle reduction_reader_kernel_id{};
