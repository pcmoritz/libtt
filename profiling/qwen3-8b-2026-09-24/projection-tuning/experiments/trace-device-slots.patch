diff --git a/lib/Dialect/TTNN/Transforms/TTNNTraceHoistTransform.cpp b/lib/Dialect/TTNN/Transforms/TTNNTraceHoistTransform.cpp
--- a/lib/Dialect/TTNN/Transforms/TTNNTraceHoistTransform.cpp
+++ b/lib/Dialect/TTNN/Transforms/TTNNTraceHoistTransform.cpp
@@ -10,6 +10,7 @@
 #include "ttmlir/Dialect/TTNN/IR/TTNNOps.h"
 #include "ttmlir/Dialect/TTNN/IR/TTNNOpsAttrs.h"
 #include "ttmlir/Dialect/TTNN/Transforms/Passes.h"
+#include <cstdlib>
 #include "ttmlir/Dialect/TTNN/Types/Types.h"
 #include "ttmlir/Dialect/TTNN/Utils/TransformUtils.h"
 #include "ttmlir/Dialect/TTNN/Utils/Utils.h"
@@ -111,8 +112,19 @@
   // for:
   // - Constants/parameters: Already on device and immutable
   // - KV cache tensors: Device-native and updated in-place by cache operations
+  bool isLargeDeviceActivation(mlir::Type type) {
+    if (!std::getenv("LIBTT_KEEP_TRACE_ACTIVATIONS")) return false;
+    auto tensor = mlir::dyn_cast<RankedTensorType>(type);
+    if (!tensor || !tensor.hasStaticShape() || tensor.getNumElements() < 1024)
+      return false;
+    auto layout = mlir::dyn_cast_or_null<TTNNLayoutAttr>(tensor.getEncoding());
+    return layout && layout.getBufferType() != BufferType::SystemMemory &&
+           (tensor.getElementType().isBF16() || tensor.getElementType().isF32());
+  }
+
   bool shouldKeepArgOnDevice(func::FuncOp op, size_t argIndex) {
-    return ttcore::isConstantOrParameterArgumentType(op, argIndex) ||
+    return isLargeDeviceActivation(op.getArgument(argIndex).getType()) ||
+           ttcore::isConstantOrParameterArgumentType(op, argIndex) ||
            ttcore::isKVCacheArgument(op, argIndex);
   }
 
@@ -590,6 +602,14 @@
     llvm::SmallVector<mlir::Value> slots(
         allocateSlotsFunc.getArguments().begin(),
         allocateSlotsFunc.getArguments().end());
+    for (size_t i = 0; i < slots.size(); ++i) {
+      if (isLargeDeviceActivation(slots[i].getType()) &&
+          !ttcore::isConstantOrParameterArgumentType(allocateSlotsFunc, i) &&
+          !ttcore::isKVCacheArgument(allocateSlotsFunc, i)) {
+        slots[i] = createSlot(builder, allocateSlotsFunc.getLoc(), deviceOp,
+                             mlir::cast<RankedTensorType>(slots[i].getType()));
+      }
+    }
     for (mlir::Type slotType : info.hostStagedSlotTypes()) {
       slots.push_back(createSlot(builder, allocateSlotsFunc.getLoc(), deviceOp,
                                  mlir::cast<RankedTensorType>(slotType)));
diff --git a/runtime/lib/ttnn/operations/trace/capture_or_execute_trace.cpp b/runtime/lib/ttnn/operations/trace/capture_or_execute_trace.cpp
--- a/runtime/lib/ttnn/operations/trace/capture_or_execute_trace.cpp
+++ b/runtime/lib/ttnn/operations/trace/capture_or_execute_trace.cpp
@@ -4,6 +4,7 @@
 
 #include <algorithm>
 #include <cstdint>
+#include "ttnn/operations/data_movement/copy/copy.hpp"
 
 #include "operations/trace/capture_or_execute_trace.h"
 #include "tt/runtime/detail/common/logger.h"
@@ -88,8 +89,8 @@
   LOG_ASSERT(srcTensor.storage_type() == ::ttnn::StorageType::DEVICE &&
                  dstTensor.storage_type() == ::ttnn::StorageType::DEVICE,
              "srcTensor must be on device and dstTensor must be on device");
-  ::ttnn::Tensor hostSrcTensor = ::ttnn::from_device(srcTensor);
-  ::ttnn::copy_to_device(hostSrcTensor, dstTensor);
+  if (&srcTensor.mesh_buffer() == &dstTensor.mesh_buffer()) return;
+  ::ttnn::copy(srcTensor, dstTensor);
 }
 
 // Runs the slot allocation program, which allocates the persistent device
@@ -133,7 +134,10 @@
   for (size_t i = 0; i < slots.size(); ++i) {
     const bool callerOwnedDeviceInput =
         i < op->inputs()->size() &&
-        ::tt::runtime::ttnn::utils::inDeviceMemory(op->inputs()->Get(i));
+        ::tt::runtime::ttnn::utils::inDeviceMemory(op->inputs()->Get(i)) &&
+        &slots[i].as<::tt::runtime::ttnn::TTNNTensorWrapper>(DeviceRuntime::TTNN)
+             .getTensor().mesh_buffer() ==
+        &tensorPool.getTTNNTensorAndValidate(op->inputs()->Get(i)).mesh_buffer();
     if (!callerOwnedDeviceInput) {
       slots[i]
           .as<::tt::runtime::ttnn::TTNNTensorWrapper>(DeviceRuntime::TTNN)
@@ -284,10 +288,14 @@
              "Trace ID must be UINT32");
 
   std::vector<::tt::runtime::Tensor> ownedTensors;
+  std::vector<bool> aliasedInputs;
   for (size_t i = 0; i < op->inputs()->size(); ++i) {
-    if (!::tt::runtime::ttnn::utils::inDeviceMemory(op->inputs()->Get(i))) {
-      ownedTensors.push_back(inputSlots[i]);
-    }
+    bool aliases = ::tt::runtime::ttnn::utils::inDeviceMemory(op->inputs()->Get(i)) &&
+        &inputSlots[i].as<::tt::runtime::ttnn::TTNNTensorWrapper>(DeviceRuntime::TTNN)
+             .getTensor().mesh_buffer() ==
+        &tensorPool.getTTNNTensorAndValidate(op->inputs()->Get(i)).mesh_buffer();
+    aliasedInputs.push_back(aliases);
+    if (!aliases) ownedTensors.push_back(inputSlots[i]);
   }
   ownedTensors.insert(ownedTensors.end(), outputSlots.begin(),
                       outputSlots.end());
@@ -299,7 +307,8 @@
       .traceKey = traceKey,
       .inputTensors = inputSlots,
       .outputTensors = outputSlots,
-      .ownedTensors = std::move(ownedTensors)};
+      .ownedTensors = std::move(ownedTensors),
+      .aliasedInputs = std::move(aliasedInputs)};
 
   // The capture program replays the trace once, so the output slots already
   // hold this invocation's results.
@@ -335,7 +344,7 @@
              "Mismatched number of trace inputs");
   for (size_t i = 0; i < op->inputs()->size(); ++i) {
     const ::tt::target::ttnn::TensorRef *input = op->inputs()->Get(i);
-    if (!::tt::runtime::ttnn::utils::inDeviceMemory(input)) {
+    if (!variant.aliasedInputs[i]) {
       continue;
     }
     const ::ttnn::Tensor &current =
diff --git a/runtime/include/tt/runtime/detail/ttnn/types/trace_cache.h b/runtime/include/tt/runtime/detail/ttnn/types/trace_cache.h
--- a/runtime/include/tt/runtime/detail/ttnn/types/trace_cache.h
+++ b/runtime/include/tt/runtime/detail/ttnn/types/trace_cache.h
@@ -27,6 +27,7 @@
   /// Slots allocated exclusively for this trace. Caller-owned device inputs
   /// are intentionally excluded.
   std::vector<::tt::runtime::Tensor> ownedTensors;
+  std::vector<bool> aliasedInputs;
 };
 
 struct TraceData {
