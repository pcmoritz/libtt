diff --git tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
--- tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
+++ tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
@@ -174,3 +174,5 @@ inline void calculate_sfpu_binary_div(const uint dst_index_in0, const uint dst_i
                 result = std::numeric_limits<float>::infinity();
-                result = sfpi::copysgn(result, in0);
+                sfpi::vUInt quotient_sign =
+                    sfpi::as<sfpi::vUInt>(in0) ^ sfpi::as<sfpi::vUInt>(in1);
+                result = sfpi::copysgn(result, sfpi::as<sfpi::vFloat>(quotient_sign));
             }
diff --git tt_metal/hw/ckernels/quasar/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h tt_metal/hw/ckernels/quasar/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
--- tt_metal/hw/ckernels/quasar/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
+++ tt_metal/hw/ckernels/quasar/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
@@ -51,6 +51,6 @@ inline sfpi::vFloat float32_to_bf16_rne(sfpi::vFloat value) {
  * @note DIV special cases (matching BH semantics):
  *   - 0 / 0 -> NaN
- *   - x / 0 -> ±inf, sign of x
+ *   - x / ±0 -> ±inf, sign is the XOR of the operand signs
  *   - x / x -> 1.0 (forced exact, regardless of reciprocal rounding)
  *
  * @tparam APPROXIMATION_MODE: unused, preserved to match the BH metal signature
@@ -93,3 +93,5 @@ inline void calculate_sfpu_binary(
                     result = std::numeric_limits<float>::infinity();
-                    result = sfpi::copysgn(result, in0);
+                    sfpi::vUInt quotient_sign =
+                        sfpi::as<sfpi::vUInt>(in0) ^ sfpi::as<sfpi::vUInt>(in1);
+                    result = sfpi::copysgn(result, sfpi::as<sfpi::vFloat>(quotient_sign));
                 }
diff --git tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
--- tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
+++ tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
@@ -176,3 +176,5 @@ inline void calculate_sfpu_binary_div(
                 result = std::numeric_limits<float>::infinity();
-                result = sfpi::copysgn(result, in0);
+                sfpi::vUInt quotient_sign =
+                    sfpi::as<sfpi::vUInt>(in0) ^ sfpi::as<sfpi::vUInt>(in1);
+                result = sfpi::copysgn(result, sfpi::as<sfpi::vFloat>(quotient_sign));
             }
diff --git ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_device_operation.cpp ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_device_operation.cpp
--- ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_device_operation.cpp
+++ ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_device_operation.cpp
@@ -31,7 +31,9 @@ bool is_binary_sfpu_op(
         case RSUB: return a == b && (a == FLOAT32 || a == INT32 || a == UINT32 || a == UINT16);
         case MUL:
             return !fast_and_approximate_mode || (a == b && (a == FLOAT32 || a == INT32 || a == UINT32 || a == UINT16));
-        case DIV: return !fast_and_approximate_mode || (a == FLOAT32 && b == FLOAT32) || (a == INT32 && b == INT32);
+        case DIV:
+            return !fast_and_approximate_mode || (a == FLOAT32 && b == FLOAT32) || (a == INT32 && b == INT32) ||
+                   (a == b && (a == FLOAT16 || a == BFLOAT16) && output == a);
         case LOGADDEXP:
         case LOGADDEXP2:
         case LDEXP:
