diff --git a/tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h b/tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
--- a/tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
+++ b/tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
@@ -174,3 +174,5 @@ inline void calculate_sfpu_binary_div(const uint dst_index_in0, const uint dst_i
                 result = std::numeric_limits<float>::infinity();
-                result = sfpi::copysgn(result, in0);
+                sfpi::vUInt quotient_sign =
+                    sfpi::as<sfpi::vUInt>(in0) ^ sfpi::as<sfpi::vUInt>(in1);
+                result = sfpi::copysgn(result, sfpi::as<sfpi::vFloat>(quotient_sign));
             }
diff --git a/tt_metal/hw/ckernels/quasar/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h b/tt_metal/hw/ckernels/quasar/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
--- a/tt_metal/hw/ckernels/quasar/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
+++ b/tt_metal/hw/ckernels/quasar/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
@@ -52,3 +52,3 @@ sfpi_inline sfpi::vFloat float32_to_bf16_rne(sfpi::vFloat in) {
  *   - 0 / 0 -> NaN
- *   - x / 0 -> ±inf, sign of x
+ *   - x / ±0 -> ±inf, sign is the XOR of the operand signs
  *   - x / x -> 1.0 (forced exact, regardless of reciprocal rounding)
@@ -93,3 +93,5 @@ inline void calculate_sfpu_binary(
                     result = std::numeric_limits<float>::infinity();
-                    result = sfpi::copysgn(result, in0);
+                    sfpi::vUInt quotient_sign =
+                        sfpi::as<sfpi::vUInt>(in0) ^ sfpi::as<sfpi::vUInt>(in1);
+                    result = sfpi::copysgn(result, sfpi::as<sfpi::vFloat>(quotient_sign));
                 }
diff --git a/tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h b/tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
--- a/tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
+++ b/tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_binary.h
@@ -176,3 +176,5 @@ inline void calculate_sfpu_binary_div(
                 result = std::numeric_limits<float>::infinity();
-                result = sfpi::copysgn(result, in0);
+                sfpi::vUInt quotient_sign =
+                    sfpi::as<sfpi::vUInt>(in0) ^ sfpi::as<sfpi::vUInt>(in1);
+                result = sfpi::copysgn(result, sfpi::as<sfpi::vFloat>(quotient_sign));
             }
diff --git a/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_device_operation.cpp b/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_device_operation.cpp
--- a/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_device_operation.cpp
+++ b/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_device_operation.cpp
@@ -31,3 +31,5 @@ bool is_binary_sfpu_op(BinaryOpType val, DataType a, DataType b, bool fast_and_a
             return !fast_and_approximate_mode || (a == b && (a == FLOAT32 || a == INT32 || a == UINT32 || a == UINT16));
-        case DIV: return !fast_and_approximate_mode || (a == FLOAT32 && b == FLOAT32) || (a == INT32 && b == INT32);
+        case DIV:
+            return !fast_and_approximate_mode || (a == FLOAT32 && b == FLOAT32) || (a == INT32 && b == INT32) ||
+                   (a == b && (a == FLOAT16 || a == BFLOAT16) && output == a);
         case LOGADDEXP:
