diff --git tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_cast_fp32_to_fp16a.h tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_cast_fp32_to_fp16a.h
index 6385ef2..4080b83 100644
--- tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_cast_fp32_to_fp16a.h
+++ tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_cast_fp32_to_fp16a.h
@@ -18,9 +18,9 @@ inline void cast_fp32_to_fp16a() {
 #pragma GCC unroll 8
     for (int d = 0; d < ITERATIONS; d++) {
         sfpi::vFloat x = sfpi::dst_reg[0];
-        x = float32_to_float16_rne(x);
-        sfpi::dst_reg[0] = x;
-        sfpi::dst_reg++;
+        sfpi::vUInt halfBits = float32_to_float16_bits_rne(x);
+        __builtin_rvtt_sfpstore(
+            halfBits.get(), 0, 9 /* swap high/low 16 */, ADDR_MOD_6);
     }
 }
 
diff --git tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_typecast.h tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_typecast.h
index 0609fef..92a788f 100644
--- tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_typecast.h
+++ tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_typecast.h
@@ -244,10 +244,8 @@ inline void calculate_typecast_fp32_to_uint32() {
 
 template <bool APPROXIMATION_MODE, int ITERATIONS>
 inline void calculate_typecast_fp32_to_fp16b() {
-    // Kept as a plain loop (no SFPLOADMACRO): #46231 rewrote this to round, mask off the
-    // low 16 bits (&0xFFFF0000), and store FP32 for 32-bit-Dest correctness. The historical
-    // macro relied on a FP16B store to truncate and does not reproduce the masked-FP32 result,
-    // so it is not equivalent and is not restored here. See #46751.
+    // Round to nearest even, then store the raw BFloat16 bits through a UInt16
+    // output CB so signed zero is not canonicalized by the BFloat16 pack path.
 #pragma GCC unroll 0
     for (int d = 0; d < ITERATIONS; d++) {
         TTI_SFPLOAD(p_sfpu::LREG1, InstrModLoadStore::DEFAULT, ADDR_MOD_7, 0);
@@ -256,7 +254,20 @@ inline void calculate_typecast_fp32_to_fp16b() {
         TTI_SFPIADD(0, p_sfpu::LREG13, p_sfpu::LREG1, sfpi::SFPIADD_MOD1_CC_NONE);  // lreg[1] += 0x7FFF
         TTI_SFPIADD(0, p_sfpu::LREG0, p_sfpu::LREG1, sfpi::SFPIADD_MOD1_CC_NONE);   // lreg[1] += lreg[0]
         TTI_SFPAND(0, p_sfpu::LREG14, p_sfpu::LREG1, 0);                            // lreg[1] &= 0xFFFF0000
-        TTI_SFPSTORE(p_sfpu::LREG1, InstrModLoadStore::FP32, ADDR_MOD_6, 0);
+        TTI_SFPSHFT((-16) & 0xFFF, p_sfpu::LREG1, p_sfpu::LREG1, 5);
+        TTI_SFPSTORE(p_sfpu::LREG1, SFPSTORE_MODE_SWAP_HI_LO16, ADDR_MOD_6, 0);
+    }
+}
+
+template <bool APPROXIMATION_MODE, int ITERATIONS>
+inline void calculate_typecast_fp16b_to_fp32() {
+    for (int d = 0; d < ITERATIONS; d++) {
+        // UInt16 tiles unpack into the low half of each 32-bit Dest word.
+        sfpi::vUInt bits(sfpi::dst_reg[0].mode<sfpi::DataLayout::U32>());
+        bits &= 0xffffU;
+        sfpi::dst_reg[0].mode<sfpi::DataLayout::F32>() =
+            sfpi::as<sfpi::vFloat>(bits << 16);
+        sfpi::dst_reg++;
     }
 }
 
diff --git tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_typecast.h tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_typecast.h
index 611b88e..d8b9ed8 100644
--- tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_typecast.h
+++ tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_typecast.h
@@ -257,10 +257,8 @@ inline void calculate_typecast_fp32_to_uint32() {
 
 template <bool APPROXIMATION_MODE, int ITERATIONS>
 inline void calculate_typecast_fp32_to_fp16b() {
-    // Kept as a plain loop (no SFPLOADMACRO): #46231 rewrote this to round, mask off the
-    // low 16 bits (&0xFFFF0000), and store FP32 for 32-bit-Dest correctness. The historical
-    // macro relied on a FP16B store to truncate and does not reproduce the masked-FP32 result,
-    // so it is not equivalent and is not restored here. See #46751.
+    // Round to nearest even, then store the raw BFloat16 bits through a UInt16
+    // output CB so signed zero is not canonicalized by the BFloat16 pack path.
 #pragma GCC unroll 0
     for (int d = 0; d < ITERATIONS; d++) {
         TTI_SFPLOAD(p_sfpu::LREG0, InstrModLoadStore::DEFAULT, ADDR_MOD_3, 0);
@@ -270,7 +268,20 @@ inline void calculate_typecast_fp32_to_fp16b() {
         TTI_SFPIADD(0, p_sfpu::LREG13, p_sfpu::LREG1, sfpi::SFPIADD_MOD1_CC_NONE);  // lreg[1] += 0x7FFF
         TTI_SFPIADD(0, p_sfpu::LREG0, p_sfpu::LREG1, sfpi::SFPIADD_MOD1_CC_NONE);   // lreg[1] += lreg[0]
         TTI_SFPAND(0, p_sfpu::LREG14, p_sfpu::LREG1, 0);                            // lreg[1] &= 0xFFFF0000
-        TTI_SFPSTORE(p_sfpu::LREG1, InstrModLoadStore::FP32, ADDR_MOD_2, 0);
+        TTI_SFPSHFT((-16) & 0xFFF, 0, p_sfpu::LREG1, 1);
+        TTI_SFPSTORE(p_sfpu::LREG1, SFPSTORE_MODE_SWAP_HI_LO16, ADDR_MOD_2, 0);
+    }
+}
+
+template <bool APPROXIMATION_MODE, int ITERATIONS>
+inline void calculate_typecast_fp16b_to_fp32() {
+    for (int d = 0; d < ITERATIONS; d++) {
+        // UInt16 tiles unpack into the low half of each 32-bit Dest word.
+        sfpi::vUInt bits(sfpi::dst_reg[0].mode<sfpi::DataLayout::U32>());
+        bits &= 0xffffU;
+        sfpi::dst_reg[0].mode<sfpi::DataLayout::F32>() =
+            sfpi::as<sfpi::vFloat>(bits << 16);
+        sfpi::dst_reg++;
     }
 }
 
diff --git tt_metal/hw/inc/api/compute/eltwise_unary/typecast.h tt_metal/hw/inc/api/compute/eltwise_unary/typecast.h
index de92c33..f9a7cf2 100644
--- tt_metal/hw/inc/api/compute/eltwise_unary/typecast.h
+++ tt_metal/hw/inc/api/compute/eltwise_unary/typecast.h
@@ -141,7 +141,13 @@ ALWI void typecast_tile(uint32_t idst) {
             idst,
             VectorMode::RC));
     } else if constexpr (in_format == DataFormat::Float16_b && out_format == DataFormat::Float32) {
-        // no SFPU kernel needed, handled by packer
+        MATH(SFPU_UNARY_CALL(
+            DST_SYNC_MODE,
+            DST_ACCUM_MODE,
+            calculate_typecast_fp16b_to_fp32,
+            (APPROX, 8 /* ITERATIONS */),
+            idst,
+            VectorMode::RC));
     } else if constexpr (in_format == DataFormat::Float32 && out_format == DataFormat::Float16_b) {
         MATH(SFPU_UNARY_CALL(
             DST_SYNC_MODE,
diff --git tt_metal/hw/inc/internal/tt-1xx/float16_conversion.h tt_metal/hw/inc/internal/tt-1xx/float16_conversion.h
index c9d7ccf..a4aded9 100644
--- tt_metal/hw/inc/internal/tt-1xx/float16_conversion.h
+++ tt_metal/hw/inc/internal/tt-1xx/float16_conversion.h
@@ -8,49 +8,49 @@
 
 namespace ckernel::sfpu {
 
-// Returns the exact Float32 representation of the IEEE Float16 value obtained
-// by round-to-nearest-even. Keeping the result in Float32 lets the packer emit
-// Float16 without making a second rounding decision.
-sfpi_inline sfpi::vFloat float32_to_float16_rne(sfpi::vFloat input) {
+// Returns the raw IEEE Float16 bits obtained by round-to-nearest-even. Packing
+// these bits as UInt16 preserves signed zero and infinity, which the Blackhole
+// Float16 pack path otherwise canonicalizes to +0 and NaN respectively.
+sfpi_inline sfpi::vUInt float32_to_float16_bits_rne(sfpi::vFloat input) {
     sfpi::vUInt bits = sfpi::as<sfpi::vUInt>(input);
-    sfpi::vUInt sign = bits & 0x80000000U;
+    sfpi::vUInt sign = (bits >> 16) & 0x8000U;
     sfpi::vUInt magnitude = bits & 0x7fffffffU;
-    sfpi::vUInt rounded;
+    sfpi::vUInt halfBits;
 
     v_if(magnitude >= 0x7f800000U) {
-        // Preserve infinities and NaNs.
-        rounded = magnitude;
+        halfBits = 0x7c00U | ((magnitude >> 13) & 0x3ffU);
+        v_if(magnitude > 0x7f800000U) { halfBits |= 0x200U; }
+        v_endif;
     }
     v_elseif(magnitude >= 0x477ff000U) {
         // Values at or above the midpoint after max-finite overflow to infinity.
-        rounded = 0x7f800000U;
+        halfBits = 0x7c00U;
     }
     v_elseif(magnitude >= 0x38800000U) {
         // Normal Float16: discard 13 mantissa bits with the retained LSB as
         // the tie breaker.
         sfpi::vUInt lsb = (magnitude >> 13) & 1U;
-        rounded = (magnitude + 0xfffU + lsb) & 0xffffe000U;
+        sfpi::vUInt rounded = magnitude + 0xfffU + lsb;
+        sfpi::vUInt exponent = (rounded >> 23) & 0xffU;
+        halfBits = ((exponent - 112U) << 10) | ((rounded >> 13) & 0x3ffU);
     }
     v_elseif(magnitude > 0x33000000U) {
-        // Float16 subnormal. Quantize the Float32 significand to a multiple of
-        // 2^-24, again adding half minus one plus the retained LSB for RNE.
+        // Float16 subnormal. The rounded significand is already the raw half
+        // mantissa; 0x400 naturally carries into the minimum normal value.
         sfpi::vUInt exponent = (magnitude >> 23) & 0xffU;
         sfpi::vUInt significand = (magnitude & 0x7fffffU) | 0x800000U;
         sfpi::vInt shift = sfpi::vInt(126) - sfpi::as<sfpi::vInt>(exponent);
         sfpi::vUInt truncated =
             sfpi::shft(significand, -shift, sfpi::ShiftMode::Logical);
-        sfpi::vUInt half =
+        sfpi::vUInt roundingHalf =
             sfpi::shft(sfpi::vUInt(1), shift - 1, sfpi::ShiftMode::Logical);
-        sfpi::vUInt value = sfpi::shft(
-            significand + half - 1U + (truncated & 1U), -shift,
+        halfBits = sfpi::shft(
+            significand + roundingHalf - 1U + (truncated & 1U), -shift,
             sfpi::ShiftMode::Logical);
-        sfpi::vFloat valueFloat = sfpi::convert<sfpi::vFloat>(
-            sfpi::as<sfpi::vSMag>(value), sfpi::RoundMode::Nearest);
-        rounded = sfpi::as<sfpi::vUInt>(valueFloat * 0x1p-24f);
     }
-    v_else { rounded = 0U; }
+    v_else { halfBits = 0U; }
     v_endif;
-    return sfpi::as<sfpi::vFloat>(rounded | sign);
+    return halfBits | sign;
 }
 
 // Expands raw IEEE Float16 bits into exact Float32 bits.
diff --git ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_device_op_types.hpp ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_device_op_types.hpp
index 2676608..b1557fb 100644
--- ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_device_op_types.hpp
+++ ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_device_op_types.hpp
@@ -11,6 +11,22 @@
 
 namespace ttnn::prim {
 
+inline tt::DataFormat typecast_input_format(
+    tt::tt_metal::DataType input_dtype, tt::tt_metal::DataType output_dtype) {
+    return (input_dtype == tt::tt_metal::DataType::FLOAT16 || input_dtype == tt::tt_metal::DataType::BFLOAT16) &&
+                   output_dtype == tt::tt_metal::DataType::FLOAT32
+               ? tt::DataFormat::UInt16
+               : tt::tt_metal::datatype_to_dataformat_converter(input_dtype);
+}
+
+inline tt::DataFormat typecast_output_format(
+    tt::tt_metal::DataType input_dtype, tt::tt_metal::DataType output_dtype) {
+    return input_dtype == tt::tt_metal::DataType::FLOAT32 &&
+                   (output_dtype == tt::tt_metal::DataType::FLOAT16 || output_dtype == tt::tt_metal::DataType::BFLOAT16)
+               ? tt::DataFormat::UInt16
+               : tt::tt_metal::datatype_to_dataformat_converter(output_dtype);
+}
+
 struct TypecastParams {
     const tt::tt_metal::DataType input_dtype;
     const tt::tt_metal::DataType output_dtype;
diff --git ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_program_factory.cpp ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_program_factory.cpp
index 3e66440..aa957c2 100644
--- ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_program_factory.cpp
@@ -26,12 +26,9 @@ tt::tt_metal::ProgramDescriptor TypecastProgramFactory::create_descriptor(
 
     tt::tt_metal::ProgramDescriptor desc;
 
-    const tt::DataFormat cb_data_format_input =
-        args.input_dtype == DataType::FLOAT16 && args.output_dtype == DataType::FLOAT32
-            ? tt::DataFormat::UInt16
-            : tt::tt_metal::datatype_to_dataformat_converter(input.dtype());
+    const tt::DataFormat cb_data_format_input = typecast_input_format(input_dtype, output_dtype);
     const uint32_t single_tile_size_input = tt::tile_size(cb_data_format_input);
-    const tt::DataFormat cb_data_format_output = tt::tt_metal::datatype_to_dataformat_converter(output.dtype());
+    const tt::DataFormat cb_data_format_output = typecast_output_format(input_dtype, output_dtype);
     const uint32_t single_tile_size_output = tt::tile_size(cb_data_format_output);
 
     const auto* device = input.device();
@@ -207,9 +204,9 @@ tt::tt_metal::ProgramDescriptor TypecastSubgridProgramFactory::create_descriptor
 
     tt::tt_metal::ProgramDescriptor desc;
 
-    tt::DataFormat cb_data_format = tt::tt_metal::datatype_to_dataformat_converter(input.dtype());
+    tt::DataFormat cb_data_format = typecast_input_format(input_dtype, output_dtype);
     uint32_t single_tile_size = tt::tile_size(cb_data_format);
-    tt::DataFormat cb_data_format_output = tt::tt_metal::datatype_to_dataformat_converter(output.dtype());
+    tt::DataFormat cb_data_format_output = typecast_output_format(input_dtype, output_dtype);
     uint32_t single_tile_size_output = tt::tile_size(cb_data_format_output);
 
     uint32_t ntiles = input.physical_volume() / tt::constants::TILE_HW;
diff --git ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_rm_chunked_program_factory.cpp ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_rm_chunked_program_factory.cpp
index 72c26d9..ff26945 100644
--- ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_rm_chunked_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_rm_chunked_program_factory.cpp
@@ -75,12 +75,9 @@ tt::tt_metal::ProgramDescriptor TypecastRowMajorChunkedProgramFactory::create_de
 
     tt::tt_metal::ProgramDescriptor desc;
 
-    const tt::DataFormat cb_data_format_input =
-        input_dtype == DataType::FLOAT16 && output_dtype == DataType::FLOAT32
-            ? tt::DataFormat::UInt16
-            : tt::tt_metal::datatype_to_dataformat_converter(input.dtype());
+    const tt::DataFormat cb_data_format_input = typecast_input_format(input_dtype, output_dtype);
     const uint32_t input_element_size = tt::datum_size(cb_data_format_input);
-    const tt::DataFormat cb_data_format_output = tt::tt_metal::datatype_to_dataformat_converter(output.dtype());
+    const tt::DataFormat cb_data_format_output = typecast_output_format(input_dtype, output_dtype);
     const uint32_t output_element_size = tt::datum_size(cb_data_format_output);
 
     const auto* device = input.device();
diff --git ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_sharded_program_factory.cpp ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_sharded_program_factory.cpp
index cbcaa4e..f2f42c1 100644
--- ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_sharded_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_sharded_program_factory.cpp
@@ -36,8 +36,8 @@ tt::tt_metal::ProgramDescriptor TypecastShardedProgramFactory::create_descriptor
         out_shard_spec.num_cores(),
         ncores);
 
-    tt::DataFormat act_df = tt::tt_metal::datatype_to_dataformat_converter(input.dtype());
-    tt::DataFormat out_df = tt::tt_metal::datatype_to_dataformat_converter(output.dtype());
+    tt::DataFormat act_df = typecast_input_format(input_dtype, output_dtype);
+    tt::DataFormat out_df = typecast_output_format(input_dtype, output_dtype);
 
     uint32_t input_tile_size = tt::tile_size(act_df);
     uint32_t output_tile_size = tt::tile_size(out_df);
diff --git ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_program_factory.cpp ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_program_factory.cpp
index e7a8a79..9401974 100644
--- ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_program_factory.cpp
@@ -869,13 +869,23 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
          operation_attributes.binary_op_type == BinaryOpType::GT ||
          operation_attributes.binary_op_type == BinaryOpType::LE ||
          operation_attributes.binary_op_type == BinaryOpType::GE);
-    const bool promote_fp16 = is_sfpu_op && !is_fp16_comparison && a_dtype == DataType::FLOAT16 &&
-                              a_dtype == b_dtype && a_dtype == c_dtype;
+    const bool promote_narrow_float =
+        is_sfpu_op && !is_fp16_comparison && unary::utils::uses_raw_16bit_sfpu_path(a_dtype) &&
+        a_dtype == b_dtype && a_dtype == c_dtype &&
+        (a_dtype != DataType::BFLOAT16 || operation_attributes.binary_op_type == BinaryOpType::DIV);
+    const bool promote_narrow_float_inputs = promote_narrow_float || is_fp16_comparison;
+    const bool exact_fp16_binary_output =
+        a_dtype == DataType::FLOAT16 && b_dtype == DataType::FLOAT16 && c_dtype == DataType::FLOAT16 &&
+        (promote_narrow_float || !is_sfpu_op);
     const auto a_data_format =
-        promote_fp16 ? unary::utils::raw_16bit_sfpu_format : datatype_to_dataformat_converter(a_dtype);
+        promote_narrow_float_inputs ? unary::utils::raw_16bit_sfpu_format : datatype_to_dataformat_converter(a_dtype);
     const auto b_data_format =
-        promote_fp16 ? unary::utils::raw_16bit_sfpu_format : datatype_to_dataformat_converter(b_dtype);
-    const auto c_data_format = datatype_to_dataformat_converter(c_dtype);
+        promote_narrow_float_inputs ? unary::utils::raw_16bit_sfpu_format : datatype_to_dataformat_converter(b_dtype);
+    // Promoted SFPU and FPU operations can leave infinity in FP32 DST,
+    // which the native FP16 packer does not preserve reliably.
+    const auto c_data_format = promote_narrow_float || exact_fp16_binary_output
+                                   ? unary::utils::raw_16bit_sfpu_format
+                                   : datatype_to_dataformat_converter(c_dtype);
 
     uint32_t a_single_tile_size = tt::tile_size(a_data_format);
     uint32_t b_single_tile_size = tt::tile_size(b_data_format);
@@ -898 +908 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
-    auto compute_kernel_defines = op_config.as_defines(a_dtype);
+    auto compute_kernel_defines = op_config.as_defines(is_fp16_comparison ? DataType::FLOAT32 : a_dtype);
@@ -922,8 +932,8 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
         ttsl::SmallVector<unary::EltwiseUnaryWithParam> rhs_activations = operation_attributes.rhs_activations;
         ttsl::SmallVector<unary::EltwiseUnaryWithParam> post_activations = operation_attributes.post_activations;
 
-        if (promote_fp16) {
-            const auto to_fp32 = unary::utils::make_fp16_to_fp32_typecast();
+        if (promote_narrow_float_inputs) {
+            const auto to_fp32 = unary::utils::make_typecast(a_dtype, DataType::FLOAT32);
             lhs_activations.insert(lhs_activations.begin(), to_fp32);
             rhs_activations.insert(rhs_activations.begin(), to_fp32);
         }
@@ -956,6 +966,9 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
         if (op_config.postprocess.has_value()) {
             post_activations.insert(post_activations.begin(), *op_config.postprocess);
         }
+        if (promote_narrow_float || exact_fp16_binary_output) {
+            post_activations.push_back(unary::utils::make_typecast(DataType::FLOAT32, c_dtype));
+        }
 
         bool is_integer_division =
             (operation_attributes.binary_op_type == BinaryOpType::DIV && a_dtype == DataType::INT32 &&
@@ -1045,7 +1058,8 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
 
     if (not compute_kernel_defines["PROCESS_LHS_ACTIVATIONS(i)"].empty()) {
         auto a_intermediate_format =
-            is_sfpu_op ? (promote_fp16 ? tt::DataFormat::Float32 : a_data_format)
+            is_sfpu_op
+                ? (promote_narrow_float_inputs && a_dtype == DataType::FLOAT16 ? tt::DataFormat::Float32 : a_data_format)
                        : (op_has_exp ? tt::DataFormat::Float16_b : a_data_format);
         uint32_t a_intermediate_single_tile_size = tt::tile_size(a_intermediate_format);
         desc.cbs.push_back(CBDescriptor{
@@ -1076,7 +1090,8 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
 
     if (not compute_kernel_defines["PROCESS_RHS_ACTIVATIONS(i)"].empty()) {
         auto b_intermediate_format =
-            is_sfpu_op ? (promote_fp16 ? tt::DataFormat::Float32 : b_data_format)
+            is_sfpu_op
+                ? (promote_narrow_float_inputs && b_dtype == DataType::FLOAT16 ? tt::DataFormat::Float32 : b_data_format)
                        : (op_has_exp ? tt::DataFormat::Float16_b : b_data_format);
         uint32_t b_intermediate_single_tile_size = tt::tile_size(b_intermediate_format);
         desc.cbs.push_back(CBDescriptor{
@@ -1202,9 +1217,8 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
                             b_data_format == tt::DataFormat::Float32 ||
                             (a_data_format == tt::DataFormat::Int32 && b_data_format == tt::DataFormat::Int32) ||
                             (a_data_format == tt::DataFormat::UInt32 && b_data_format == tt::DataFormat::UInt32) ||
-                            // Quant SFPU kernels compute on the fp32 input in DST; keep fp32 dest
-                            // accumulation regardless of the (possibly narrow, e.g. uint8) output format.
-                            operation_attributes.is_quant_op;
+                            // Quant and promoted narrow-float SFPU kernels compute on fp32 values in DST.
+                            operation_attributes.is_quant_op || promote_narrow_float_inputs;
 
     uint32_t src0_cb_index = tt::CBIndex::c_0;
     uint32_t src1_cb_index = tt::CBIndex::c_1;
@@ -1207,7 +1221,7 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
 
-    if (is_sfpu_op && !is_fp16_comparison) {
+    if (is_sfpu_op) {
         if (op_type != BinaryOpType::POWER) {
-            if (!promote_fp16) {
+            if (!promote_narrow_float_inputs || a_dtype == DataType::BFLOAT16) {
                 unpack_to_dest_mode[src0_cb_index] = unpack_to_dest_mode[tt::CBIndex::c_5] =
                     tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
                 unpack_to_dest_mode[src1_cb_index] = unpack_to_dest_mode[tt::CBIndex::c_6] =
diff --git ttnn/cpp/ttnn/operations/eltwise/unary/common/unary_op_utils.hpp ttnn/cpp/ttnn/operations/eltwise/unary/common/unary_op_utils.hpp
index 20a83d3..85f060e 100644
--- ttnn/cpp/ttnn/operations/eltwise/unary/common/unary_op_utils.hpp
+++ ttnn/cpp/ttnn/operations/eltwise/unary/common/unary_op_utils.hpp
@@ -18,10 +18,14 @@ using DataType = tt::tt_metal::DataType;
 
 inline constexpr auto raw_16bit_sfpu_format = tt::DataFormat::UInt16;
 
-inline EltwiseUnaryWithParam make_fp16_to_fp32_typecast() {
+inline bool uses_raw_16bit_sfpu_path(DataType data_type) {
+    return data_type == DataType::FLOAT16 || data_type == DataType::BFLOAT16;
+}
+
+inline EltwiseUnaryWithParam make_typecast(DataType input_type, DataType output_type) {
     return {
         UnaryOpType::TYPECAST,
-        {static_cast<int>(DataType::FLOAT16), static_cast<int>(DataType::FLOAT32)},
+        {static_cast<int>(input_type), static_cast<int>(output_type)},
     };
 }
 
diff --git ttnn/cpp/ttnn/operations/eltwise/unary/device/unary_program_factory.cpp ttnn/cpp/ttnn/operations/eltwise/unary/device/unary_program_factory.cpp
index 13987fe..a33bad9 100644
--- ttnn/cpp/ttnn/operations/eltwise/unary/device/unary_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/eltwise/unary/device/unary_program_factory.cpp
@@ -119,17 +119,31 @@ tt::tt_metal::ProgramDescriptor UnaryDeviceOperation::ProgramFactory::create_des
     const auto& ops_chain = operation_attributes.op_chain;
     TT_FATAL(!ops_chain.empty(), "Unary: op_chain must not be empty");
     auto compute_ops_chain = ops_chain;
-    const bool promote_fp16 = input.dtype() == DataType::FLOAT16 &&
-                              ops_chain[0].type() != UnaryOpType::TYPECAST &&
-                              ops_chain[0].type() != UnaryOpType::BITCAST &&
-                              get_compute_kernel_path(ops_chain[0].type(), input.dtype()) == "eltwise_sfpu.cpp";
-    if (promote_fp16) {
-        compute_ops_chain.insert(compute_ops_chain.begin(), make_fp16_to_fp32_typecast());
+    const bool promote_narrow_float =
+        uses_raw_16bit_sfpu_path(input.dtype()) && ops_chain[0].type() != UnaryOpType::TYPECAST &&
+        ops_chain[0].type() != UnaryOpType::BITCAST &&
+        (input.dtype() == DataType::FLOAT16 || ops_chain[0].type() == UnaryOpType::NEG ||
+         ops_chain[0].type() == UnaryOpType::LOG1P) &&
+        get_compute_kernel_path(ops_chain[0].type(), input.dtype()) == "eltwise_sfpu.cpp";
+    const auto op_type = ops_chain[0].type();
+    const bool use_native_fp16_pack =
+        input.dtype() == DataType::FLOAT16 && op_type != UnaryOpType::NEG && op_type != UnaryOpType::ABS &&
+        op_type != UnaryOpType::ISINF && op_type != UnaryOpType::ISNAN && op_type != UnaryOpType::ISFINITE &&
+        op_type != UnaryOpType::ISPOSINF && op_type != UnaryOpType::ISNEGINF && op_type != UnaryOpType::LOG &&
+        op_type != UnaryOpType::LOG1P && op_type != UnaryOpType::EXP && op_type != UnaryOpType::EXPM1;
+    if (promote_narrow_float) {
+        compute_ops_chain.insert(compute_ops_chain.begin(), make_typecast(input.dtype(), DataType::FLOAT32));
+        // NEG, LOG, LOG1P, EXP, and EXPM1 need bit-accurate conversion for IEEE
+        // special values. Keep native packing for other ops because inserting the
+        // conversion into an op chain can change results (notably EXP2).
+        if (!use_native_fp16_pack) {
+            compute_ops_chain.push_back(make_typecast(DataType::FLOAT32, input.dtype()));
+        }
     }
 
     const bool fp32_dest_acc_en = operation_attributes.fp32_dest_acc_en ||
                                   input.dtype() == DataType::FLOAT16 ||
-                                  output.dtype() == DataType::FLOAT16;
+                                  output.dtype() == DataType::FLOAT16 || promote_narrow_float;
     uint32_t packed_scalar1 = 0;
     uint32_t packed_scalar2 = 0;
 
@@ -138,11 +152,11 @@ tt::tt_metal::ProgramDescriptor UnaryDeviceOperation::ProgramFactory::create_des
     const bool is_row_major = input.layout() == Layout::ROW_MAJOR;
 
     DataFormat cb_data_format =
-        input.dtype() == DataType::FLOAT16 && compute_ops_chain[0].type() == UnaryOpType::TYPECAST
-            ? raw_16bit_sfpu_format
-            : datatype_to_dataformat_converter(input.dtype());
+        promote_narrow_float ? raw_16bit_sfpu_format : datatype_to_dataformat_converter(input.dtype());
     uint32_t single_tile_size = tile_size(cb_data_format);
-    DataFormat cb_data_format_output = datatype_to_dataformat_converter(output.dtype());
+    DataFormat cb_data_format_output =
+        promote_narrow_float && !use_native_fp16_pack ? raw_16bit_sfpu_format
+                                                      : datatype_to_dataformat_converter(output.dtype());
     if (ops_chain[0].type() == unary::UnaryOpType::BITCAST && output.dtype() == DataType::FLOAT16) {
         cb_data_format_output = DataFormat::UInt16;
     }
