diff --git a/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_program_factory.cpp b/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_program_factory.cpp
--- a/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_program_factory.cpp
+++ b/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/binary_ng_program_factory.cpp
@@ -879,4 +879,8 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
          operation_attributes.binary_op_type == BinaryOpType::GE);
-    const auto a_data_format = datatype_to_dataformat_converter(a_dtype);
-    const auto b_data_format = datatype_to_dataformat_converter(b_dtype);
+    const bool promote_fp16 = is_sfpu_op && !is_fp16_comparison && a_dtype == DataType::FLOAT16 &&
+                              a_dtype == b_dtype && a_dtype == c_dtype;
+    const auto a_data_format =
+        promote_fp16 ? unary::utils::raw_16bit_sfpu_format : datatype_to_dataformat_converter(a_dtype);
+    const auto b_data_format =
+        promote_fp16 ? unary::utils::raw_16bit_sfpu_format : datatype_to_dataformat_converter(b_dtype);
     const auto c_data_format = datatype_to_dataformat_converter(c_dtype);
@@ -928,2 +932,8 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
 
+        if (promote_fp16) {
+            const auto to_fp32 = unary::utils::make_fp16_to_fp32_typecast();
+            lhs_activations.insert(lhs_activations.begin(), to_fp32);
+            rhs_activations.insert(rhs_activations.begin(), to_fp32);
+        }
+
         if (op_config.process_lhs.has_value()) {
@@ -1044,5 +1054,5 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
     if (not compute_kernel_defines["PROCESS_LHS_ACTIVATIONS(i)"].empty()) {
-        auto a_intermediate_format = is_sfpu_op   ? a_data_format
-                                     : op_has_exp ? tt::DataFormat::Float16_b
-                                                  : a_data_format;
+        auto a_intermediate_format =
+            is_sfpu_op ? (promote_fp16 ? tt::DataFormat::Float32 : a_data_format)
+                       : (op_has_exp ? tt::DataFormat::Float16_b : a_data_format);
         uint32_t a_intermediate_single_tile_size = tt::tile_size(a_intermediate_format);
@@ -1075,5 +1085,5 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
     if (not compute_kernel_defines["PROCESS_RHS_ACTIVATIONS(i)"].empty()) {
-        auto b_intermediate_format = is_sfpu_op   ? b_data_format
-                                     : op_has_exp ? tt::DataFormat::Float16_b
-                                                  : b_data_format;
+        auto b_intermediate_format =
+            is_sfpu_op ? (promote_fp16 ? tt::DataFormat::Float32 : b_data_format)
+                       : (op_has_exp ? tt::DataFormat::Float16_b : b_data_format);
         uint32_t b_intermediate_single_tile_size = tt::tile_size(b_intermediate_format);
@@ -1207,8 +1217,10 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
         if (op_type != BinaryOpType::POWER) {
-            unpack_to_dest_mode[src0_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
-            unpack_to_dest_mode[src1_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+            if (!promote_fp16) {
+                unpack_to_dest_mode[src0_cb_index] = unpack_to_dest_mode[tt::CBIndex::c_5] =
+                    tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+                unpack_to_dest_mode[src1_cb_index] = unpack_to_dest_mode[tt::CBIndex::c_6] =
+                    tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+            }
             unpack_to_dest_mode[src0interim_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
             unpack_to_dest_mode[src1interim_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
-            unpack_to_dest_mode[tt::CBIndex::c_5] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
-            unpack_to_dest_mode[tt::CBIndex::c_6] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
         } else {
@@ -1260,3 +1272,3 @@ tt::tt_metal::ProgramDescriptor BinaryNgDeviceOperation::ProgramFactory::create_
     // unaffected.
-    if (use_llk_bcast && a_data_format == tt::DataFormat::UInt16 && b_data_format == tt::DataFormat::UInt16 &&
+    if (use_llk_bcast && a_dtype == DataType::UINT16 && b_dtype == DataType::UINT16 &&
         (op_config.postprocess.has_value() ||
diff --git a/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/kernels/compute/eltwise_utils_sfpu.hpp b/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/kernels/compute/eltwise_utils_sfpu.hpp
--- a/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/kernels/compute/eltwise_utils_sfpu.hpp
+++ b/ttnn/cpp/ttnn/operations/eltwise/binary_ng/device/kernels/compute/eltwise_utils_sfpu.hpp
@@ -14,5 +14,3 @@
 // so we can briefly retarget the packer at cb_post and then restore it to cb_out's
-// data format on the way out. SFPU variant: no unpacker srca reconfigure is needed —
-// the downstream binary SFPU op uses copy_tile_to_dst_init_short_with_dt to switch
-// formats itself.
+// data format on the way out.
 template <typename ActivationFn>
@@ -25,2 +23,3 @@ ALWI void preprocess_sfpu_impl(
     using namespace ckernel;
+    reconfig_data_format_srca(/*old*/ cb_post.get_cb_id(), /*new*/ cb_pre.get_cb_id());
 
@@ -49,2 +48,3 @@ ALWI void preprocess_sfpu_impl(
     pack_reconfig_data_format(/*old*/ cb_post.get_cb_id(), /*new*/ cb_out.get_cb_id());
+    reconfig_data_format_srca(/*old*/ cb_pre.get_cb_id(), /*new*/ cb_post.get_cb_id());
 }
diff --git a/ttnn/cpp/ttnn/operations/eltwise/unary/common/unary_op_utils.hpp b/ttnn/cpp/ttnn/operations/eltwise/unary/common/unary_op_utils.hpp
--- a/ttnn/cpp/ttnn/operations/eltwise/unary/common/unary_op_utils.hpp
+++ b/ttnn/cpp/ttnn/operations/eltwise/unary/common/unary_op_utils.hpp
@@ -18,2 +18,11 @@ using DataType = tt::tt_metal::DataType;
 
+inline constexpr auto raw_16bit_sfpu_format = tt::DataFormat::UInt16;
+
+inline EltwiseUnaryWithParam make_fp16_to_fp32_typecast() {
+    return {
+        UnaryOpType::TYPECAST,
+        {static_cast<int>(DataType::FLOAT16), static_cast<int>(DataType::FLOAT32)},
+    };
+}
+
 template <typename T = float>
diff --git a/ttnn/cpp/ttnn/operations/eltwise/unary/device/unary_program_factory.cpp b/ttnn/cpp/ttnn/operations/eltwise/unary/device/unary_program_factory.cpp
--- a/ttnn/cpp/ttnn/operations/eltwise/unary/device/unary_program_factory.cpp
+++ b/ttnn/cpp/ttnn/operations/eltwise/unary/device/unary_program_factory.cpp
@@ -120,2 +120,10 @@ tt::tt_metal::ProgramDescriptor UnaryDeviceOperation::ProgramFactory::create_des
     TT_FATAL(!ops_chain.empty(), "Unary: op_chain must not be empty");
+    auto compute_ops_chain = ops_chain;
+    const bool promote_fp16 = input.dtype() == DataType::FLOAT16 &&
+                              ops_chain[0].type() != UnaryOpType::TYPECAST &&
+                              ops_chain[0].type() != UnaryOpType::BITCAST &&
+                              get_compute_kernel_path(ops_chain[0].type(), input.dtype()) == "eltwise_sfpu.cpp";
+    if (promote_fp16) {
+        compute_ops_chain.insert(compute_ops_chain.begin(), make_fp16_to_fp32_typecast());
+    }
 
@@ -131,3 +139,6 @@ tt::tt_metal::ProgramDescriptor UnaryDeviceOperation::ProgramFactory::create_des
 
-    DataFormat cb_data_format = datatype_to_dataformat_converter(input.dtype());
+    DataFormat cb_data_format =
+        input.dtype() == DataType::FLOAT16 && compute_ops_chain[0].type() == UnaryOpType::TYPECAST
+            ? raw_16bit_sfpu_format
+            : datatype_to_dataformat_converter(input.dtype());
     uint32_t single_tile_size = tile_size(cb_data_format);
@@ -186,3 +197,3 @@ tt::tt_metal::ProgramDescriptor UnaryDeviceOperation::ProgramFactory::create_des
     const bool math_approx_mode = false;
-    std::map<std::string, std::string> unary_defines = get_block_defines(ops_chain, "0", "0", input.dtype());
+    std::map<std::string, std::string> unary_defines = get_block_defines(compute_ops_chain, "0", "0", input.dtype());
     CMAKE_UNIQUE_NAMESPACE::apply_input_dtype_defines(input.dtype(), unary_defines);
