diff --git a/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_common.hpp b/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_common.hpp
--- a/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_common.hpp
+++ b/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_common.hpp
@@ -22,3 +22,3 @@
  * @param switch_dir If true, alternates the sorting direction for each tile pair to build a bitonic sequence.
- * @param ascending Initial sorting direction: true for ascending, false for descending.
+ * @param initial_dir Initial local sorting direction: true for ascending, false for descending.
  * @param end_phase Indicates the current phase of the bitonic sort (used by the local sort kernel).
@@ -29,2 +29,3 @@
  */
+template <bool stable, bool descending>
 FORCE_INLINE
@@ -37,3 +38,3 @@ void sort_Wt_tiles_row_to_bitonic_sequence(
     const bool switch_dir,
-    const bool ascending,
+    const bool initial_dir,
     const int end_phase) {
@@ -42,3 +43,3 @@ void sort_Wt_tiles_row_to_bitonic_sequence(
 
-    bool ascending_local = ascending;
+    bool ascending_local = initial_dir;
     for (uint32_t wt = 0; wt < Wt; wt += 2) {
@@ -61,3 +62,3 @@ void sort_Wt_tiles_row_to_bitonic_sequence(
         // llk_topk_sort -> inplace
-        ckernel::topk_local_sort(0, (int)ascending_local, end_phase);
+        ckernel::topk_local_sort<stable, descending>(0, (int)ascending_local, end_phase);
 
diff --git a/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_cross_core_data_exchange.cpp b/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_cross_core_data_exchange.cpp
--- a/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_cross_core_data_exchange.cpp
+++ b/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_cross_core_data_exchange.cpp
@@ -30,2 +30,3 @@ void kernel_main() {
     constexpr bool ascending = get_arg(args::ascending) == 1;
+    constexpr bool stable = get_arg(args::stable) != 0;
 
@@ -94,3 +95,3 @@ void kernel_main() {
         // Read input value data
-        sort_Wt_tiles_row_to_bitonic_sequence(
+        sort_Wt_tiles_row_to_bitonic_sequence<stable, !ascending>(
             input_tensor_dfb,
@@ -151,20 +152,11 @@ void kernel_main() {
 
-                            uint32_t tile_input_low = input_dest_start;
-                            uint32_t tile_input_high = input_dest_end;
-                            uint32_t tile_index_low = index_dest_start;
-                            uint32_t tile_index_high = index_dest_end;
-
                             if (sub == 1) {
                                 // Use sort LLK only the last stage to sort the last pair of tiles - speed up
-                                ckernel::topk_local_sort(/*idst=*/0, (int)dir, /*end_phase(log2(K))=*/5);
+                                ckernel::topk_local_sort<stable, !ascending>(
+                                    /*idst=*/0, (int)dir, /*end_phase(log2(K))=*/5);
                             } else {
-                                ckernel::topk_merge(/*idst=*/0, m_iter, /*k=*/32);
-
-                                // topk_merge puts smallest values in DEST[0] and largest in DEST[1]
-                                // We swap their indices when using descending order
                                 if (dir) {
-                                    tile_input_low = input_dest_end;
-                                    tile_input_high = input_dest_start;
-                                    tile_index_low = index_dest_end;
-                                    tile_index_high = index_dest_start;
+                                    ckernel::topk_merge<true, stable, !ascending>(/*idst=*/0, m_iter, /*k=*/32);
+                                } else {
+                                    ckernel::topk_merge<false, stable, !ascending>(/*idst=*/0, m_iter, /*k=*/32);
                                 }
@@ -243,7 +235,10 @@ void kernel_main() {
 
-                        ckernel::topk_merge(0, m_iter, 32);
+                        if (dir) {
+                            ckernel::topk_merge<true, stable, !ascending>(0, m_iter, 32);
+                        } else {
+                            ckernel::topk_merge<false, stable, !ascending>(0, m_iter, 32);
+                        }
 
-                        // topk_merge puts smallest values in DEST[0] and largest in DEST[1]
-                        // If core must keep smallest values, then keep DEST[1] instead of DEST[0]
-                        const uint32_t select_lower = dir ^ (i < j);
+                        // DEST[0] corresponds to the left tile slot in the requested direction.
+                        const uint32_t keep_left_slot = (i < j);
 
@@ -251,3 +246,3 @@ void kernel_main() {
                         uint32_t index_output_tile = index_dest_start;
-                        if (!select_lower) {
+                        if (!keep_left_slot) {
                             value_output_tile = input_dest_end;
diff --git a/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_single_row_multi_core.cpp b/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_single_row_multi_core.cpp
--- a/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_single_row_multi_core.cpp
+++ b/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_single_row_multi_core.cpp
@@ -147,22 +147,12 @@ void kernel_main() {
 
-                            uint32_t tile_input_low = input_dest_start;
-                            uint32_t tile_input_high = input_dest_end;
-                            uint32_t tile_index_low = index_dest_start;
-                            uint32_t tile_index_high = index_dest_end;
-
                             if (sub == 1) {
                                 // Use sort LLK only the last substage to sort the last pair of tiles - speed up
-                                ckernel::topk_local_sort(/*idst=*/0, (int)dir, /*end_phase(log2(K))=*/5);
+                                ckernel::topk_local_sort<stable, descending>(
+                                    /*idst=*/0, (int)dir, /*end_phase(log2(K))=*/5);
                             } else {
-                                // For all other stages use topk_merge to put the top K values in one tile, and the
-                                // bottom K values in another tile
-                                ckernel::topk_merge(/*idst=*/0, m_iter, /*k=*/32);
-
-                                // topk_merge puts smallest values in DEST[0] and largest in DEST[1]
-                                // We swap their indices when using descending order
+                                // Merge directly in the requested compare direction.
                                 if (dir) {
-                                    tile_input_low = input_dest_end;
-                                    tile_input_high = input_dest_start;
-                                    tile_index_low = index_dest_end;
-                                    tile_index_high = index_dest_start;
+                                    ckernel::topk_merge<true, stable, descending>(/*idst=*/0, m_iter, /*k=*/32);
+                                } else {
+                                    ckernel::topk_merge<false, stable, descending>(/*idst=*/0, m_iter, /*k=*/32);
                                 }
@@ -186,8 +176,8 @@ void kernel_main() {
                                 pack_reconfig_data_format(input_tensor_transposed_cb_id);
-                                pack_tile(tile_input_low, input_tensor_transposed_cb_id);
-                                pack_tile(tile_input_high, input_tensor_transposed_cb_id);
+                                pack_tile(input_dest_start, input_tensor_transposed_cb_id);
+                                pack_tile(input_dest_end, input_tensor_transposed_cb_id);
 
                                 pack_reconfig_data_format(index_tensor_transposed_cb_id);
-                                pack_tile(tile_index_low, index_tensor_transposed_cb_id);
-                                pack_tile(tile_index_high, index_tensor_transposed_cb_id);
+                                pack_tile(index_dest_start, index_tensor_transposed_cb_id);
+                                pack_tile(index_dest_end, index_tensor_transposed_cb_id);
                                 tile_regs_release();
@@ -249,8 +239,8 @@ void kernel_main() {
                                 pack_reconfig_data_format(input_tensor_output_cb_id);
-                                pack_tile(tile_input_low, input_tensor_output_cb_id);
-                                pack_tile(tile_input_high, input_tensor_output_cb_id);
+                                pack_tile(input_dest_start, input_tensor_output_cb_id);
+                                pack_tile(input_dest_end, input_tensor_output_cb_id);
 
                                 pack_reconfig_data_format(index_tensor_output_cb_id);
-                                pack_tile(tile_index_low, index_tensor_output_cb_id);
-                                pack_tile(tile_index_high, index_tensor_output_cb_id);
+                                pack_tile(index_dest_start, index_tensor_output_cb_id);
+                                pack_tile(index_dest_end, index_tensor_output_cb_id);
                                 tile_regs_release();
diff --git a/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_single_row_single_core.cpp b/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_single_row_single_core.cpp
--- a/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_single_row_single_core.cpp
+++ b/ttnn/cpp/ttnn/operations/data_movement/sort/device/kernels/compute/sort_single_row_single_core.cpp
@@ -176,3 +176,3 @@ void kernel_main() {
 
-        sort_Wt_tiles_row_to_bitonic_sequence(
+        sort_Wt_tiles_row_to_bitonic_sequence<stable, descending>(
             input_tensor_dfb,
@@ -228,20 +228,11 @@ void kernel_main() {
 
-                        uint32_t tile_input_low = input_dest_start;
-                        uint32_t tile_input_high = input_dest_end;
-                        uint32_t tile_index_low = index_dest_start;
-                        uint32_t tile_index_high = index_dest_end;
-
                         if (sub == 1) {
                             // Use sort LLK only the last stage to sort the last pair of tiles - speed up
-                            ckernel::topk_local_sort(/*idst=*/0, (int)dir, /*end_phase(log2(K))=*/5);
+                            ckernel::topk_local_sort<stable, descending>(
+                                /*idst=*/0, (int)dir, /*end_phase(log2(K))=*/5);
                         } else {
-                            ckernel::topk_merge(/*idst=*/0, m_iter, /*k=*/64);
-
                             if (dir) {
-                                // topk_merge puts smallest values in DEST[0] and largest in DEST[1]
-                                // We swap their indices when using descending order
-                                tile_input_low = input_dest_end;
-                                tile_input_high = input_dest_start;
-                                tile_index_low = index_dest_end;
-                                tile_index_high = index_dest_start;
+                                ckernel::topk_merge<true, stable, descending>(/*idst=*/0, m_iter, /*k=*/64);
+                            } else {
+                                ckernel::topk_merge<false, stable, descending>(/*idst=*/0, m_iter, /*k=*/64);
                             }
diff --git a/ttnn/cpp/ttnn/operations/data_movement/sort/device/sort_program_factory.cpp b/ttnn/cpp/ttnn/operations/data_movement/sort/device/sort_program_factory.cpp
--- a/ttnn/cpp/ttnn/operations/data_movement/sort/device/sort_program_factory.cpp
+++ b/ttnn/cpp/ttnn/operations/data_movement/sort/device/sort_program_factory.cpp
@@ -1128,2 +1128,3 @@ ttnn::device_operation::ProgramArtifacts SortProgramFactoryCrossCoreDataExchange
                 {"ascending", static_cast<uint32_t>(!attributes.descending)},
+                {"stable", static_cast<uint32_t>(attributes.stable)},
                 {"W_value_slice_bytes", W_value_slice_bytes},
diff --git a/ttnn/cpp/ttnn/operations/data_movement/sort/sort.cpp b/ttnn/cpp/ttnn/operations/data_movement/sort/sort.cpp
--- a/ttnn/cpp/ttnn/operations/data_movement/sort/sort.cpp
+++ b/ttnn/cpp/ttnn/operations/data_movement/sort/sort.cpp
@@ -8,2 +8,3 @@
 #include "ttnn/operations/core/core.hpp"
+#include "ttnn/operations/copy/typecast/typecast.hpp"
 #include "ttnn/operations/creation/creation.hpp"
@@ -231,4 +232,2 @@ std::vector<Tensor> sort(
     std::optional<std::tuple<Tensor&, Tensor&>> optional_output_tensors) {
-    TT_FATAL(!stable, "ttnn::sort: stable=True is not yet implemented.");
-
     const ttnn::Shape& original_lshape = input_tensor.logical_shape();
@@ -296,3 +295,3 @@ std::vector<Tensor> sort(
     // the output can be converted back to the sharded config after sorting.
-    const Tensor transform_input = input_tensor.memory_config().is_sharded()
+    const Tensor unsharded_input = input_tensor.memory_config().is_sharded()
                                        ? ttnn::to_memory_config(input_tensor, ttnn::DRAM_MEMORY_CONFIG)
@@ -300,2 +299,11 @@ std::vector<Tensor> sort(
 
+    // The SFPU bitonic primitive compares BF16/FP32 values. IEEE Float16 is
+    // exactly representable in Float32, so promote it once around the device
+    // sort instead of teaching every compare/merge and padding stage a second
+    // 16-bit encoding. A caller-provided Float16 values output cannot back the
+    // promoted sort and is therefore populated only after the final typecast.
+    const bool promote_float16 = input_tensor.dtype() == DataType::FLOAT16;
+    const Tensor transform_input =
+        promote_float16 ? ttnn::typecast(unsharded_input, DataType::FLOAT32) : unsharded_input;
+
     Tensor padded_input_tensor =
@@ -317,3 +325,3 @@ std::vector<Tensor> sort(
         const bool indices_layout_match = (out1.layout() == padded_input_tensor.layout());
-        preallocated_layout_mismatch = !values_layout_match || !indices_layout_match;
+        preallocated_layout_mismatch = promote_float16 || !values_layout_match || !indices_layout_match;
         if (!preallocated_layout_mismatch) {
@@ -335,2 +343,6 @@ std::vector<Tensor> sort(
 
+    if (promote_float16) {
+        results[0] = ttnn::typecast(results[0], DataType::FLOAT16);
+    }
+
     // The device op always writes to DRAM when sort_mem_cfg is sharded (to avoid
