diff --git tt_metal/impl/tensor/tensor_apis.cpp tt_metal/impl/tensor/tensor_apis.cpp
--- tt_metal/impl/tensor/tensor_apis.cpp
+++ tt_metal/impl/tensor/tensor_apis.cpp
@@ -1240,6 +1240,7 @@ HostTensor to_dtype(const HostTensor& input_tensor, DataType dtype) {
                 case DataType::BFLOAT8_B:
                     return with_src_and_dst.operator()<SrcType, CMAKE_UNIQUE_NAMESPACE::bfloat8_tag>();
                 case DataType::FLOAT32: return with_src_and_dst.operator()<SrcType, float>();
+                case DataType::FLOAT16: return with_src_and_dst.operator()<SrcType, float16>();
                 case DataType::BFLOAT16: return with_src_and_dst.operator()<SrcType, bfloat16>();
                 case DataType::UINT8: return with_src_and_dst.operator()<SrcType, uint8_t>();
                 case DataType::UINT16: return with_src_and_dst.operator()<SrcType, uint16_t>();
diff --git tt_metal/hw/inc/api/compute/eltwise_unary/typecast.h tt_metal/hw/inc/api/compute/eltwise_unary/typecast.h
--- tt_metal/hw/inc/api/compute/eltwise_unary/typecast.h
+++ tt_metal/hw/inc/api/compute/eltwise_unary/typecast.h
@@ -164,14 +164,25 @@ ALWI void typecast_tile(uint32_t idst) {
             (APPROX, 8 /* ITERATIONS */, DST_ACCUM_MODE),
             idst,
             VectorMode::RC));
-    } else if constexpr (in_format == DataFormat::UInt16 && out_format == DataFormat::Float32) {
+    } else if constexpr (
+        in_format == DataFormat::UInt16 &&
+        (out_format == DataFormat::Float32 || out_format == DataFormat::Float16)) {
         MATH(SFPU_UNARY_CALL(
             DST_SYNC_MODE,
             DST_ACCUM_MODE,
             calculate_typecast_uint16_to_fp32,
             (APPROX, 8 /* ITERATIONS */, DST_ACCUM_MODE),
             idst,
             VectorMode::RC));
+        if constexpr (out_format == DataFormat::Float16) {
+            MATH(SFPU_UNARY_CALL(
+                DST_SYNC_MODE,
+                DST_ACCUM_MODE,
+                cast_fp32_to_fp16a,
+                (APPROX, 8 /* ITERATIONS */),
+                idst,
+                VectorMode::RC));
+        }
     } else if constexpr (in_format == DataFormat::Float32 && out_format == DataFormat::Int32) {
         MATH(SFPU_UNARY_CALL(
             DST_SYNC_MODE,
@@ -189,6 +199,15 @@ ALWI void typecast_tile(uint32_t idst) {
             (APPROX, 8 /* ITERATIONS */),
             idst,
             VectorMode::RC));
+        if constexpr (out_format == DataFormat::Float16) {
+            MATH(SFPU_UNARY_CALL(
+                DST_SYNC_MODE,
+                DST_ACCUM_MODE,
+                cast_fp32_to_fp16a,
+                (APPROX, 8 /* ITERATIONS */),
+                idst,
+                VectorMode::RC));
+        }
     } else if constexpr (in_format == DataFormat::Bfp8_b && out_format == DataFormat::UInt16) {
         MATH(SFPU_UNARY_CALL(
             DST_SYNC_MODE,
@@ -245,14 +264,25 @@ ALWI void typecast_tile(uint32_t idst) {
             (APPROX, 8 /* ITERATIONS */),
             idst,
             VectorMode::RC));
-    } else if constexpr (in_format == DataFormat::UInt32 && out_format == DataFormat::Float32) {
+    } else if constexpr (
+        in_format == DataFormat::UInt32 &&
+        (out_format == DataFormat::Float32 || out_format == DataFormat::Float16)) {
         MATH(SFPU_UNARY_CALL(
             DST_SYNC_MODE,
             DST_ACCUM_MODE,
             calculate_typecast_uint32_to_fp32,
             (APPROX, 8 /* ITERATIONS */),
             idst,
             VectorMode::RC));
+        if constexpr (out_format == DataFormat::Float16) {
+            MATH(SFPU_UNARY_CALL(
+                DST_SYNC_MODE,
+                DST_ACCUM_MODE,
+                cast_fp32_to_fp16a,
+                (APPROX, 8 /* ITERATIONS */),
+                idst,
+                VectorMode::RC));
+        }
     } else if constexpr (in_format == DataFormat::Bfp8_b && out_format == DataFormat::UInt32) {
         MATH(SFPU_UNARY_CALL(
             DST_SYNC_MODE,
@@ -391,14 +420,25 @@ ALWI void typecast_tile(uint32_t idst) {
             (APPROX, 8 /* ITERATIONS */, (in_format == DataFormat::UInt16)),
             idst,
             VectorMode::RC));
-    } else if constexpr (in_format == DataFormat::UInt8 && out_format == DataFormat::Float32) {
+    } else if constexpr (
+        in_format == DataFormat::UInt8 &&
+        (out_format == DataFormat::Float32 || out_format == DataFormat::Float16)) {
         MATH(SFPU_UNARY_CALL(
             DST_SYNC_MODE,
             DST_ACCUM_MODE,
             calculate_typecast_uint32_to_fp32,
             (APPROX, 8 /* ITERATIONS */),
             idst,
             VectorMode::RC));
+        if constexpr (out_format == DataFormat::Float16) {
+            MATH(SFPU_UNARY_CALL(
+                DST_SYNC_MODE,
+                DST_ACCUM_MODE,
+                cast_fp32_to_fp16a,
+                (APPROX, 8 /* ITERATIONS */),
+                idst,
+                VectorMode::RC));
+        }
     } else if constexpr (
         in_format == DataFormat::UInt8 &&
         (out_format == DataFormat::Float16_b || out_format == DataFormat::Bfp8_b || out_format == DataFormat::Bfp4_b)) {
@@ -451,12 +489,16 @@ ALWI void typecast_tile_init() {
     } else if constexpr (in_format == DataFormat::UInt32 && out_format == DataFormat::UInt16) {
         MATH(SFPU_UNARY_INIT_FN(typecast, sfpu::init_typecast_uint32_to_uint16, (APPROX)));
     } else if constexpr (in_format == DataFormat::Int32 && out_format == DataFormat::UInt16) {
         MATH(SFPU_UNARY_INIT_FN(typecast, sfpu::init_typecast_int32_to_uint16, (APPROX)));
-    } else if constexpr (in_format == DataFormat::UInt32 && out_format == DataFormat::Float32) {
+    } else if constexpr (
+        in_format == DataFormat::UInt32 &&
+        (out_format == DataFormat::Float32 || out_format == DataFormat::Float16)) {
         MATH(SFPU_UNARY_INIT_FN(typecast, sfpu::init_typecast_uint32_to_fp32, (APPROX)));
     } else if constexpr (
         in_format == DataFormat::Int32 && (out_format == DataFormat::Float32 || out_format == DataFormat::Float16)) {
         MATH(SFPU_UNARY_INIT_FN(typecast, sfpu::init_typecast_int32_to_fp32, (APPROX)));
-    } else if constexpr (in_format == DataFormat::UInt16 && out_format == DataFormat::Float32) {
+    } else if constexpr (
+        in_format == DataFormat::UInt16 &&
+        (out_format == DataFormat::Float32 || out_format == DataFormat::Float16)) {
         MATH(SFPU_UNARY_INIT_FN(typecast, sfpu::init_typecast_uint16_to_fp32, (APPROX)));
     } else if constexpr (
@@ -484,8 +530,10 @@ ALWI void typecast_tile_init() {
     } else if constexpr (
         (in_format == DataFormat::Int32 || in_format == DataFormat::UInt32 || in_format == DataFormat::UInt16) &&
         out_format == DataFormat::UInt8) {
         MATH(SFPU_UNARY_INIT_FN(typecast, sfpu::init_typecast_uint_to_uint8, (APPROX)));
-    } else if constexpr (in_format == DataFormat::UInt8 && out_format == DataFormat::Float32) {
+    } else if constexpr (
+        in_format == DataFormat::UInt8 &&
+        (out_format == DataFormat::Float32 || out_format == DataFormat::Float16)) {
         MATH(SFPU_UNARY_INIT_FN(typecast, sfpu::init_typecast_uint32_to_fp32, (APPROX)));
     } else if constexpr (
         in_format == DataFormat::UInt8 &&
diff --git ttnn/cpp/ttnn/operations/copy/typecast/typecast.cpp ttnn/cpp/ttnn/operations/copy/typecast/typecast.cpp
--- ttnn/cpp/ttnn/operations/copy/typecast/typecast.cpp
+++ ttnn/cpp/ttnn/operations/copy/typecast/typecast.cpp
@@ -47,7 +47,8 @@ Tensor typecast_impl(
                                               input_dtype == DataType::BFLOAT4_B)) or
         (input_dtype == DataType::UINT16 and output_dtype == DataType::UINT8) or (input_dtype == DataType::UINT8);
     bool fp32_dest_acc_en = preserve_fp32_precision or output_dtype == DataType::UINT32 or
-                            output_dtype == DataType::INT32 or output_dtype == DataType::FLOAT32 or
+                            output_dtype == DataType::INT32 or output_dtype == DataType::FLOAT32 or
+                            (input_dtype == DataType::UINT16 and output_dtype == DataType::FLOAT16) or
                             input_dtype == DataType::UINT32 or input_dtype == DataType::INT32 or
                             output_dtype == DataType::FP8_E4M3 or input_dtype == DataType::FP8_E4M3;
     bool bfp8_pack_precise = (output_dtype == DataType::BFLOAT8_B);
diff --git ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_device_op_types.hpp ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_device_op_types.hpp
--- ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_device_op_types.hpp
+++ ttnn/cpp/ttnn/operations/copy/typecast/device/typecast_device_op_types.hpp
@@ -22,7 +22,13 @@ inline tt::DataFormat typecast_input_format(
 inline tt::DataFormat typecast_output_format(
     tt::tt_metal::DataType input_dtype, tt::tt_metal::DataType output_dtype) {
-    return input_dtype == tt::tt_metal::DataType::FLOAT32 &&
-                   (output_dtype == tt::tt_metal::DataType::FLOAT16 || output_dtype == tt::tt_metal::DataType::BFLOAT16)
+    const bool exact_float16_output =
+        output_dtype == tt::tt_metal::DataType::FLOAT16 &&
+        (input_dtype == tt::tt_metal::DataType::FLOAT32 || input_dtype == tt::tt_metal::DataType::UINT8 ||
+         input_dtype == tt::tt_metal::DataType::UINT16 || input_dtype == tt::tt_metal::DataType::UINT32 ||
+         input_dtype == tt::tt_metal::DataType::INT32);
+    const bool exact_bfloat16_output =
+        input_dtype == tt::tt_metal::DataType::FLOAT32 && output_dtype == tt::tt_metal::DataType::BFLOAT16;
+    return exact_float16_output || exact_bfloat16_output
                ? tt::DataFormat::UInt16
                : tt::tt_metal::datatype_to_dataformat_converter(output_dtype);
 }
