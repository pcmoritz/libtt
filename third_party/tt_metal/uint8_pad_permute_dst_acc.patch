diff --git ttnn/cpp/ttnn/operations/data_movement/fill_pad/device/fill_pad_program_factory.cpp ttnn/cpp/ttnn/operations/data_movement/fill_pad/device/fill_pad_program_factory.cpp
--- ttnn/cpp/ttnn/operations/data_movement/fill_pad/device/fill_pad_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/data_movement/fill_pad/device/fill_pad_program_factory.cpp
@@ -59,10 +59,12 @@ tt::tt_metal::ProgramDescriptor FillPadProgramFactory::create_descriptor(
     const bool is_fp32 = (input_tensor.dtype() == DataType::FLOAT32);
     const bool is_fp16 = (input_tensor.dtype() == DataType::FLOAT16);
     const bool is_uint32 = (input_tensor.dtype() == DataType::UINT32);
     const bool is_int32 = (input_tensor.dtype() == DataType::INT32);
-    // 32-bit integer types need fp32_dest_acc_en so DST holds full 32-bit values
+    // 32-bit integer types and unpacked UINT8 lanes need fp32_dest_acc_en
+    // so DST holds full 32-bit values
     // and where_tile<UInt32/Int32> can use INT32-mode SFPLOAD/SFPSTORE correctly.
-    const bool need_fp32_dest_acc = is_fp32 || is_uint32 || is_int32;
+    const bool need_fp32_dest_acc =
+        is_fp32 || is_uint32 || is_int32 || input_tensor.dtype() == DataType::UINT8;
     // Float types: raw bit pattern of fill_value for fill_tile_bitcast.
     // Integer types: packed native bit pattern for fill_tile_int.
     const uint32_t fill_bits = detail::pack_fill_value_for_dtype(input_tensor.dtype(), fill_value);
@@ -471,8 +473,9 @@ tt::tt_metal::ProgramDescriptor FillPadL1ShardedProgramFactory::create_descripto
     const bool is_fp32 = (input_tensor.dtype() == DataType::FLOAT32);
     const bool is_fp16 = (input_tensor.dtype() == DataType::FLOAT16);
     const bool is_uint32 = (input_tensor.dtype() == DataType::UINT32);
     const bool is_int32 = (input_tensor.dtype() == DataType::INT32);
-    const bool need_fp32_dest_acc = is_fp32 || is_uint32 || is_int32;
+    const bool need_fp32_dest_acc =
+        is_fp32 || is_uint32 || is_int32 || input_tensor.dtype() == DataType::UINT8;
     const uint32_t fill_bits = detail::pack_fill_value_for_dtype(input_tensor.dtype(), fill_value);
 
     // ---- CB indices ----
diff --git ttnn/cpp/ttnn/operations/data_movement/permute/device/permute_tiled_program_factory.cpp ttnn/cpp/ttnn/operations/data_movement/permute/device/permute_tiled_program_factory.cpp
--- ttnn/cpp/ttnn/operations/data_movement/permute/device/permute_tiled_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/data_movement/permute/device/permute_tiled_program_factory.cpp
@@ -177,7 +177,8 @@ tt::tt_metal::ProgramDescriptor PermuteDeviceOperation::MultiCoreTileInvariant:
     if (swap_hw) {
         std::vector<uint32_t> compute_kernel_args = {};
         bool fp32_dest_acc_en = cb_data_format == tt::DataFormat::Float32 || cb_data_format == tt::DataFormat::Int32 ||
-                                cb_data_format == tt::DataFormat::UInt32;
+                                cb_data_format == tt::DataFormat::UInt32 ||
+                                cb_data_format == tt::DataFormat::UInt8;
         std::vector<tt::tt_metal::UnpackToDestMode> unpack_to_dest_mode(
             NUM_CIRCULAR_BUFFERS, tt::tt_metal::UnpackToDestMode::Default);
         if (cb_data_format == tt::DataFormat::Float32) {
diff --git ttnn/cpp/ttnn/operations/data_movement/transpose/device/transpose_wh_program_factory.cpp ttnn/cpp/ttnn/operations/data_movement/transpose/device/transpose_wh_program_factory.cpp
--- ttnn/cpp/ttnn/operations/data_movement/transpose/device/transpose_wh_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/data_movement/transpose/device/transpose_wh_program_factory.cpp
@@ -149,8 +149,9 @@ tt::tt_metal::ProgramDescriptor TransposeWHProgramFactory::create_descriptor(
     Buffer* src0_buffer = input_tensor.buffer();
     IDevice* device = input_tensor.device();
 
     bool fp32_dest_acc_en = src0_cb_data_format == tt::DataFormat::Float32 ||
                             src0_cb_data_format == tt::DataFormat::Int32 ||
-                            src0_cb_data_format == tt::DataFormat::UInt32;
+                            src0_cb_data_format == tt::DataFormat::UInt32 ||
+                            src0_cb_data_format == tt::DataFormat::UInt8;
 
     auto compute_with_storage_grid_size = device->compute_with_storage_grid_size();
