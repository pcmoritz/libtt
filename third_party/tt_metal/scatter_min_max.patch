diff --git a/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/common.hpp b/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/common.hpp
--- a/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/common.hpp
+++ b/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/common.hpp
@@ -35,3 +35,3 @@ template <>
 struct df_to_std<DataFormat::Int32> {
-    using std_type = uint32_t;
+    using std_type = int32_t;
 };
diff --git a/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/dataflow/reader_bf16_reduction_scatter.cpp b/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/dataflow/reader_bf16_reduction_scatter.cpp
--- a/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/dataflow/reader_bf16_reduction_scatter.cpp
+++ b/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/dataflow/reader_bf16_reduction_scatter.cpp
@@ -72,6 +72,7 @@ FORCE_INLINE void scatter_along_chunk(
         volatile index_type& index_value = index_l1_read_ptr[index_in_index_chunk];
-        if (index_value < input_offset || index_value >= input_offset + input_chunk_size) {
+        if (index_value < input_offset ||
+            index_value >= input_offset + static_cast<index_type>(input_chunk_size)) {
             continue;
         }
-        if (index_value >= input_stick_size) {
+        if (index_value >= static_cast<index_type>(input_stick_size)) {
             continue;
diff --git a/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/dataflow/reader_scatter.cpp b/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/dataflow/reader_scatter.cpp
--- a/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/dataflow/reader_scatter.cpp
+++ b/ttnn/cpp/ttnn/operations/data_movement/scatter/device/kernels/dataflow/reader_scatter.cpp
@@ -84,6 +84,7 @@ FORCE_INLINE void scatter_along_chunk(
         volatile index_type& index_value = index_l1_read_ptr[index_in_index_chunk];
-        if (index_value < input_offset || index_value >= input_offset + input_chunk_size) {
+        if (index_value < static_cast<index_type>(input_offset) ||
+            index_value >= static_cast<index_type>(input_offset + input_chunk_size)) {
             continue;
         }
-        if (index_value >= input_stick_size) {
+        if (index_value >= static_cast<index_type>(input_stick_size)) {
             continue;
diff --git a/ttnn/cpp/ttnn/operations/data_movement/scatter/scatter.cpp b/ttnn/cpp/ttnn/operations/data_movement/scatter/scatter.cpp
--- a/ttnn/cpp/ttnn/operations/data_movement/scatter/scatter.cpp
+++ b/ttnn/cpp/ttnn/operations/data_movement/scatter/scatter.cpp
@@ -23,3 +23,3 @@ namespace CMAKE_UNIQUE_NAMESPACE {
 
-constexpr std::string_view ALLOWED_REDUCTIONS[] = {"add", "multiply"};
+constexpr std::string_view ALLOWED_REDUCTIONS[] = {"add", "multiply", "amin", "amax"};
 
@@ -66,3 +66,3 @@ void validate_inputs(
                 std::end(ALLOWED_REDUCTIONS),
-            "reduce must be either 'add' or 'multiply' (case-sensitive), got {}",
+            "unsupported scatter reduction '{}'",
             *opt_reduction_string);
