diff --git ttnn/cpp/ttnn/operations/reduction/topk/device/topk_device_operation.cpp ttnn/cpp/ttnn/operations/reduction/topk/device/topk_device_operation.cpp
index 0fd7eed..e272193 100644
--- ttnn/cpp/ttnn/operations/reduction/topk/device/topk_device_operation.cpp
+++ ttnn/cpp/ttnn/operations/reduction/topk/device/topk_device_operation.cpp
@@ -37,5 +37,4 @@ namespace ttnn::prim {
  *
- * 2. OUTPUT DATA TYPE: Must support 16-bit indices (dimension size < 65536)
- *    - Multi-core implementation currently only supports UInt16 indices
- *    - Dimensions >= 65536 force single-core execution with UInt32 indices
+ * 2. DIMENSION LIMIT: Dimension size must be less than 65536
+ *    - Larger dimensions force single-core execution
  *
@@ -67,4 +66,3 @@ TopKDeviceOperation::program_factory_t TopKDeviceOperation::select_program_facto

-    // Apply requirement #2: Multi-core implementation constraint
-    // Multi-core only supports UInt16 indices (dimension size must fit in 16 bits)
+    // Apply requirement #2: Multi-core dimension constraint
     multicore_supported &= (input_shape[args.dim] < std::numeric_limits<uint16_t>::max());
@@ -84,3 +82,4 @@ TopKDeviceOperation::program_factory_t TopKDeviceOperation::select_program_facto
             tt::tt_metal::datatype_to_dataformat_converter(input_tensor.dtype());
-        const tt::DataFormat index_cb_data_format = tt::DataFormat::UInt16;  // Multi-core always uses UInt16
+        const tt::DataFormat index_cb_data_format =
+            input_tensor.dtype() == DataType::FLOAT32 ? tt::DataFormat::UInt32 : tt::DataFormat::UInt16;

@@ -156,4 +155,5 @@ void TopKDeviceOperation::validate_on_program_cache_miss(
     TT_FATAL(
-        input_tensor_dtype == DataType::BFLOAT16 || input_tensor_dtype == DataType::BFLOAT8_B,
-        "Input tensor must be BFLOAT16, or BFLOAT8_B, got: {}",
+        input_tensor_dtype == DataType::BFLOAT16 || input_tensor_dtype == DataType::BFLOAT8_B ||
+            input_tensor_dtype == DataType::FLOAT32,
+        "Input tensor must be BFLOAT16, BFLOAT8_B, or FLOAT32, got: {}",
         input_tensor_dtype);
@@ -167,2 +167,5 @@ void TopKDeviceOperation::validate_on_program_cache_miss(
             indices_tensor_dtype);
+        TT_FATAL(
+            input_tensor_dtype != DataType::FLOAT32 || indices_tensor_dtype == DataType::UINT32,
+            "Optional input tensor must be UINT32 when input is FLOAT32");
     }
@@ -173,6 +176,2 @@ void TopKDeviceOperation::validate_on_program_cache_miss(
         const auto output_tensor1_dtype = std::get<1>(preallocated_outputs.value()).dtype();  // Indices tensor
-        TT_FATAL(
-            output_tensor0_dtype == DataType::BFLOAT16 || output_tensor0_dtype == DataType::BFLOAT8_B,
-            "Preallocated output tensor must be BFLOAT16 or BFLOAT8_B got: {}",
-            output_tensor0_dtype);
         TT_FATAL(
@@ -181,2 +180,5 @@ void TopKDeviceOperation::validate_on_program_cache_miss(
             output_tensor1_dtype);
+        TT_FATAL(
+            input_tensor_dtype != DataType::FLOAT32 || output_tensor1_dtype == DataType::UINT32,
+            "Preallocated indices tensor must be UINT32 when input is FLOAT32");
         TT_FATAL(
@@ -202,3 +204,4 @@ void TopKDeviceOperation::validate_on_program_cache_miss(
     bool can_run = false;
-    bool uint16_output = (input_shape[args.dim] <= std::numeric_limits<uint16_t>::max());
+    bool uint16_output = input_tensor_dtype != DataType::FLOAT32 &&
+                         input_shape[args.dim] <= std::numeric_limits<uint16_t>::max();

@@ -210,3 +213,4 @@ void TopKDeviceOperation::validate_on_program_cache_miss(
         tt::DataFormat value_cb_data_format = tt::tt_metal::datatype_to_dataformat_converter(input_tensor.dtype());
-        tt::DataFormat index_cb_data_format = tt::DataFormat::UInt16;
+        tt::DataFormat index_cb_data_format =
+            uint16_output ? tt::DataFormat::UInt16 : tt::DataFormat::UInt32;

@@ -266,3 +270,4 @@ TopKDeviceOperation::spec_return_value_t TopKDeviceOperation::compute_output_spe
     // Choose index data type based on dimension size (16-bit vs 32-bit indices)
-    const bool uint16_output = (input_shape[args.dim] <= std::numeric_limits<uint16_t>::max());  // 65535
+    const bool uint16_output = input_tensor.dtype() != DataType::FLOAT32 &&
+                               input_shape[args.dim] <= std::numeric_limits<uint16_t>::max();  // 65535

diff --git ttnn/cpp/ttnn/operations/reduction/topk/device/topk_multi_core_program_factory.cpp ttnn/cpp/ttnn/operations/reduction/topk/device/topk_multi_core_program_factory.cpp
index aad67a0..ed1744b 100644
--- ttnn/cpp/ttnn/operations/reduction/topk/device/topk_multi_core_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/reduction/topk/device/topk_multi_core_program_factory.cpp
@@ -458,4 +458,13 @@ tt::tt_metal::ProgramDescriptor TopKDeviceOperation::TopKMultiCoreProgramFactory
     compute_local_desc.compile_time_args = compute_args;
+    std::vector<tt::tt_metal::UnpackToDestMode> local_unpack_to_dest_mode(
+        NUM_CIRCULAR_BUFFERS, tt::tt_metal::UnpackToDestMode::Default);
+    if (input_cb_data_format == tt::DataFormat::Float32) {
+        local_unpack_to_dest_mode[input_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+        local_unpack_to_dest_mode[input_transposed_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+        local_unpack_to_dest_mode[values_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+    }
     compute_local_desc.config = ComputeConfigDescriptor{
+        .fp32_dest_acc_en = is32_bit_data,
         .dst_full_sync_en = false,
+        .unpack_to_dest_mode = std::move(local_unpack_to_dest_mode),
     };
@@ -487,4 +496,13 @@ tt::tt_metal::ProgramDescriptor TopKDeviceOperation::TopKMultiCoreProgramFactory
     compute_final_desc.compile_time_args = compute_args_final;
+    std::vector<tt::tt_metal::UnpackToDestMode> final_unpack_to_dest_mode(
+        NUM_CIRCULAR_BUFFERS, tt::tt_metal::UnpackToDestMode::Default);
+    if (input_cb_data_format == tt::DataFormat::Float32) {
+        final_unpack_to_dest_mode[gathered_values_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+        final_unpack_to_dest_mode[final_values_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+        final_unpack_to_dest_mode[values_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+    }
     compute_final_desc.config = ComputeConfigDescriptor{
+        .fp32_dest_acc_en = is32_bit_data,
         .dst_full_sync_en = false,
+        .unpack_to_dest_mode = std::move(final_unpack_to_dest_mode),
     };
diff --git ttnn/cpp/ttnn/operations/reduction/topk/device/topk_single_core_program_factory.cpp ttnn/cpp/ttnn/operations/reduction/topk/device/topk_single_core_program_factory.cpp
index 47ff32a..3f935c7 100644
--- ttnn/cpp/ttnn/operations/reduction/topk/device/topk_single_core_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/reduction/topk/device/topk_single_core_program_factory.cpp
@@ -30,3 +30,3 @@ tt::tt_metal::ProgramDescriptor TopKDeviceOperation::TopKSingleCoreProgramFactor
     const ttnn::Shape input_shape = input_tensor.padded_shape();
-    const bool uint16_output = (input_shape[args.dim] < std::numeric_limits<uint16_t>::max());
+    const bool uint16_output = index_tensor.dtype() == DataType::UINT16;

@@ -249,2 +249,10 @@ tt::tt_metal::ProgramDescriptor TopKDeviceOperation::TopKSingleCoreProgramFactor
     compute_desc.compile_time_args = compute_args;
+    std::vector<tt::tt_metal::UnpackToDestMode> unpack_to_dest_mode(
+        NUM_CIRCULAR_BUFFERS, tt::tt_metal::UnpackToDestMode::Default);
+    if (input_cb_data_format == tt::DataFormat::Float32) {
+        unpack_to_dest_mode[input_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+        unpack_to_dest_mode[transposed_val_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+        unpack_to_dest_mode[result_prep_val_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+        unpack_to_dest_mode[output_val_cb_index] = tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+    }
     compute_desc.config = ComputeConfigDescriptor{
@@ -252,2 +260,3 @@ tt::tt_metal::ProgramDescriptor TopKDeviceOperation::TopKSingleCoreProgramFactor
         .dst_full_sync_en = false,
+        .unpack_to_dest_mode = std::move(unpack_to_dest_mode),
     };
