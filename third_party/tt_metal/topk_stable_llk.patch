diff --git a/tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_topk.h b/tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_topk.h
--- a/tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_topk.h
+++ b/tt_metal/hw/ckernels/blackhole/metal/llk_api/llk_sfpu/ckernel_sfpu_topk.h
@@ -16,6 +16,6 @@ namespace sfpu {
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false>
+template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false, bool DESCENDING = false>
 inline void calculate_bitonic_topk_phases_steps(
     uint idir, uint i_end_phase, uint i_start_phase, uint i_end_step, uint i_start_step) {
-    _bitonic_topk_phases_steps<APPROXIMATION_MODE, is_fp32_dest_acc_en, STABLE_SORT>(
+    _bitonic_topk_phases_steps<APPROXIMATION_MODE, is_fp32_dest_acc_en, STABLE_SORT, DESCENDING>(
         idir, i_end_phase, i_start_phase, i_end_step, i_start_step);
@@ -23,10 +23,16 @@ inline void calculate_bitonic_topk_phases_steps(
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool idir = false, bool STABLE_SORT = false>
+template <
+    bool APPROXIMATION_MODE,
+    bool is_fp32_dest_acc_en,
+    bool idir,
+    bool STABLE_SORT,
+    bool DESCENDING>
 inline void calculate_bitonic_topk_merge(uint m_iter, uint k) {
-    _bitonic_topk_merge<APPROXIMATION_MODE, is_fp32_dest_acc_en, idir, STABLE_SORT>(m_iter, k);
+    _bitonic_topk_merge<APPROXIMATION_MODE, is_fp32_dest_acc_en, idir, STABLE_SORT, DESCENDING>(m_iter, k);
 }
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false>
+template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false, bool DESCENDING = false>
 inline void calculate_bitonic_topk_rebuild(uint idir, uint m_iter, uint k, uint logk, uint skip_second) {
-    _bitonic_topk_rebuild<APPROXIMATION_MODE, is_fp32_dest_acc_en, STABLE_SORT>(idir, m_iter, k, logk, skip_second);
+    _bitonic_topk_rebuild<APPROXIMATION_MODE, is_fp32_dest_acc_en, STABLE_SORT, DESCENDING>(
+        idir, m_iter, k, logk, skip_second);
 }
diff --git a/tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_topk.h b/tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_topk.h
--- a/tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_topk.h
+++ b/tt_metal/hw/ckernels/wormhole_b0/metal/llk_api/llk_sfpu/ckernel_sfpu_topk.h
@@ -16,6 +16,6 @@ namespace sfpu {
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false>
+template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false, bool DESCENDING = false>
 inline void calculate_bitonic_topk_phases_steps(
     uint idir, uint i_end_phase, uint i_start_phase, uint i_end_step, uint i_start_step) {
-    _bitonic_topk_phases_steps<APPROXIMATION_MODE, is_fp32_dest_acc_en, STABLE_SORT>(
+    _bitonic_topk_phases_steps<APPROXIMATION_MODE, is_fp32_dest_acc_en, STABLE_SORT, DESCENDING>(
         idir, i_end_phase, i_start_phase, i_end_step, i_start_step);
@@ -23,10 +23,16 @@ inline void calculate_bitonic_topk_phases_steps(
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool idir = false, bool STABLE_SORT = false>
+template <
+    bool APPROXIMATION_MODE,
+    bool is_fp32_dest_acc_en,
+    bool idir,
+    bool STABLE_SORT,
+    bool DESCENDING>
 inline void calculate_bitonic_topk_merge(uint m_iter, uint k) {
-    _bitonic_topk_merge<APPROXIMATION_MODE, is_fp32_dest_acc_en, idir, STABLE_SORT>(m_iter, k);
+    _bitonic_topk_merge<APPROXIMATION_MODE, is_fp32_dest_acc_en, idir, STABLE_SORT, DESCENDING>(m_iter, k);
 }
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false>
+template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false, bool DESCENDING = false>
 inline void calculate_bitonic_topk_rebuild(uint idir, uint m_iter, uint k, uint logk, uint skip_second) {
-    _bitonic_topk_rebuild<APPROXIMATION_MODE, is_fp32_dest_acc_en, STABLE_SORT>(idir, m_iter, k, logk, skip_second);
+    _bitonic_topk_rebuild<APPROXIMATION_MODE, is_fp32_dest_acc_en, STABLE_SORT, DESCENDING>(
+        idir, m_iter, k, logk, skip_second);
 }
diff --git a/tt_metal/tt-llk/tt_llk_blackhole/common/inc/sfpu/ckernel_sfpu_topk.h b/tt_metal/tt-llk/tt_llk_blackhole/common/inc/sfpu/ckernel_sfpu_topk.h
--- a/tt_metal/tt-llk/tt_llk_blackhole/common/inc/sfpu/ckernel_sfpu_topk.h
+++ b/tt_metal/tt-llk/tt_llk_blackhole/common/inc/sfpu/ckernel_sfpu_topk.h
@@ -140,3 +140,41 @@ inline void bitonic_topk_store16(std::uint32_t dist0, std::uint32_t dist1)
 
-template <bool STABLE_SORT>
+template <std::uint32_t VC, std::uint32_t VD, std::uint32_t MODE, bool DESCENDING>
+TT_ALWAYS_INLINE void topk_cmp_swap_stable()
+{
+    static_assert(
+        VC < p_sfpu::LREG4 && VD < p_sfpu::LREG4 && VC != VD,
+        "stable TopK requires two distinct value registers from LREG0-LREG3");
+    constexpr std::uint32_t IDX_VC = p_sfpu::LREG4 + VC;
+    constexpr std::uint32_t IDX_VD = p_sfpu::LREG4 + VD;
+
+    // Primary key: value compare-exchange.
+    TTI_SFPSWAP(0, VC, VD, MODE);
+    TTI_SFPSWAP(0, VC, VD, MODE);
+
+    // Predicate lanes whose post-SFPSWAP value bit patterns are identical.
+    // This sequence owns the condition code while it is active. Callers must
+    // not invoke it from inside another predicated SFPU sequence.
+    TTI_SFPENCC(3, 0, 0, 10);
+    TTI_SFPXOR(0, VC, VD, 0);
+    TTI_SFPSETCC(0, VD, 0, sfpi::SFPSETCC_MOD1_LREG_EQ0);
+
+    // Secondary key: index compare-exchange under tie mask.
+    // The index direction is relative to the current SFPSWAP min/max
+    // configuration. A bitonic `dir` flip therefore reverses the value and
+    // index comparisons together, as required for reversed subsequences.
+    if constexpr (!DESCENDING)
+    {
+        TTI_SFPSWAP(0, IDX_VC, IDX_VD, MODE);
+    }
+    else
+    {
+        TTI_SFPSWAP(0, IDX_VD, IDX_VC, MODE);
+    }
+    TTI_SFPENCC(3, 0, 0, 10);
+
+    // Restore values after XOR scratch operation.
+    TTI_SFPXOR(0, VC, VD, 0);
+}
+
+template <bool STABLE_SORT, bool DESCENDING = false>
 inline void bitonic_topk_ph3_st4_to_1(bool dir, bool &init_replay, int replay_start)
@@ -150,29 +188,22 @@ inline void bitonic_topk_ph3_st4_to_1(bool dir, bool &init_replay, int replay_st
 
-    constexpr int replay_count = STABLE_SORT ? 9 : 5;
-
-    if (init_replay)
+    if constexpr (STABLE_SORT)
     {
-        if constexpr (STABLE_SORT)
-        {
-            load_replay_buf<Exec>(
-                replay_start,
-                replay_count,
-                []
-                {
-                    // Step 4
-                    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX);
-                    TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
-                    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG1/3 NOP
-                    TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
-
-                    // Step 3 (1-cycle stall: shares LREG3 with Step 4 above)
-                    TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX);
-                    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG2/3 NOP
-                    TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/1 NOP
-                    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG2/3 NOP
-
-                    TTI_SFPTRANSP(0, 0, 0, 0);
-                });
-        }
-        else
+        // First pass.
+        topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        TTI_SFPTRANSP(0, 0, 0, 0);
+
+        // Second pass (matches legacy replay behavior).
+        topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        TTI_SFPTRANSP(0, 0, 0, 0);
+    }
+    else
+    {
+        constexpr int replay_count = 5;
+        if (init_replay)
         {
@@ -193,7 +224,8 @@ inline void bitonic_topk_ph3_st4_to_1(bool dir, bool &init_replay, int replay_st
                 });
+            init_replay = false;
+        }
+        else
+        {
+            lltt::replay(replay_start, replay_count);
         }
-        init_replay = false;
-    }
-    else
-    {
         lltt::replay(replay_start, replay_count);
@@ -201,4 +233,2 @@ inline void bitonic_topk_ph3_st4_to_1(bool dir, bool &init_replay, int replay_st
 
-    lltt::replay(replay_start, replay_count);
-
     if (dir == static_cast<bool>(SortDir::ArgMin))
@@ -214,10 +244,8 @@ inline void bitonic_topk_ph2_st3_to_1();
 
-template <>
-inline void bitonic_topk_ph2_st3_to_1<true>()
+template <bool DESCENDING>
+inline void bitonic_topk_ph2_st3_to_1_stable()
 {
     // Step 3
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/1 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG2/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/1 NOP
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
 
@@ -226,12 +254,8 @@ inline void bitonic_topk_ph2_st3_to_1<true>()
     // Step 2
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_01_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX); // Hides LREG0/2 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_01_MAX); // Hides LREG1/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX); // Hides LREG0/2 NOP
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_01_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX, DESCENDING>();
 
-    // Step 1 (1-cycle stall: shares LREG1 with Step 2 above)
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_01_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX); // Hides LREG0/1 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_01_MAX); // Hides LREG2/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX); // Hides LREG0/1 NOP
+    // Step 1
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_01_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX, DESCENDING>();
 
@@ -264,4 +288,4 @@ inline void bitonic_topk_ph1_st2_to_1();
 
-template <>
-inline void bitonic_topk_ph1_st2_to_1<true>()
+template <bool DESCENDING>
+inline void bitonic_topk_ph1_st2_to_1_stable()
 {
@@ -270,12 +294,8 @@ inline void bitonic_topk_ph1_st2_to_1<true>()
     // Step 2
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_02_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX); // Hides LREG0/2 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_02_MAX); // Hides LREG1/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX); // Hides LREG0/2 NOP
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_02_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX, DESCENDING>();
 
-    // Step 1 (1-cycle stall: shares LREG1 with Step 2 above)
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_02_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX); // Hides LREG0/1 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_02_MAX); // Hides LREG2/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX); // Hides LREG0/1 NOP
+    // Step 1
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_02_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX, DESCENDING>();
 
@@ -303,4 +323,4 @@ inline void bitonic_topk_ph0_st1_to_1();
 
-template <>
-inline void bitonic_topk_ph0_st1_to_1<true>()
+template <bool DESCENDING>
+inline void bitonic_topk_ph0_st1_to_1_stable()
 {
@@ -309,6 +329,4 @@ inline void bitonic_topk_ph0_st1_to_1<true>()
     // Step 1
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/1 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG2/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/1 NOP
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
 
@@ -332,4 +350,4 @@ inline void bitonic_topk_step_N(bool dir);
 
-template <>
-inline void bitonic_topk_step_N<true>(bool dir)
+template <bool DESCENDING>
+inline void bitonic_topk_step_N_stable(bool dir)
 {
@@ -338,6 +356,4 @@ inline void bitonic_topk_step_N<true>(bool dir)
     {
-        TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX);
-        TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
-        TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG1/3 NOP
-        TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
+        topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
     }
@@ -345,6 +361,4 @@ inline void bitonic_topk_step_N<true>(bool dir)
     {
-        TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG0, p_sfpswap::ALL_ROWS_MAX);
-        TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
-        TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG0, p_sfpswap::ALL_ROWS_MAX); // Hides LREG1/3 NOP
-        TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
+        topk_cmp_swap_stable<p_sfpu::LREG2, p_sfpu::LREG0, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG3, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
     }
@@ -407,3 +421,3 @@ inline void bitonic_topk_inc_x4_dest(std::uint32_t inc, bool cr)
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false>
+template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false, bool DESCENDING = false>
 inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, const int i_start_phase, const int i_end_step, const int i_start_step)
@@ -444,7 +458,5 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             }
-                            constexpr int replay_count = STABLE_SORT ? 6 : 4;
-                            if (init_phase)
+                            if constexpr (STABLE_SORT)
                             {
-                                load_replay_buf<Exec>(16, replay_count, [] { bitonic_topk_ph0_st1_to_1<STABLE_SORT>(); });
-                                init_phase = false;
+                                bitonic_topk_ph0_st1_to_1_stable<DESCENDING>();
                             }
@@ -452,3 +464,12 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             {
-                                lltt::replay(16, replay_count);
+                                constexpr int replay_count = 4;
+                                if (init_phase)
+                                {
+                                    load_replay_buf<Exec>(16, replay_count, [] { bitonic_topk_ph0_st1_to_1<STABLE_SORT>(); });
+                                    init_phase = false;
+                                }
+                                else
+                                {
+                                    lltt::replay(16, replay_count);
+                                }
                             }
@@ -470,7 +491,5 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             lltt::replay(0, 8);
-                            constexpr int replay_count = STABLE_SORT ? 10 : 6;
-                            if (init_phase)
+                            if constexpr (STABLE_SORT)
                             {
-                                load_replay_buf<Exec>(16, replay_count, [] { bitonic_topk_ph1_st2_to_1<STABLE_SORT>(); });
-                                init_phase = false;
+                                bitonic_topk_ph1_st2_to_1_stable<DESCENDING>();
                             }
@@ -478,3 +497,12 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             {
-                                lltt::replay(16, replay_count);
+                                constexpr int replay_count = 6;
+                                if (init_phase)
+                                {
+                                    load_replay_buf<Exec>(16, replay_count, [] { bitonic_topk_ph1_st2_to_1<STABLE_SORT>(); });
+                                    init_phase = false;
+                                }
+                                else
+                                {
+                                    lltt::replay(16, replay_count);
+                                }
                             }
@@ -487,7 +515,5 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             lltt::replay(0, 8);
-                            constexpr int replay_count = STABLE_SORT ? 14 : 9;
-                            if (init_phase)
+                            if constexpr (STABLE_SORT)
                             {
-                                load_replay_buf<Exec>(16, replay_count, [] { bitonic_topk_ph2_st3_to_1<STABLE_SORT>(); });
-                                init_phase = false;
+                                bitonic_topk_ph2_st3_to_1_stable<DESCENDING>();
                             }
@@ -495,3 +521,12 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             {
-                                lltt::replay(16, replay_count);
+                                constexpr int replay_count = 9;
+                                if (init_phase)
+                                {
+                                    load_replay_buf<Exec>(16, replay_count, [] { bitonic_topk_ph2_st3_to_1<STABLE_SORT>(); });
+                                    init_phase = false;
+                                }
+                                else
+                                {
+                                    lltt::replay(16, replay_count);
+                                }
                             }
@@ -504,3 +539,3 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             lltt::replay(0, 8);
-                            bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_phase, 16);
+                            bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_phase, 16);
                             lltt::replay(8, 8);
@@ -530,3 +565,10 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                                     bitonic_topk_load16<is_fp32_dest_acc_en>(4, 2 * dist); // load/store with offset of face 1 (in row major face layout)
-                                    bitonic_topk_step_N<STABLE_SORT>(dir);
+                                    if constexpr (STABLE_SORT)
+                                    {
+                                        bitonic_topk_step_N_stable<DESCENDING>(dir);
+                                    }
+                                    else
+                                    {
+                                        bitonic_topk_step_N<STABLE_SORT>(dir);
+                                    }
                                     bitonic_topk_store16<is_fp32_dest_acc_en, false>(
@@ -560,3 +602,3 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             lltt::replay(0, 8);
-                            bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_phase, 16);
+                            bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_phase, 16);
                             lltt::replay(8, 8);
@@ -576,3 +618,10 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool top_min, bool STABLE_SORT = false>
+// top_min selects this merge stage's value comparator; DESCENDING selects
+// the stable tie-break direction for the complete sort.
+template <
+    bool APPROXIMATION_MODE,
+    bool is_fp32_dest_acc_en,
+    bool top_min,
+    bool STABLE_SORT,
+    bool DESCENDING>
 inline void _bitonic_topk_merge(const int m_iter, const int k)
@@ -601,6 +650,15 @@ inline void _bitonic_topk_merge(const int m_iter, const int k)
                     bitonic_topk_load8<is_fp32_dest_acc_en>(dst_offset, ld_dist);
-                    TTI_SFPSWAP(0, top_min ? p_sfpu::LREG1 : p_sfpu::LREG0, top_min ? p_sfpu::LREG0 : p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX);
                     if constexpr (STABLE_SORT)
                     {
-                        // 1-cycle stall: second swap for index tracking on same LREGs
+                        if constexpr (top_min)
+                        {
+                            topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG0, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+                        }
+                        else
+                        {
+                            topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+                        }
+                    }
+                    else
+                    {
                         TTI_SFPSWAP(0, top_min ? p_sfpu::LREG1 : p_sfpu::LREG0, top_min ? p_sfpu::LREG0 : p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX);
@@ -628,3 +686,3 @@ inline void _bitonic_topk_merge(const int m_iter, const int k)
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false>
+template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false, bool DESCENDING = false>
 inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k, const int logk, const int skip_second)
@@ -666,3 +724,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                                 bitonic_topk_load8<is_fp32_dest_acc_en>(0, ld_offset);
-                                bitonic_topk_ph1_st2_to_1<STABLE_SORT>();
+                                bitonic_topk_ph1_st2_to_1_stable<DESCENDING>();
                                 bitonic_topk_store8<is_fp32_dest_acc_en>(0, ld_offset);
@@ -703,3 +761,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                                 bitonic_topk_load16<is_fp32_dest_acc_en>(ld_offset, ld_dist);
-                                bitonic_topk_ph1_st2_to_1<STABLE_SORT>();
+                                bitonic_topk_ph1_st2_to_1_stable<DESCENDING>();
                                 bitonic_topk_store16<is_fp32_dest_acc_en, true>(ld_offset, ld_dist);
@@ -745,3 +803,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                             bitonic_topk_load16<is_fp32_dest_acc_en>(4, ld_offset);
-                            bitonic_topk_ph2_st3_to_1<STABLE_SORT>();
+                            bitonic_topk_ph2_st3_to_1_stable<DESCENDING>();
                             bitonic_topk_store16<is_fp32_dest_acc_en, true>(4, ld_offset);
@@ -786,3 +844,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                             bitonic_topk_load16<is_fp32_dest_acc_en>(4, 8);
-                            bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_rebuild, 8);
+                            bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_rebuild, 8);
                             bitonic_topk_store16<is_fp32_dest_acc_en, true>(4, 8);
@@ -799,3 +857,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                                 load_replay_buf<Exec>(0, 8, [] { bitonic_topk_load16<is_fp32_dest_acc_en>(4, 8); });
-                                bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_rebuild, 8);
+                                bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_rebuild, 8);
                                 load_replay_buf<Exec>(
@@ -815,3 +873,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                                 lltt::replay(0, 8);
-                                bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_rebuild, 8);
+                                bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_rebuild, 8);
                                 lltt::replay(13, 12);
@@ -843,3 +901,10 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                                 bitonic_topk_load16<is_fp32_dest_acc_en>(4, 2 * dist); // load/store with offset of face 1 (in row major face layout)
-                                bitonic_topk_step_N<STABLE_SORT>(dir);
+                                if constexpr (STABLE_SORT)
+                                {
+                                    bitonic_topk_step_N_stable<DESCENDING>(dir);
+                                }
+                                else
+                                {
+                                    bitonic_topk_step_N<STABLE_SORT>(dir);
+                                }
                                 bitonic_topk_store16<is_fp32_dest_acc_en, false>(4, 2 * dist); // load/store with offset of face 1 (in row major face layout)
@@ -874,3 +939,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                             load_replay_buf<Exec>(0, 8, [] { bitonic_topk_load16<is_fp32_dest_acc_en>(4, 8); });
-                            bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_rebuild, 8);
+                            bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_rebuild, 8);
                             load_replay_buf<Exec>(17, 8, [] { bitonic_topk_store16<is_fp32_dest_acc_en, true>(4, 8); });
@@ -880,3 +945,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                             lltt::replay(0, 8);
-                            bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_rebuild, 8);
+                            bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_rebuild, 8);
                             lltt::replay(17, 8);
diff --git a/tt_metal/tt-llk/tt_llk_wormhole_b0/common/inc/sfpu/ckernel_sfpu_topk.h b/tt_metal/tt-llk/tt_llk_wormhole_b0/common/inc/sfpu/ckernel_sfpu_topk.h
--- a/tt_metal/tt-llk/tt_llk_wormhole_b0/common/inc/sfpu/ckernel_sfpu_topk.h
+++ b/tt_metal/tt-llk/tt_llk_wormhole_b0/common/inc/sfpu/ckernel_sfpu_topk.h
@@ -140,3 +140,41 @@ inline void bitonic_topk_store16(std::uint32_t dist0, std::uint32_t dist1)
 
-template <bool STABLE_SORT>
+template <std::uint32_t VC, std::uint32_t VD, std::uint32_t MODE, bool DESCENDING>
+TT_ALWAYS_INLINE void topk_cmp_swap_stable()
+{
+    static_assert(
+        VC < p_sfpu::LREG4 && VD < p_sfpu::LREG4 && VC != VD,
+        "stable TopK requires two distinct value registers from LREG0-LREG3");
+    constexpr std::uint32_t IDX_VC = p_sfpu::LREG4 + VC;
+    constexpr std::uint32_t IDX_VD = p_sfpu::LREG4 + VD;
+
+    // Primary key: value compare-exchange.
+    TTI_SFPSWAP(0, VC, VD, MODE);
+    TTI_SFPSWAP(0, VC, VD, MODE);
+
+    // Predicate lanes whose post-SFPSWAP value bit patterns are identical.
+    // This sequence owns the condition code while it is active. Callers must
+    // not invoke it from inside another predicated SFPU sequence.
+    TTI_SFPENCC(3, 0, 0, 10);
+    TTI_SFPXOR(0, VC, VD, 0);
+    TTI_SFPSETCC(0, VD, 0, sfpi::SFPSETCC_MOD1_LREG_EQ0);
+
+    // Secondary key: index compare-exchange under tie mask.
+    // The index direction is relative to the current SFPSWAP min/max
+    // configuration. A bitonic `dir` flip therefore reverses the value and
+    // index comparisons together, as required for reversed subsequences.
+    if constexpr (!DESCENDING)
+    {
+        TTI_SFPSWAP(0, IDX_VC, IDX_VD, MODE);
+    }
+    else
+    {
+        TTI_SFPSWAP(0, IDX_VD, IDX_VC, MODE);
+    }
+    TTI_SFPENCC(3, 0, 0, 10);
+
+    // Restore values after XOR scratch operation.
+    TTI_SFPXOR(0, VC, VD, 0);
+}
+
+template <bool STABLE_SORT, bool DESCENDING = false>
 inline void bitonic_topk_ph3_st4_to_1(bool dir, bool &init_replay, int replay_start)
@@ -150,24 +188,25 @@ inline void bitonic_topk_ph3_st4_to_1(bool dir, bool &init_replay, int replay_st
 
-    constexpr int replay_count = STABLE_SORT ? 9 : 5;
-
-    if (init_replay)
+    if constexpr (STABLE_SORT)
     {
-        lltt::record<lltt::Exec>(replay_start, replay_count);
+        // First pass.
+        topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        TTI_SFPTRANSP(0, 0, 0, 0);
 
-        if constexpr (STABLE_SORT)
+        // Second pass.
+        topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        TTI_SFPTRANSP(0, 0, 0, 0);
+    }
+    else
+    {
+        constexpr int replay_count = 5;
+        if (init_replay)
         {
-            // Step 4
-            TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX);
-            TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
-            TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG1/3 NOP
-            TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
+            lltt::record<lltt::Exec>(replay_start, replay_count);
 
-            // Step 3 (1-cycle stall: shares LREG3 with Step 4 above)
-            TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX);
-            TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG2/3 NOP
-            TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/1 NOP
-            TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG2/3 NOP
-        }
-        else
-        {
             // Step 4
@@ -179,10 +218,11 @@ inline void bitonic_topk_ph3_st4_to_1(bool dir, bool &init_replay, int replay_st
             TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX);
-        }
 
-        TTI_SFPTRANSP(0, 0, 0, 0);
+            TTI_SFPTRANSP(0, 0, 0, 0);
 
-        init_replay = false;
-    }
-    else
-    {
+            init_replay = false;
+        }
+        else
+        {
+            lltt::replay(replay_start, replay_count);
+        }
         lltt::replay(replay_start, replay_count);
@@ -190,4 +230,2 @@ inline void bitonic_topk_ph3_st4_to_1(bool dir, bool &init_replay, int replay_st
 
-    lltt::replay(replay_start, replay_count);
-
     if (dir == static_cast<bool>(SortDir::ArgMin))
@@ -203,10 +241,8 @@ inline void bitonic_topk_ph2_st3_to_1();
 
-template <>
-inline void bitonic_topk_ph2_st3_to_1<true>()
+template <bool DESCENDING>
+inline void bitonic_topk_ph2_st3_to_1_stable()
 {
     // Step 3
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/1 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG2/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/1 NOP
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
 
@@ -215,12 +251,8 @@ inline void bitonic_topk_ph2_st3_to_1<true>()
     // Step 2
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_01_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX); // Hides LREG0/2 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_01_MAX); // Hides LREG1/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX); // Hides LREG0/2 NOP
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_01_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX, DESCENDING>();
 
-    // Step 1 (1-cycle stall: shares LREG1 with Step 2 above)
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_01_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX); // Hides LREG0/1 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_01_MAX); // Hides LREG2/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX); // Hides LREG0/1 NOP
+    // Step 1
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_01_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_01_MAX, DESCENDING>();
 
@@ -253,4 +285,4 @@ inline void bitonic_topk_ph1_st2_to_1();
 
-template <>
-inline void bitonic_topk_ph1_st2_to_1<true>()
+template <bool DESCENDING>
+inline void bitonic_topk_ph1_st2_to_1_stable()
 {
@@ -259,12 +291,8 @@ inline void bitonic_topk_ph1_st2_to_1<true>()
     // Step 2
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_02_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX); // Hides LREG0/2 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_02_MAX); // Hides LREG1/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX); // Hides LREG0/2 NOP
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ROWS_02_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX, DESCENDING>();
 
-    // Step 1 (1-cycle stall: shares LREG1 with Step 2 above)
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_02_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX); // Hides LREG0/1 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_02_MAX); // Hides LREG2/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX); // Hides LREG0/1 NOP
+    // Step 1
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ROWS_02_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG2, p_sfpu::LREG3, p_sfpswap::ROWS_02_MAX, DESCENDING>();
 
@@ -292,4 +320,4 @@ inline void bitonic_topk_ph0_st1_to_1();
 
-template <>
-inline void bitonic_topk_ph0_st1_to_1<true>()
+template <bool DESCENDING>
+inline void bitonic_topk_ph0_st1_to_1_stable()
 {
@@ -298,6 +326,4 @@ inline void bitonic_topk_ph0_st1_to_1<true>()
     // Step 1
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX);
-    TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/1 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG2/3 NOP
-    TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/1 NOP
+    topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+    topk_cmp_swap_stable<p_sfpu::LREG3, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
 
@@ -321,4 +347,4 @@ inline void bitonic_topk_step_N(bool dir);
 
-template <>
-inline void bitonic_topk_step_N<true>(bool dir)
+template <bool DESCENDING>
+inline void bitonic_topk_step_N_stable(bool dir)
 {
@@ -327,6 +353,4 @@ inline void bitonic_topk_step_N<true>(bool dir)
     {
-        TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX);
-        TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
-        TTI_SFPSWAP(0, p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX); // Hides LREG1/3 NOP
-        TTI_SFPSWAP(0, p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
+        topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG2, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG3, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
     }
@@ -334,6 +358,4 @@ inline void bitonic_topk_step_N<true>(bool dir)
     {
-        TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG0, p_sfpswap::ALL_ROWS_MAX);
-        TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
-        TTI_SFPSWAP(0, p_sfpu::LREG2, p_sfpu::LREG0, p_sfpswap::ALL_ROWS_MAX); // Hides LREG1/3 NOP
-        TTI_SFPSWAP(0, p_sfpu::LREG3, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX); // Hides LREG0/2 NOP
+        topk_cmp_swap_stable<p_sfpu::LREG2, p_sfpu::LREG0, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+        topk_cmp_swap_stable<p_sfpu::LREG3, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
     }
@@ -396,3 +418,3 @@ inline void bitonic_topk_inc_x4_dest(std::uint32_t inc, bool cr)
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false>
+template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false, bool DESCENDING = false>
 inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, const int i_start_phase, const int i_end_step, const int i_start_step)
@@ -434,8 +456,5 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             }
-                            constexpr int replay_count = STABLE_SORT ? 6 : 4;
-                            if (init_phase)
+                            if constexpr (STABLE_SORT)
                             {
-                                lltt::record<lltt::Exec>(16, replay_count);
-                                bitonic_topk_ph0_st1_to_1<STABLE_SORT>();
-                                init_phase = false;
+                                bitonic_topk_ph0_st1_to_1_stable<DESCENDING>();
                             }
@@ -443,3 +462,13 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             {
-                                lltt::replay(16, replay_count);
+                                constexpr int replay_count = 4;
+                                if (init_phase)
+                                {
+                                    lltt::record<lltt::Exec>(16, replay_count);
+                                    bitonic_topk_ph0_st1_to_1<STABLE_SORT>();
+                                    init_phase = false;
+                                }
+                                else
+                                {
+                                    lltt::replay(16, replay_count);
+                                }
                             }
@@ -462,8 +491,5 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             lltt::replay(0, 8);
-                            constexpr int replay_count = STABLE_SORT ? 10 : 6;
-                            if (init_phase)
+                            if constexpr (STABLE_SORT)
                             {
-                                lltt::record<lltt::Exec>(16, replay_count);
-                                bitonic_topk_ph1_st2_to_1<STABLE_SORT>();
-                                init_phase = false;
+                                bitonic_topk_ph1_st2_to_1_stable<DESCENDING>();
                             }
@@ -471,3 +497,13 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             {
-                                lltt::replay(16, replay_count);
+                                constexpr int replay_count = 6;
+                                if (init_phase)
+                                {
+                                    lltt::record<lltt::Exec>(16, replay_count);
+                                    bitonic_topk_ph1_st2_to_1<STABLE_SORT>();
+                                    init_phase = false;
+                                }
+                                else
+                                {
+                                    lltt::replay(16, replay_count);
+                                }
                             }
@@ -480,8 +516,5 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             lltt::replay(0, 8);
-                            constexpr int replay_count = STABLE_SORT ? 14 : 9;
-                            if (init_phase)
+                            if constexpr (STABLE_SORT)
                             {
-                                lltt::record<lltt::Exec>(16, replay_count);
-                                bitonic_topk_ph2_st3_to_1<STABLE_SORT>();
-                                init_phase = false;
+                                bitonic_topk_ph2_st3_to_1_stable<DESCENDING>();
                             }
@@ -489,3 +522,13 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             {
-                                lltt::replay(16, replay_count);
+                                constexpr int replay_count = 9;
+                                if (init_phase)
+                                {
+                                    lltt::record<lltt::Exec>(16, replay_count);
+                                    bitonic_topk_ph2_st3_to_1<STABLE_SORT>();
+                                    init_phase = false;
+                                }
+                                else
+                                {
+                                    lltt::replay(16, replay_count);
+                                }
                             }
@@ -498,3 +541,3 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             lltt::replay(0, 8);
-                            bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_phase, 16);
+                            bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_phase, 16);
                             lltt::replay(8, 8);
@@ -524,3 +567,10 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                                     bitonic_topk_load16<is_fp32_dest_acc_en>(4, 2 * dist); // load/store with offset of face 1 (in row major face layout)
-                                    bitonic_topk_step_N<STABLE_SORT>(dir);
+                                    if constexpr (STABLE_SORT)
+                                    {
+                                        bitonic_topk_step_N_stable<DESCENDING>(dir);
+                                    }
+                                    else
+                                    {
+                                        bitonic_topk_step_N<STABLE_SORT>(dir);
+                                    }
                                     bitonic_topk_store16<is_fp32_dest_acc_en, false>(
@@ -554,3 +604,3 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
                             lltt::replay(0, 8);
-                            bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_phase, 16);
+                            bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_phase, 16);
                             lltt::replay(8, 8);
@@ -570,3 +620,10 @@ inline void _bitonic_topk_phases_steps(const int idir, const int i_end_phase, co
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool top_min, bool STABLE_SORT = false>
+// top_min selects this merge stage's value comparator; DESCENDING selects
+// the stable tie-break direction for the complete sort.
+template <
+    bool APPROXIMATION_MODE,
+    bool is_fp32_dest_acc_en,
+    bool top_min,
+    bool STABLE_SORT,
+    bool DESCENDING>
 inline void _bitonic_topk_merge(const int m_iter, const int k)
@@ -595,6 +652,15 @@ inline void _bitonic_topk_merge(const int m_iter, const int k)
                     bitonic_topk_load8<is_fp32_dest_acc_en>(dst_offset, ld_dist);
-                    TTI_SFPSWAP(0, top_min ? p_sfpu::LREG1 : p_sfpu::LREG0, top_min ? p_sfpu::LREG0 : p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX);
                     if constexpr (STABLE_SORT)
                     {
-                        // 1-cycle stall: second swap for index tracking on same LREGs
+                        if constexpr (top_min)
+                        {
+                            topk_cmp_swap_stable<p_sfpu::LREG1, p_sfpu::LREG0, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+                        }
+                        else
+                        {
+                            topk_cmp_swap_stable<p_sfpu::LREG0, p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX, DESCENDING>();
+                        }
+                    }
+                    else
+                    {
                         TTI_SFPSWAP(0, top_min ? p_sfpu::LREG1 : p_sfpu::LREG0, top_min ? p_sfpu::LREG0 : p_sfpu::LREG1, p_sfpswap::ALL_ROWS_MAX);
@@ -622,3 +688,3 @@ inline void _bitonic_topk_merge(const int m_iter, const int k)
 
-template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false>
+template <bool APPROXIMATION_MODE, bool is_fp32_dest_acc_en, bool STABLE_SORT = false, bool DESCENDING = false>
 inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k, const int logk, const int skip_second)
@@ -659,3 +725,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                                 bitonic_topk_load8<is_fp32_dest_acc_en>(0, ld_offset);
-                                bitonic_topk_ph1_st2_to_1<STABLE_SORT>();
+                                bitonic_topk_ph1_st2_to_1_stable<DESCENDING>();
                                 bitonic_topk_store8<is_fp32_dest_acc_en>(0, ld_offset);
@@ -692,3 +758,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                                 bitonic_topk_load16<is_fp32_dest_acc_en>(ld_offset, ld_dist);
-                                bitonic_topk_ph1_st2_to_1<STABLE_SORT>();
+                                bitonic_topk_ph1_st2_to_1_stable<DESCENDING>();
                                 bitonic_topk_store16<is_fp32_dest_acc_en, true>(ld_offset, ld_dist);
@@ -729,3 +795,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                             bitonic_topk_load16<is_fp32_dest_acc_en>(4, ld_offset);
-                            bitonic_topk_ph2_st3_to_1<STABLE_SORT>();
+                            bitonic_topk_ph2_st3_to_1_stable<DESCENDING>();
                             bitonic_topk_store16<is_fp32_dest_acc_en, true>(4, ld_offset);
@@ -765,3 +831,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                             bitonic_topk_load16<is_fp32_dest_acc_en>(4, 8);
-                            bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_rebuild, 8);
+                            bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_rebuild, 8);
                             bitonic_topk_store16<is_fp32_dest_acc_en, true>(4, 8);
@@ -779,3 +845,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                                 bitonic_topk_load16<is_fp32_dest_acc_en>(4, 8);
-                                bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_rebuild, 8);
+                                bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_rebuild, 8);
                                 lltt::record<lltt::Exec>(13, 12);
@@ -790,3 +856,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                                 lltt::replay(0, 8);
-                                bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_rebuild, 8);
+                                bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_rebuild, 8);
                                 lltt::replay(13, 12);
@@ -818,3 +884,10 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                                 bitonic_topk_load16<is_fp32_dest_acc_en>(4, 2 * dist); // load/store with offset of face 1 (in row major face layout)
-                                bitonic_topk_step_N<STABLE_SORT>(dir);
+                                if constexpr (STABLE_SORT)
+                                {
+                                    bitonic_topk_step_N_stable<DESCENDING>(dir);
+                                }
+                                else
+                                {
+                                    bitonic_topk_step_N<STABLE_SORT>(dir);
+                                }
                                 bitonic_topk_store16<is_fp32_dest_acc_en, false>(4, 2 * dist); // load/store with offset of face 1 (in row major face layout)
@@ -850,3 +923,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                             bitonic_topk_load16<is_fp32_dest_acc_en>(4, 8);
-                            bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_rebuild, 8);
+                            bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_rebuild, 8);
                             lltt::record<lltt::Exec>(17, 8);
@@ -857,3 +930,3 @@ inline void _bitonic_topk_rebuild(const bool idir, const int m_iter, const int k
                             lltt::replay(0, 8);
-                            bitonic_topk_ph3_st4_to_1<STABLE_SORT>(dir, init_rebuild, 8);
+                            bitonic_topk_ph3_st4_to_1<STABLE_SORT, DESCENDING>(dir, init_rebuild, 8);
                             lltt::replay(17, 8);
