--- tt_metal/jit_build/build.cpp
+++ tt_metal/jit_build/build.cpp
@@ -85,6 +85,18 @@ void hard_link_or_copy(const std::filesystem::path& target, const std::filesystem::path& link) {
     }
 }
 
+std::string get_runtime_asset_root(const std::string& source_root) {
+    const char* asset_root = std::getenv("TT_METAL_RUNTIME_ASSET_ROOT");
+    if (asset_root == nullptr || asset_root[0] == '\0') {
+        return source_root;
+    }
+    std::string path = asset_root;
+    if (path.back() != '/') {
+        path.push_back('/');
+    }
+    return path;
+}
+
 }  // namespace
 
 std::string get_default_root_path() {
@@ -378,7 +390,8 @@ JitBuildState::JitBuildState(const JitBuildEnv& env, const JitBuiltStateConfig&
         this->lflags_ += common_flags;
     }
 
-    this->linker_script_ = env_.root_ + jit_build_query.linker_script(params);
+    const std::string runtime_asset_root = get_runtime_asset_root(env_.root_);
+    this->linker_script_ = runtime_asset_root + jit_build_query.linker_script(params);
 
     // Source files
     {
@@ -400,7 +413,7 @@ JitBuildState::JitBuildState(const JitBuildEnv& env, const JitBuiltStateConfig&
     {
         auto it = std::back_inserter(this->extra_link_objs_);
         for (const auto& obj : jit_build_query.link_objs(params)) {
-            fmt::format_to(it, "{}{} ", env_.root_, obj);
+            fmt::format_to(it, "{}{} ", runtime_asset_root, obj);
         }
     }
 
