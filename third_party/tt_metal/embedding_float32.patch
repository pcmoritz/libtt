diff --git ttnn/cpp/ttnn/operations/embedding/device/embedding_device_operation.cpp ttnn/cpp/ttnn/operations/embedding/device/embedding_device_operation.cpp
--- ttnn/cpp/ttnn/operations/embedding/device/embedding_device_operation.cpp
+++ ttnn/cpp/ttnn/operations/embedding/device/embedding_device_operation.cpp
@@ -34,5 +34,7 @@ void EmbeddingsDeviceOperation::validate_on_program_cache_miss(
         weights.layout() == Layout::ROW_MAJOR, "Weights tensor layout must be ROW_MAJOR but got {}", weights.layout());
     TT_FATAL(a.dtype() == DataType::UINT32 or a.dtype() == DataType::BFLOAT16, "Input must be UINT32 or BFLOAT16");
-    TT_FATAL(weights.dtype() == DataType::BFLOAT16, "Weights tensor must have BFLOAT16 dtype");
+    TT_FATAL(
+        weights.dtype() == DataType::BFLOAT16 or weights.dtype() == DataType::FLOAT32,
+        "Weights tensor must have BFLOAT16 or FLOAT32 dtype");
     TT_FATAL(
         a.memory_config().memory_layout() == TensorMemoryLayout::INTERLEAVED,
diff --git ttnn/cpp/ttnn/operations/embedding/device/kernels/compute/tilize_chunked.cpp ttnn/cpp/ttnn/operations/embedding/device/kernels/compute/tilize_chunked.cpp
--- ttnn/cpp/ttnn/operations/embedding/device/kernels/compute/tilize_chunked.cpp
+++ ttnn/cpp/ttnn/operations/embedding/device/kernels/compute/tilize_chunked.cpp
@@ -29,5 +29,6 @@ void kernel_main() {
             compute_kernel_lib::tilize_config::InitUninitMode::InitAndUninit,
             compute_kernel_lib::tilize_config::WaitMode::WaitBlock,
-            compute_kernel_lib::tilize_config::ReconfigureRegisterDatatypeMode::NoReconfigure>(
+            compute_kernel_lib::tilize_config::ReconfigureRegisterDatatypeMode::NoReconfigure,
+            compute_kernel_lib::tilize_config::Fp32Mode::Lossless>(
             per_core_block_cnt * num_chunks);
     } else {
@@ -40,5 +41,6 @@ void kernel_main() {
                     compute_kernel_lib::tilize_config::InitUninitMode::InitAndUninit,
                     compute_kernel_lib::tilize_config::WaitMode::WaitBlock,
-                    compute_kernel_lib::tilize_config::ReconfigureRegisterDatatypeMode::NoReconfigure>(num_chunks - 1);
+                    compute_kernel_lib::tilize_config::ReconfigureRegisterDatatypeMode::NoReconfigure,
+                    compute_kernel_lib::tilize_config::Fp32Mode::Lossless>(num_chunks - 1);
             }
             compute_kernel_lib::tilize<
@@ -48,5 +50,6 @@ void kernel_main() {
                 compute_kernel_lib::tilize_config::InitUninitMode::InitAndUninit,
                 compute_kernel_lib::tilize_config::WaitMode::WaitBlock,
-                compute_kernel_lib::tilize_config::ReconfigureRegisterDatatypeMode::NoReconfigure>(1);
+                compute_kernel_lib::tilize_config::ReconfigureRegisterDatatypeMode::NoReconfigure,
+                compute_kernel_lib::tilize_config::Fp32Mode::Lossless>(1);
         }
     }
diff --git ttnn/cpp/ttnn/operations/embedding/device/embeddings_fused_program_factory.cpp ttnn/cpp/ttnn/operations/embedding/device/embeddings_fused_program_factory.cpp
--- ttnn/cpp/ttnn/operations/embedding/device/embeddings_fused_program_factory.cpp
+++ ttnn/cpp/ttnn/operations/embedding/device/embeddings_fused_program_factory.cpp
@@ -243,4 +243,18 @@ tt::tt_metal::ProgramDescriptor EmbeddingsFusedProgramFactory::create_descriptor
         use_chunked_processing ? "ttnn/cpp/ttnn/operations/embedding/device/kernels/compute/tilize_chunked.cpp"
                                : "ttnn/cpp/ttnn/operations/data_movement/tilize/device/kernels/compute/tilize.cpp";
+    auto make_compute_config = [&] {
+        const bool fp32 = weights.dtype() == DataType::FLOAT32;
+        std::vector<tt::tt_metal::UnpackToDestMode> unpack_to_dest_mode(
+            NUM_CIRCULAR_BUFFERS,
+            tt::tt_metal::UnpackToDestMode::Default);
+        if (fp32) {
+            unpack_to_dest_mode[src0_cb_index] =
+                tt::tt_metal::UnpackToDestMode::UnpackToDestFp32;
+        }
+        return ComputeConfigDescriptor{
+            .fp32_dest_acc_en = fp32,
+            .unpack_to_dest_mode = std::move(unpack_to_dest_mode),
+        };
+    };
 
     if (num_blocks_per_core_group_1 > 0) {
@@ -258,5 +272,5 @@ tt::tt_metal::ProgramDescriptor EmbeddingsFusedProgramFactory::create_descriptor
         compute_desc.core_ranges = core_group_1;
         compute_desc.compile_time_args = std::move(compute_args_1);
-        compute_desc.config = ComputeConfigDescriptor{};
+        compute_desc.config = make_compute_config();
         compute_desc_1 = std::move(compute_desc);
     }
@@ -276,5 +290,5 @@ tt::tt_metal::ProgramDescriptor EmbeddingsFusedProgramFactory::create_descriptor
         compute_desc.core_ranges = core_group_2;
         compute_desc.compile_time_args = std::move(compute_args_2);
-        compute_desc.config = ComputeConfigDescriptor{};
+        compute_desc.config = make_compute_config();
         compute_desc_2 = std::move(compute_desc);
     }
