load("@rules_cc//cc:defs.bzl", "cc_library")

exports_files(["BUILD.overlay.bazel"])

cc_library(
    name = "tt_metal_disabled_optional_runtime",
    srcs = ["stubs/disabled_optional_runtime.cc"],
    copts = ["-std=c++20"],
    deps = [
        "@tt_metal//:ttnn_tensor_upstream",
    ],
    alwayslink = True,
    linkstatic = True,
    visibility = ["//visibility:public"],
)
