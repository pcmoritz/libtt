--- a/lib/Dialect/TTNN/Transforms/TTNNActivationDtypeLowering.cpp
+++ b/lib/Dialect/TTNN/Transforms/TTNNActivationDtypeLowering.cpp
@@ -180 +179,0 @@ static bool tryMLPUpGateMatcher(MatmulOp matmul) {
-  bool sawCCL = false;
@@ -194 +193,0 @@ static bool tryMLPUpGateMatcher(MatmulOp matmul) {
-      sawCCL |= isCCLOp(user);
@@ -202,3 +200,0 @@ static bool tryMLPUpGateMatcher(MatmulOp matmul) {
-  if (!sawCCL) {
-    return false;
-  }
@@ -210,9 +205,11 @@ static bool tryMLPUpGateMatcher(MatmulOp matmul) {
   if (!gate || !gate.getResult().hasOneUse()) {
     return false;
   }
-  auto ff2 = mlir::dyn_cast<MatmulOp>(*gate.getResult().getUsers().begin());
-  if (!ff2 || !matmulReachesResidualAdd(ff2)) {
-    return false;
+  Operation *ff2 = *gate.getResult().getUsers().begin();
+  if (auto ff2Matmul = mlir::dyn_cast<MatmulOp>(ff2)) {
+    if (!matmulReachesResidualAdd(ff2Matmul)) return false;
+  } else if (auto ff2Linear = mlir::dyn_cast<LinearOp>(ff2); !ff2Linear || !ff2Linear.getBias() || !isLowerableFloat(getValueDtype(ff2Linear.getBias())) || !isLowerableFloat(getValueDtype(ff2Linear.getResult()))) {
+    return false;
   }
 
   downcastChain(matmul, flowOps);
