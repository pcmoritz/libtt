diff --git a/lib/Conversion/StableHLOToTTIR/StableHLOToTTIRPatterns.cpp b/lib/Conversion/StableHLOToTTIR/StableHLOToTTIRPatterns.cpp
--- a/lib/Conversion/StableHLOToTTIR/StableHLOToTTIRPatterns.cpp
+++ b/lib/Conversion/StableHLOToTTIR/StableHLOToTTIRPatterns.cpp
@@ -10308,3 +10308,3 @@ public:
     IntegerAttr dimAttr = IntegerAttr::get(rewriter.getIntegerType(32), -1);
-    auto sortedAttr = BoolAttr::get(rewriter.getContext(), false);
+    auto sortedAttr = BoolAttr::get(rewriter.getContext(), true);
 
@@ -10334,6 +10334,6 @@ public:
 
+    // mhlo.topk defaults largest to true and always returns ordered results.
     auto largestAttr = dictAttr.getAs<BoolAttr>("largest");
     if (!largestAttr) {
-      return rewriter.notifyMatchFailure(
-          srcOp, "Missing largest attribute in mhlo.attributes");
+      largestAttr = rewriter.getBoolAttr(true);
     }
diff --git a/lib/Dialect/TTNN/IR/TTNNWorkaroundsPass.cpp b/lib/Dialect/TTNN/IR/TTNNWorkaroundsPass.cpp
--- a/lib/Dialect/TTNN/IR/TTNNWorkaroundsPass.cpp
+++ b/lib/Dialect/TTNN/IR/TTNNWorkaroundsPass.cpp
@@ -1767,3 +1767,3 @@ TTNNOperandsWorkarounds TTNNOperandsWorkaroundsFactory::
 
-// Input is forced to BFloat16 unless it is already BFloat16 or BFP_BFloat8.
+// Float16 and unsupported 32-bit integers use Float32; other unsupported types use BFloat16.
 // Issue page: https://github.com/tenstorrent/tt-metal/issues/40086
@@ -1776,2 +1776,4 @@ TTNNOperandsWorkaroundsFactory::createTopKOpOperandsWorkarounds(
       ttcore::elementTypeToDataType(inputElementType);
+  bool inputUsesFloat32 = inputDataType == ttcore::DataType::Float32 ||
+                          inputElementType.isF16() || inputElementType.isInteger(32);
 
@@ -1781,6 +1783,8 @@ TTNNOperandsWorkaroundsFactory::createTopKOpOperandsWorkarounds(
   if (!inputDataType || (*inputDataType != ttcore::DataType::BFloat16 &&
-                         *inputDataType != ttcore::DataType::BFP_BFloat8)) {
-    inputWorkaround.tensorDataTypeWorkaround = ttcore::DataType::BFloat16;
-    outputValuesWorkaround.tensorDataTypeWorkaround =
-        ttcore::DataType::BFloat16;
+                         *inputDataType != ttcore::DataType::BFP_BFloat8 &&
+                         *inputDataType != ttcore::DataType::Float32)) {
+    auto workaroundDataType = inputUsesFloat32 ? ttcore::DataType::Float32
+                                               : ttcore::DataType::BFloat16;
+    inputWorkaround.tensorDataTypeWorkaround = workaroundDataType;
+    outputValuesWorkaround.tensorDataTypeWorkaround = workaroundDataType;
   } else {
@@ -1799,4 +1803,5 @@ TTNNOperandsWorkaroundsFactory::createTopKOpOperandsWorkarounds(
   int64_t paddedDimSize = paddedShape[dimIndex];
-  bool useUint16Indices =
-      paddedDimSize <= std::numeric_limits<uint16_t>::max(); // 65535
+  bool useUint16Indices = !inputUsesFloat32 &&
+                          paddedDimSize <=
+                              std::numeric_limits<uint16_t>::max(); // 65535
 
