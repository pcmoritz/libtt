diff --git a/include/ttmlir/Dialect/TTCore/IR/TTCoreOpsTypes.h b/include/ttmlir/Dialect/TTCore/IR/TTCoreOpsTypes.h
--- a/include/ttmlir/Dialect/TTCore/IR/TTCoreOpsTypes.h
+++ b/include/ttmlir/Dialect/TTCore/IR/TTCoreOpsTypes.h
@@ -207,6 +207,9 @@ inline std::optional<DataType> elementTypeToDataTypeImpl(Type elementType) {
       return DataType::Bool;
+    // TTNN has no signed 8- or 16-bit tensor dtype.
     case 8:
-      return DataType::UInt8;
+      return (intType.isSigned() || intType.isSignless()) ? DataType::Int32
+                                                          : DataType::UInt8;
     case 16:
-      return DataType::UInt16;
+      return (intType.isSigned() || intType.isSignless()) ? DataType::Int32
+                                                          : DataType::UInt16;
     case 32:
diff --git a/runtime/include/tt/runtime/utils.h b/runtime/include/tt/runtime/utils.h
--- a/runtime/include/tt/runtime/utils.h
+++ b/runtime/include/tt/runtime/utils.h
@@ -38,18 +38,7 @@ inline void handleEquallySignedIntegerBufferCast(const FromTy *oldBuffer,
 
-  constexpr ToTy toTyMax = std::numeric_limits<ToTy>::max();
-  constexpr ToTy toTyMin = std::numeric_limits<ToTy>::min();
-  if constexpr (sizeof(ToTy) >= sizeof(FromTy)) {
-    for (int64_t i = 0; i < numElements; i++) {
-      newBuffer[i] = static_cast<ToTy>(oldBuffer[i]);
-    }
-  } else {
-    for (int64_t i = 0; i < numElements; i++) {
-      if (oldBuffer[i] < static_cast<FromTy>(toTyMin)) {
-        newBuffer[i] = toTyMin;
-      } else if (oldBuffer[i] > static_cast<FromTy>(toTyMax)) {
-        newBuffer[i] = toTyMax;
-      } else {
-        newBuffer[i] = static_cast<ToTy>(oldBuffer[i]);
-      }
-    }
+  // Integer narrowing is modular: retain the low bits rather than saturating.
+  // This is also required when converting a widened runtime alias (for
+  // example, Int32 storage) back to its logical Int8 or Int16 type.
+  for (int64_t i = 0; i < numElements; i++) {
+    newBuffer[i] = static_cast<ToTy>(oldBuffer[i]);
   }
diff --git a/runtime/lib/common/utils.cpp b/runtime/lib/common/utils.cpp
--- a/runtime/lib/common/utils.cpp
+++ b/runtime/lib/common/utils.cpp
@@ -275,5 +275,5 @@ getUnsupportedDataTypeAlias(::tt::target::DataType unsupportedDataType) {
   case ::tt::target::DataType::Int16:
-    return ::tt::target::DataType::UInt16;
+    return ::tt::target::DataType::Int32;
   case ::tt::target::DataType::Int8:
-    return ::tt::target::DataType::UInt8;
+    return ::tt::target::DataType::Int32;
   case ::tt::target::DataType::Bool:
@@ -331,2 +331,12 @@ void handleBufferCast(const void *oldBuffer, void *newBuffer,
         static_cast<int64_t *>(newBuffer), numElements);
+  } else if (oldDataType == tt::target::DataType::Int16 &&
+             newDataType == tt::target::DataType::Int32) {
+    detail::handleIntegerBufferCast<int16_t, int32_t>(
+        static_cast<const int16_t *>(oldBuffer),
+        static_cast<int32_t *>(newBuffer), numElements);
+  } else if (oldDataType == tt::target::DataType::Int32 &&
+             newDataType == tt::target::DataType::Int16) {
+    detail::handleIntegerBufferCast<int32_t, int16_t>(
+        static_cast<const int32_t *>(oldBuffer),
+        static_cast<int16_t *>(newBuffer), numElements);
   } else if (oldDataType == tt::target::DataType::Int16 &&
@@ -341,2 +351,12 @@ void handleBufferCast(const void *oldBuffer, void *newBuffer,
         static_cast<int16_t *>(newBuffer), numElements);
+  } else if (oldDataType == tt::target::DataType::Int8 &&
+             newDataType == tt::target::DataType::Int32) {
+    detail::handleIntegerBufferCast<int8_t, int32_t>(
+        static_cast<const int8_t *>(oldBuffer),
+        static_cast<int32_t *>(newBuffer), numElements);
+  } else if (oldDataType == tt::target::DataType::Int32 &&
+             newDataType == tt::target::DataType::Int8) {
+    detail::handleIntegerBufferCast<int32_t, int8_t>(
+        static_cast<const int32_t *>(oldBuffer),
+        static_cast<int8_t *>(newBuffer), numElements);
   } else if (oldDataType == tt::target::DataType::Int8 &&
