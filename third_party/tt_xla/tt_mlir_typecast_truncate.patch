diff --git a/lib/Conversion/TTIRToTTNN/TTIRToTTNN.cpp b/lib/Conversion/TTIRToTTNN/TTIRToTTNN.cpp
--- a/lib/Conversion/TTIRToTTNN/TTIRToTTNN.cpp
+++ b/lib/Conversion/TTIRToTTNN/TTIRToTTNN.cpp
@@ -2662,2 +2662,15 @@ public:
     auto resultType = op.getType();
+    Value input = adaptor.getInput();
+    auto resultIntegerType =
+        dyn_cast<IntegerType>(resultType.getElementType());
+    if (isa<FloatType>(op.getInput().getType().getElementType()) &&
+        resultIntegerType && resultIntegerType.isUnsigned() &&
+        resultIntegerType.getWidth() == 16) {
+      // TTNN's float-to-UInt16 typecast rounds to the nearest integer, while
+      // StableHLO convert truncates toward zero. Values for which conversion
+      // to an unsigned type is defined are nonnegative, so floor is equivalent
+      // to truncation here. Other destination types already use TTNN paths
+      // with the required conversion semantics.
+      input = rewriter.create<ttnn::FloorOp>(op.getLoc(), input);
+    }
 
@@ -2665,3 +2678,3 @@ public:
         op, this->getTypeConverter()->convertType(resultType),
-        adaptor.getInput());
+        input);
     return success();
