diff --git a/pjrt_implementation/src/api/compile_options_parser.cc b/pjrt_implementation/src/api/compile_options_parser.cc
--- a/pjrt_implementation/src/api/compile_options_parser.cc
+++ b/pjrt_implementation/src/api/compile_options_parser.cc
@@ -69,3 +69,3 @@ bool CompileOptionsParser::parseNestedProtobufField(
 
-  const std::string &data = field.length_delimited();
+  auto data = field.length_delimited();
   google::protobuf::io::CodedInputStream input(
@@ -94,3 +94,3 @@ void CompileOptionsParser::extractDeviceIdsFromComputationDevice(
       // Packed repeated field
-      const std::string &packed_data = field.length_delimited();
+      auto packed_data = field.length_delimited();
       google::protobuf::io::CodedInputStream packed_stream(
@@ -263,3 +263,3 @@ tt_pjrt_status CompileOptionsParser::extractCustomProtobufFields(
 
-    const std::string &bytes = field.length_delimited();
+    auto bytes = field.length_delimited();
     google::protobuf::io::CodedInputStream cis(
@@ -286,3 +286,4 @@ tt_pjrt_status CompileOptionsParser::extractCustomProtobufFields(
               google::protobuf::UnknownField::TYPE_LENGTH_DELIMITED) {
-        key = entry_field.length_delimited();
+        auto key_bytes = entry_field.length_delimited();
+        key.assign(key_bytes.data(), key_bytes.size());
       } else if (entry_field.number() == kMapValueFieldNumber &&
@@ -290,3 +291,3 @@ tt_pjrt_status CompileOptionsParser::extractCustomProtobufFields(
                      google::protobuf::UnknownField::TYPE_LENGTH_DELIMITED) {
-        const std::string &override_bytes = entry_field.length_delimited();
+        auto override_bytes = entry_field.length_delimited();
         google::protobuf::io::CodedInputStream override_stream(
@@ -324,3 +325,4 @@ tt_pjrt_status CompileOptionsParser::extractCustomProtobufFields(
         case 1: {
-          value = value_field.length_delimited();
+          auto value_bytes = value_field.length_delimited();
+          value.assign(value_bytes.data(), value_bytes.size());
           break;
