diff --git a/include/ttmlir/Dialect/TTNN/IR/TTNNOps.td b/include/ttmlir/Dialect/TTNN/IR/TTNNOps.td
--- a/include/ttmlir/Dialect/TTNN/IR/TTNNOps.td
+++ b/include/ttmlir/Dialect/TTNN/IR/TTNNOps.td
@@ -1346,3 +1346,11 @@ def TTNN_ConcatOp : TTNN_Op<"concat"> {
 
     let hasVerifier = 1;
+
+    let extraClassDeclaration = [{
+      wa::TTNNOperandsWorkarounds getOperandsWorkarounds() {
+        ::mlir::Operation::operand_range inputs = getInputs();
+        return wa::TTNNOperandsWorkaroundsFactory::
+            createConcatOpOperandsWorkarounds(inputs, getDim());
+      }
+    }];
 }
diff --git a/include/ttmlir/Dialect/TTNN/IR/TTNNWorkaroundsPass.h b/include/ttmlir/Dialect/TTNN/IR/TTNNWorkaroundsPass.h
--- a/include/ttmlir/Dialect/TTNN/IR/TTNNWorkaroundsPass.h
+++ b/include/ttmlir/Dialect/TTNN/IR/TTNNWorkaroundsPass.h
@@ -286,4 +286,7 @@ public:
   static TTNNOperandsWorkarounds
   createReshapeOpOperandsWorkarounds(RankedTensorType inputType);
+
+  static TTNNOperandsWorkarounds createConcatOpOperandsWorkarounds(
+      mlir::Operation::operand_range inputs, int64_t dim);
 
   static TTNNOperandsWorkarounds createDropoutOpOperandsWorkarounds();
diff --git a/lib/Dialect/TTNN/IR/TTNNWorkaroundsPass.cpp b/lib/Dialect/TTNN/IR/TTNNWorkaroundsPass.cpp
--- a/lib/Dialect/TTNN/IR/TTNNWorkaroundsPass.cpp
+++ b/lib/Dialect/TTNN/IR/TTNNWorkaroundsPass.cpp
@@ -433,4 +433,37 @@ TTNNOperandsWorkaroundsFactory::createReshapeOpOperandsWorkarounds(
       .addOutputOperandWorkaround(typeWorkarounds);
 }
+
+TTNNOperandsWorkarounds
+TTNNOperandsWorkaroundsFactory::createConcatOpOperandsWorkarounds(
+    mlir::Operation::operand_range inputs, int64_t dim) {
+  auto inputType = mlir::cast<RankedTensorType>(inputs.front().getType());
+  int64_t rank = inputType.getRank();
+  dim = dim < 0 ? dim + rank : dim;
+
+  bool hasPartialTile = false;
+  if (dim == rank - 1) {
+    constexpr int64_t tileWidth = ttcore::TileType::getDefaultShape()[1];
+    for (Value input : inputs) {
+      auto type = mlir::cast<RankedTensorType>(input.getType());
+      if (type.getDimSize(dim) % tileWidth != 0) {
+        hasPartialTile = true;
+        break;
+      }
+    }
+  }
+
+  if (!hasPartialTile) {
+    return TTNNOperandsWorkarounds::createEmptyTTNNOperandsWorkarounds(
+        inputs.size(), /*outputSize=*/1);
+  }
+
+  TTNNOperandWorkarounds rowMajor(Layout::RowMajor);
+  auto workarounds = TTNNOperandsWorkarounds::
+      createEmptyTTNNOperandsWorkarounds();
+  for (size_t i = 0; i < inputs.size(); ++i) {
+    workarounds.addInputOperandWorkaround(rowMajor);
+  }
+  return workarounds.addOutputOperandWorkaround(rowMajor);
+}
 
 // Factory method to create a set of workarounds for UpdateCache operation
diff --git a/lib/Dialect/TTNN/Transforms/Workarounds/TTNNWorkaroundsPatterns.cpp b/lib/Dialect/TTNN/Transforms/Workarounds/TTNNWorkaroundsPatterns.cpp
--- a/lib/Dialect/TTNN/Transforms/Workarounds/TTNNWorkaroundsPatterns.cpp
+++ b/lib/Dialect/TTNN/Transforms/Workarounds/TTNNWorkaroundsPatterns.cpp
@@ -548,3 +548,4 @@ const std::set<mlir::StringRef>
         ttnn::EmbeddingOp::getOperationName(),
         ttnn::ScatterOp::getOperationName(),
+        ttnn::ConcatOp::getOperationName(),
         // TopK's operands workaround forces input bf16 + indices ui16/ui32;
