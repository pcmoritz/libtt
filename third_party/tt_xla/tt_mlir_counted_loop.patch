diff --git a/include/ttmlir/Target/TTNN/operations/mlir_native.fbs b/include/ttmlir/Target/TTNN/operations/mlir_native.fbs
--- a/include/ttmlir/Target/TTNN/operations/mlir_native.fbs
+++ b/include/ttmlir/Target/TTNN/operations/mlir_native.fbs
@@ -11,3 +11,20 @@ table FuncCallOp {
   outputs: [tt.target.ttnn.TensorRef];
   semaphore_inputs: [tt.target.ttnn.GlobalSemaphoreRef];
 }
+
+// Repeatedly invoke a private program, updating the state slots named by
+// output_indices after each invocation.
+table CountedLoopOp {
+  program_id: uint32;
+  condition_program_id: int32 = -1;
+  initial: int64;
+  limit: int64;
+  step: int64;
+  initial_index: int32 = -1;
+  limit_index: int32 = -1;
+  step_index: int32 = -1;
+  state_count: uint32;
+  output_indices: [uint32];
+  inputs: [tt.target.ttnn.TensorRef];
+  outputs: [tt.target.ttnn.TensorRef];
+}
diff --git a/include/ttmlir/Target/TTNN/program.fbs b/include/ttmlir/Target/TTNN/program.fbs
--- a/include/ttmlir/Target/TTNN/program.fbs
+++ b/include/ttmlir/Target/TTNN/program.fbs
@@ -55,6 +55,7 @@ union OpType {
   NLPCreateQKVHeadsDecodeOp,
   NLPConcatHeadsOp,
   ConstantOp,
+  CountedLoopOp,
   Conv1dOp,
   Conv2dOp,
   ConvTranspose2dOp,
diff --git a/lib/Conversion/StableHLOToTTIR/StableHLOToTTIRPatterns.cpp b/lib/Conversion/StableHLOToTTIR/StableHLOToTTIRPatterns.cpp
--- a/lib/Conversion/StableHLOToTTIR/StableHLOToTTIRPatterns.cpp
+++ b/lib/Conversion/StableHLOToTTIR/StableHLOToTTIRPatterns.cpp
@@ -11446,8 +11446,14 @@ namespace mlir::tt {
 namespace mlir::tt {
 
+void populateStableHLOCountedLoopToTTIRPatterns(
+    MLIRContext *context, RewritePatternSet &patterns,
+    TypeConverter &typeConverter);
+
 void populateStableHLOToTTIRPatterns(MLIRContext *ctx,
                                      RewritePatternSet &patterns,
                                      TypeConverter &typeConverter) {
+  populateStableHLOCountedLoopToTTIRPatterns(ctx, patterns, typeConverter);
+
   addElementwiseUnaryOpsConversionPatterns(ctx, patterns, typeConverter);
   addElementwiseBinaryOpsConversionPatterns(ctx, patterns, typeConverter);
   addQuantizeOpsConversionPattern(ctx, patterns, typeConverter);
diff --git a/lib/Target/TTNN/TTNNToFlatbuffer.cpp b/lib/Target/TTNN/TTNNToFlatbuffer.cpp
--- a/lib/Target/TTNN/TTNNToFlatbuffer.cpp
+++ b/lib/Target/TTNN/TTNNToFlatbuffer.cpp
@@ -43,3 +43,11 @@ namespace mlir::tt::ttnn {
+bool isCountedLoopCall(func::CallOp call);
+
+::flatbuffers::Offset<::tt::target::ttnn::Operation>
+createCountedLoopOperation(
+    FlatbufferObjectCache &cache, func::CallOp call,
+    const llvm::StringMap<uint32_t> &programIndexMap,
+    const std::string &debugString, const std::string &locInfo);
+
 #define GEN_PASS_DEF_TTNNSERIALIZETOBINARY
 #include "ttmlir/Dialect/TTNN/Transforms/Passes.h.inc"
 
@@ -4883,2 +4891,7 @@ emitTTNNOperation(FlatbufferObjectCache &cache, Operation *op,
+  if (auto funcCallOp = dyn_cast<func::CallOp>(op);
+      funcCallOp && isCountedLoopCall(funcCallOp)) {
+    return createCountedLoopOperation(cache, funcCallOp, programIndexMap,
+                                      debugString, locInfo);
+  }
   if (auto funcCallOp = dyn_cast<func::CallOp>(op);
       funcCallOp && !isCpuHoistedFuncCall(funcCallOp)) {
diff --git a/lib/Dialect/TTNN/Transforms/TTNNLayout.cpp b/lib/Dialect/TTNN/Transforms/TTNNLayout.cpp
--- a/lib/Dialect/TTNN/Transforms/TTNNLayout.cpp
+++ b/lib/Dialect/TTNN/Transforms/TTNNLayout.cpp
@@ -27,2 +27,4 @@ namespace mlir::tt::ttnn {
+void populateCountedLoopLayoutPatterns(RewritePatternSet &patterns);
+
 #define GEN_PASS_DEF_TTNNLAYOUT
 #include "ttmlir/Dialect/TTNN/Transforms/Passes.h.inc"
@@ -827,4 +829,5 @@ public:
       RewritePatternSet patterns(&getContext());
       patterns.add<TTNNLayoutRewriter>(&getContext());
+      populateCountedLoopLayoutPatterns(patterns);
       // Update the return op output layout based on its consumers
       // Logic here should match that of TTNNLayoutFuncInputOutputTypeRewriter
diff --git a/runtime/lib/ttnn/operations/mlir_native/func_call.h b/runtime/lib/ttnn/operations/mlir_native/func_call.h
--- a/runtime/lib/ttnn/operations/mlir_native/func_call.h
+++ b/runtime/lib/ttnn/operations/mlir_native/func_call.h
@@ -11,5 +11,7 @@
 namespace tt::runtime::ttnn::operations::mlir_native {
 
 void run(const ::tt::target::ttnn::FuncCallOp *op, ProgramContext &context);
+void run(const ::tt::target::ttnn::CountedLoopOp *op,
+         ProgramContext &context);
 
 } // namespace tt::runtime::ttnn::operations::mlir_native
diff --git a/runtime/lib/ttnn/program_executor.cpp b/runtime/lib/ttnn/program_executor.cpp
--- a/runtime/lib/ttnn/program_executor.cpp
+++ b/runtime/lib/ttnn/program_executor.cpp
@@ -358,5 +358,9 @@ void ProgramExecutor::runOperation(const ::tt::target::ttnn::Operation *op) {
     return operations::mlir_native::run(op->type_as_FuncCallOp(), getContext());
   }
+  case ::tt::target::ttnn::OpType::CountedLoopOp: {
+    return operations::mlir_native::run(op->type_as_CountedLoopOp(),
+                                        getContext());
+  }
   case ::tt::target::ttnn::OpType::CumSumOp: {
     return operations::reduction::cumsum::run(op->type_as_CumSumOp(),
                                               getContext());
diff --git a/runtime/lib/ttnn/runtime.cpp b/runtime/lib/ttnn/runtime.cpp
--- a/runtime/lib/ttnn/runtime.cpp
+++ b/runtime/lib/ttnn/runtime.cpp
@@ -1554,6 +1554,11 @@ std::vector<const ::tt::target::ttnn::TensorRef *>
         opContext.type_as_FuncCallOp()->outputs());
     break;
   }
+  case ::tt::target::ttnn::OpType::CountedLoopOp: {
+    tensorRefs = utils::convertFbTensorRefsToVector(
+        opContext.type_as_CountedLoopOp()->outputs());
+    break;
+  }
   case ::tt::target::ttnn::OpType::CaptureOrExecuteTraceOp: {
     tensorRefs = utils::convertFbTensorRefsToVector(
         opContext.type_as_CaptureOrExecuteTraceOp()->outputs());
@@ -2190,6 +2195,12 @@ std::vector<const ::tt::target::ttnn::TensorRef *>
     }
     break;
   }
+  case ::tt::target::ttnn::OpType::CountedLoopOp: {
+    for (const auto *input : *opContext.type_as_CountedLoopOp()->inputs()) {
+      tensorRefs.push_back(input);
+    }
+    break;
+  }
   case ::tt::target::ttnn::OpType::WriteTensorOp: {
     tensorRefs = {opContext.type_as_WriteTensorOp()->host_tensor(),
                   opContext.type_as_WriteTensorOp()->device_tensor()};
