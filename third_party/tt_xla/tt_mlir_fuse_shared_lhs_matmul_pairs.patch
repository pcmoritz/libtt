diff --git a/lib/Dialect/TTIR/Transforms/TTIRFusing.cpp b/lib/Dialect/TTIR/Transforms/TTIRFusing.cpp
--- a/lib/Dialect/TTIR/Transforms/TTIRFusing.cpp
+++ b/lib/Dialect/TTIR/Transforms/TTIRFusing.cpp
@@ -1876,3 +1876,3 @@ private:
     bool isValid() const {
-      return ops.size() >= 3 && totalOutputDim <= kMaxFusedOutputDim;
+      return ops.size() >= 2 && totalOutputDim <= kMaxFusedOutputDim;
     }
@@ -2016,3 +2016,4 @@ private:
       auto op = dyn_cast<OpType>(user);
-      if (!op || op.getA() != sharedLHS) {
+      if (!op || op.getA() != sharedLHS ||
+          llvm::is_contained(result.ops, op)) {
         continue;
