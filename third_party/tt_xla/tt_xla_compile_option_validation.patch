diff --git a/pjrt_implementation/inc/api/compile_options.h b/pjrt_implementation/inc/api/compile_options.h
--- a/pjrt_implementation/inc/api/compile_options.h
+++ b/pjrt_implementation/inc/api/compile_options.h
@@ -159,6 +159,10 @@ struct CompileOptions {

   static CompileOptions
   parse(const std::unordered_map<std::string, std::string> &compile_options);
+
+  // Returns an error message when a compile option is unknown or malformed.
+  static std::optional<std::string>
+  validate(const std::unordered_map<std::string, std::string> &compile_options);
 };

 namespace internal {
diff --git a/pjrt_implementation/inc/api/error_instance.h b/pjrt_implementation/inc/api/error_instance.h
--- a/pjrt_implementation/inc/api/error_instance.h
+++ b/pjrt_implementation/inc/api/error_instance.h
@@ -30,6 +30,10 @@ public:
   // Creates new error instance.
   static std::unique_ptr<ErrorInstance> makeError(tt_pjrt_status status);

+  // Creates new error instance with a descriptive message.
+  static std::unique_ptr<ErrorInstance> makeError(tt_pjrt_status status,
+                                                  std::string message);
+
   // Binds PJRT API functions implementation related to PJRT_Error structure.
   static void bindApi(PJRT_Api *api);

@@ -51,6 +55,9 @@ private:
   // Constructs error instance from the given status.
   ErrorInstance(tt_pjrt_status status);

+  // Constructs error instance from the given status and message.
+  ErrorInstance(tt_pjrt_status status, std::string message);
+
   // Error status.
   const tt_pjrt_status m_status;

diff --git a/pjrt_implementation/src/api/client_instance.cc b/pjrt_implementation/src/api/client_instance.cc
--- a/pjrt_implementation/src/api/client_instance.cc
+++ b/pjrt_implementation/src/api/client_instance.cc
@@ -30,6 +30,7 @@

 // tt-xla includes
 #include "api/buffer_instance.h"
+#include "api/compile_options.h"
 #include "api/compile_options_parser.h"
 #include "api/error_instance.h"
 #include "api/event_instance.h"
@@ -862,4 +863,9 @@ PJRT_Error *onClientCompile(PJRT_Client_Compile_Args *args) {
     return *ErrorInstance::makeError(compile_options_status).release();
   }
+  if (auto validation_error = CompileOptions::validate(compile_options_map)) {
+    return *ErrorInstance::makeError(tt_pjrt_status::kInvalidArgument,
+                                     std::move(*validation_error))
+                .release();
+  }
 
   // SPMD partition count (>1 = genuine multi-device SPMD run) tells the module
diff --git a/pjrt_implementation/src/api/compile_options.cc b/pjrt_implementation/src/api/compile_options.cc
--- a/pjrt_implementation/src/api/compile_options.cc
+++ b/pjrt_implementation/src/api/compile_options.cc
@@ -11,4 +11,5 @@
 #include <string>
 #include <unordered_map>
+#include <unordered_set>
 
 namespace tt::pjrt {
@@ -115,6 +116,52 @@ CompileOptions CompileOptions::parse(
   return options;
 }

+std::optional<std::string> CompileOptions::validate(
+    const std::unordered_map<std::string, std::string> &compile_options) {
+  static const std::unordered_set<std::string> supported_options = {
+      optimization_level_key,
+      "experimental_weight_dtype",
+      "experimental-kv-cache-dtype",
+      "math_fidelity",
+      "fp32_dest_acc_en",
+      "experimental_enable_fusing_conv2d_with_multiply_pattern",
+      "backend",
+      "enable_trace",
+      "export_tensors",
+      "enable_const_eval",
+      "enable_const_eval_on_cpu",
+      "enable_const_eval_inputs_to_system_memory",
+      "experimental_enable_permute_matmul_fusion",
+      "codegen_try_recover_structure",
+      "codegen_split_files",
+      "experimental-enable-dram-space-saving-optimization",
+      "enable_create_d2m_subgraphs",
+      "ttnn_perf_metrics_enabled",
+      "ttnn_perf_metrics_output_file",
+      "dry_run",
+      "export_path",
+      "export_model_name",
+      // XLA consumes these options. TT-XLA has no equivalent behavior, but
+      // accepts them for PJRT compatibility.
+      "xla_embed_ir_in_executable",
+      "xla_dump_max_hlo_modules",
+      "xla_gpu_auto_spmd_partitioning_memory_budget_ratio",
+  };
+
+  for (const auto &[name, value] : compile_options) {
+    if (!supported_options.contains(name)) {
+      return "No such compile option: '" + name + "'";
+    }
+
+    if (name == "xla_embed_ir_in_executable" && value != "true" &&
+        value != "false") {
+      return "'" + value + "' is not a valid bool value.";
+    }
+  }
+
+  return std::nullopt;
+}
+
 namespace internal {

 std::optional<bool> parseBoolOption(
diff --git a/pjrt_implementation/src/api/error_instance.cc b/pjrt_implementation/src/api/error_instance.cc
--- a/pjrt_implementation/src/api/error_instance.cc
+++ b/pjrt_implementation/src/api/error_instance.cc
@@ -10,6 +10,9 @@

 #include "api/error_instance.h"

+// c++ standard library includes
+#include <utility>
+
 // tt-xla includes
 #include "utils/logging.h"

@@ -27,6 +30,20 @@ std::unique_ptr<ErrorInstance> ErrorInstance::makeError(tt_pjrt_status status) {
   return std::make_unique<make_unique_enabler>(status);
 }

+std::unique_ptr<ErrorInstance>
+ErrorInstance::makeError(tt_pjrt_status status, std::string message) {
+  if (tt_pjrt_status_is_ok(status)) {
+    return nullptr;
+  }
+
+  struct make_unique_enabler : public ErrorInstance {
+    make_unique_enabler(tt_pjrt_status status, std::string message)
+        : ErrorInstance(status, std::move(message)) {}
+  };
+
+  return std::make_unique<make_unique_enabler>(status, std::move(message));
+}
+
 ErrorInstance::ErrorInstance(tt_pjrt_status status)
     : m_status(status),
       m_message(
@@ -34,6 +51,9 @@ ErrorInstance::ErrorInstance(tt_pjrt_status status)
               ? "OK"
               : ("Error code: " + std::to_string(static_cast<int>(status)))) {}

+ErrorInstance::ErrorInstance(tt_pjrt_status status, std::string message)
+    : m_status(status), m_message(std::move(message)) {}
+
 void ErrorInstance::bindApi(PJRT_Api *api) {
   api->PJRT_Error_Destroy = internal::onErrorDestroy;
   api->PJRT_Error_Message = internal::onErrorMessage;
