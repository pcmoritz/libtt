diff --git a/pjrt_implementation/src/api/buffer_instance.cc b/pjrt_implementation/src/api/buffer_instance.cc
--- a/pjrt_implementation/src/api/buffer_instance.cc
+++ b/pjrt_implementation/src/api/buffer_instance.cc
@@ -508,4 +508,5 @@ void BufferInstance::markAsDataReady() {
 
-// We do not copy to device memory. We are just coping buffer from provided pjrt
-// buffer and pretend that we have copied buffer instance to device.
+// Buffer data is represented by an owned host tensor until execution. Create a
+// distinct owned tensor even when the destination is the same device/memory;
+// JAX uses this API to implement explicit, non-aliasing copies.
 tt_pjrt_status BufferInstance::copyToDeviceMemory(DeviceInstance *dst_device,
@@ -513,9 +514,2 @@ tt_pjrt_status BufferInstance::copyToDeviceMemory(DeviceInstance *dst_device,
                                                   BufferInstance **dst_buffer) {
-  // PJRT API specification requires returning error in case of copying to same
-  // device/memory space.
-  if (getMemory() == dst_memory || getDevice() == dst_device) {
-    LOG_F(ERROR, "Cannot copy buffer to the same memory or device");
-    return tt_pjrt_status::kInvalidArgument;
-  }
-
   std::unique_ptr<BufferInstance> dst_buffer_instance =
