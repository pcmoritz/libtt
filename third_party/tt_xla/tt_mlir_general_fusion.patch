diff --git a/include/ttmlir/Dialect/TTIR/Transforms/Passes.td b/include/ttmlir/Dialect/TTIR/Transforms/Passes.td
--- a/include/ttmlir/Dialect/TTIR/Transforms/Passes.td
+++ b/include/ttmlir/Dialect/TTIR/Transforms/Passes.td
@@ -316,6 +316,10 @@ def TTIRFusing: Pass<"ttir-fusing", "::mlir::ModuleOp">
              "enable-permute-slice-after-matmul-fusion",
              "bool", /*default=*/"true",
              "Push a matmul/linear output slice into the operand producing the sliced dim">,
+      Option<"specializedFusionEnabled",
+             "enable-specialized-fusion",
+             "bool", /*default=*/"true",
+             "Fuse primitive graphs into specialized named operations">,
   ];
 }

diff --git a/lib/Conversion/D2MToTTNN/D2MToTTNN.cpp b/lib/Conversion/D2MToTTNN/D2MToTTNN.cpp
--- a/lib/Conversion/D2MToTTNN/D2MToTTNN.cpp
+++ b/lib/Conversion/D2MToTTNN/D2MToTTNN.cpp
@@ -31,8 +31,10 @@ namespace {

 static SmallVector<int64_t> getTensorShape(MemRefType memrefType) {
   auto deviceLayout = ttcore::getDeviceLayout(memrefType);
-  ArrayRef<int64_t> gridShape = deviceLayout.getGridShape(memrefType);
-  ArrayRef<int64_t> shardShape = deviceLayout.getShardShape(memrefType);
+  auto gridShape =
+      ttcore::collapseGridTo2D(deviceLayout.getGridShape(memrefType));
+  auto shardShape =
+      ttcore::collapseGridTo2D(deviceLayout.getShardShape(memrefType));

   SmallVector<int64_t> tileDims = {1, 1};
   if (auto tileType =
@@ -71,7 +73,8 @@ static ttnn::TTNNLayoutAttr getTTNNLayoutFromDeviceLayout(MLIRContext *ctx,
   }

   auto deviceLayout = ttcore::getDeviceLayout(memrefValue);
-  ArrayRef<int64_t> shardShape = deviceLayout.getShardShape(memrefType);
+  auto shardShape =
+      ttcore::collapseGridTo2D(deviceLayout.getShardShape(memrefType));

   auto shardMemref =
       MemRefType::get(shardShape, memrefType.getElementType(),
@@ -89,7 +92,14 @@ static ttnn::TTNNLayoutAttr getTTNNLayoutFromDeviceLayout(MLIRContext *ctx,
     auto virtMap = d2m::utils::getVirtualGridForwardMapping(memrefValue);

     if (!virtMap) {
-      ttnnGridShape.assign(gridShape.begin(), gridShape.end());
-      memLayoutEnum = ttnn::TensorMemoryLayout::BlockSharded;
+      ttnnGridShape = ttcore::collapseGridTo2D(gridShape);
+      if (bufferType == ttnn::BufferType::DRAM && ttnnGridShape[0] == 1) {
+        memLayoutEnum = ttnn::TensorMemoryLayout::WidthSharded;
+      } else if (bufferType == ttnn::BufferType::DRAM &&
+                 ttnnGridShape[1] == 1) {
+        memLayoutEnum = ttnn::TensorMemoryLayout::HeightSharded;
+      } else {
+        memLayoutEnum = ttnn::TensorMemoryLayout::BlockSharded;
+      }
     } else {
       auto physicalGrid = d2m::utils::getPhysicalGridShape(memrefValue);
@@ -307 +307 @@ static SmallVector<mlir::Attribute> createKernelDescriptors(
-    const ttmetal::MathFidelity mathFidelity,
+    const ttmetal::MathFidelity mathFidelity, bool fp32DestAccum,
@@ -347 +347 @@ static SmallVector<mlir::Attribute> createKernelDescriptors(
-          /*fp32DestAccum*/ false,
+          fp32DestAccum,
@@ -464,10 +477,6 @@ materializeIntermediateTensor(memref::AllocOp op, IRRewriter &rewriter,
       auto castLayoutAttr =
           mlir::cast<ttnn::TTNNLayoutAttr>(castResultType.getEncoding());

-      TT_assertv(castResultType.getNumElements() ==
-                     convertedTensorType.getNumElements(),
-                 "ttnn_metal_layout_cast and converted type must have the same "
-                 "volume");
       TT_assertv(castLayoutAttr.getBufferType() ==
                      convertedLayoutAttr.getBufferType(),
                  "ttnn_metal_layout_cast and converted type must have the same "
@@ -731,4 +740,9 @@ static LogicalResult convertGenericOp(
   const auto arch = ttcore::getOpChipDescAttr(op).getArch().getValue();
+  bool fp32DestAccum = llvm::any_of(op.getInputsAndOutputs(), [](Value value) {
+    return ttcore::getNumberOfBits(ttcore::elementTypeToDataType(
+               ttcore::getOperandInnerElementType(value))) == 32;
+  });
   SmallVector<mlir::Attribute> kernelDescriptors = createKernelDescriptors(
       rewriter, op.getThreads(), coreRangeSet, opSymTable, mathFidelity,
-      semIndexMap, additionalArgMapping, cbOperandIndexToPortMapping, arch);
+      fp32DestAccum, semIndexMap, additionalArgMapping,
+      cbOperandIndexToPortMapping, arch);
@@ -1049,6 +1048,21 @@ static LogicalResult cleanupAndVerify(ModuleOp moduleOp,
       return;
     }
     Value resolved = findIOTensor(op.getOperand(), valueMapping);
+    auto resultType = mlir::cast<RankedTensorType>(op.getResult().getType());
+    if (resolved.getType() != resultType) {
+      auto resolvedType = mlir::cast<RankedTensorType>(resolved.getType());
+      auto resolvedLayout =
+          mlir::cast<ttnn::TTNNLayoutAttr>(resolvedType.getEncoding());
+      auto resultLayout =
+          mlir::cast<ttnn::TTNNLayoutAttr>(resultType.getEncoding());
+      TT_assertv(resolvedLayout.getMemref() == resultLayout.getMemref(),
+                 "exit cast must preserve the physical buffer layout");
+      auto emptyOp = resolved.getDefiningOp<ttnn::EmptyOp>();
+      TT_assertv(emptyOp, "exit cast result must be backed by ttnn.empty");
+      resolved.setType(resultType);
+      emptyOp.setShapeAttr(
+          ttnn::ShapeAttr::get(moduleOp.getContext(), resultType.getShape()));
+    }
     op.getResult().replaceAllUsesWith(resolved);
   });

diff --git a/lib/Conversion/TTIRToD2M/TTIRToD2M.cpp b/lib/Conversion/TTIRToD2M/TTIRToD2M.cpp
--- a/lib/Conversion/TTIRToD2M/TTIRToD2M.cpp
+++ b/lib/Conversion/TTIRToD2M/TTIRToD2M.cpp
@@ -70,7 +70,7 @@ bool isOuterReduction(TTIRReductionOp op) {
 }

 class D2MNamedRewriterCommon {
-protected:
+public:
   using base = D2MNamedRewriterCommon;

   D2MNamedRewriterCommon(ttcore::MemorySpace defaultInputMemSpace,
@@ -217,9 +217,9 @@ protected:
   }

   RankedTensorType
-  getMetalTensorFromTTNNTensor(mlir::ConversionPatternRewriter &rewriter,
-                               Value value) const {
-    auto tensorType = mlir::cast<mlir::RankedTensorType>(value.getType());
+  getMetalTensorFromTTNNTensor(mlir::OpBuilder &rewriter,
+                               RankedTensorType tensorType,
+                               Type elementTypeOverride = {}) const {
     Attribute ttnnLayout = tensorType.getEncoding();

     auto [memSpace, elementType, memLayout] = [&]()
@@ -235,6 +235,15 @@ protected:

     auto optimalGrid = getGridForTTNNTensor(tensorType);

+    constexpr auto defaultTileShape = ttcore::TileType::getDefaultShape();
+    llvm::SmallVector<int64_t> tileShape(defaultTileShape.begin(),
+                                         defaultTileShape.end());
+    if (elementTypeOverride) {
+      elementType = elementTypeOverride;
+      auto tileType = mlir::cast<ttcore::TileType>(elementType);
+      tileShape = {tileType.getHeight(), tileType.getWidth()};
+    }
+
     llvm::SmallVector<int64_t> dimAlignments(tensorType.getShape().size(), 1);
     if (dimAlignments.size() == 1) {
       dimAlignments[0] = ttcore::TileType::getDefaultShape()[1];
@@ -264,14 +273,21 @@ protected:
     }

     llvm::SmallVector<int64_t> unshardedShape =
-        metalLayout.getPhysicalShape(ttcore::TileType::getDefaultShape());
+        metalLayout.getPhysicalShape(tileShape);

     llvm::SmallVector<int64_t> shardedShape = metalLayout.getDeviceShape(
-        optimalGrid, ttcore::TileType::getDefaultShape());
+        optimalGrid, tileShape);

     return mlir::RankedTensorType::get(shardedShape, elementType, metalLayout);
   }

+  RankedTensorType
+  getMetalTensorFromTTNNTensor(mlir::OpBuilder &rewriter,
+                               Value value) const {
+    return getMetalTensorFromTTNNTensor(
+        rewriter, mlir::cast<mlir::RankedTensorType>(value.getType()));
+  }
+
   static void copyVirtualGridAttrs(d2m::EmptyOp dst, d2m::EmptyOp src) {
     if (auto attr = src.getVirtualGridInverseMappingAttr()) {
       dst.setVirtualGridInverseMappingAttr(attr);
@@ -320,34 +336,55 @@ protected:
     bool isTTNN = isTTNNTensor(value.getType());
     if (isTTNN) {
       assert(ttnnMode && "Unexpected TTNN tensor as op operand");
+      auto ttnnTensorType =
+          mlir::cast<mlir::RankedTensorType>(value.getType());
       auto metalTensorType = getMetalTensorFromTTNNTensor(rewriter, value);
+      auto requestedMetalRank = noCollapse ? 2 * ttnnTensorType.getRank()
+                                           : metalTensorType.getRank();
-      auto metalCastOp = rewriter.create<ttir::TTNNMetalLayoutCastOp>(
-          value.getLoc(), metalTensorType, value);
+      ttir::TTNNMetalLayoutCastOp metalCastOp;
+      Value metalValue;
+      if (auto producerCast =
+              value.getDefiningOp<ttir::TTNNMetalLayoutCastOp>()) {
+        auto producerType =
+            mlir::dyn_cast<RankedTensorType>(producerCast.getInput().getType());
+        if (producerType && mlir::isa_and_nonnull<ttcore::MetalLayoutAttr>(
+                                producerType.getEncoding())) {
+          metalValue = producerCast.getInput();
+          metalTensorType = producerType;
+        }
+      }
+      if (!metalValue) {
+        metalCastOp = rewriter.create<ttir::TTNNMetalLayoutCastOp>(
+            value.getLoc(), metalTensorType, value);
+        metalValue = metalCastOp.getResult();
+      }

       // Propagate both VGM maps for height/width sharded TTNN layouts
       // so that downstream passes (GenericOp::build, getMemoryMap, etc.)
       // can discover the virtual-to-physical core mapping.
-      auto inputEncoding =
-          mlir::cast<RankedTensorType>(value.getType()).getEncoding();
-      if (auto ttnnLayout =
-              mlir::dyn_cast_if_present<ttnn::TTNNLayoutAttr>(inputEncoding)) {
-        auto memLayout = ttnnLayout.getMemLayout().getValue();
-        if (memLayout == ttnn::TensorMemoryLayout::HeightSharded ||
-            memLayout == ttnn::TensorMemoryLayout::WidthSharded) {
-          llvm::SmallVector<int64_t> ttnnGridShape(ttnnLayout.getGridShape());
-          llvm::SmallVector<int64_t> virtualGrid;
-          if (memLayout == ttnn::TensorMemoryLayout::HeightSharded) {
-            virtualGrid = {ttnnGridShape[0] * ttnnGridShape[1], 1};
-          } else {
-            virtualGrid = {1, ttnnGridShape[0] * ttnnGridShape[1]};
+      auto inputEncoding = ttnnTensorType.getEncoding();
+      if (metalCastOp) {
+        if (auto ttnnLayout = mlir::dyn_cast_if_present<ttnn::TTNNLayoutAttr>(
+                inputEncoding)) {
+          auto memLayout = ttnnLayout.getMemLayout().getValue();
+          if (memLayout == ttnn::TensorMemoryLayout::HeightSharded ||
+              memLayout == ttnn::TensorMemoryLayout::WidthSharded) {
+            llvm::SmallVector<int64_t> ttnnGridShape(
+                ttnnLayout.getGridShape());
+            llvm::SmallVector<int64_t> virtualGrid;
+            if (memLayout == ttnn::TensorMemoryLayout::HeightSharded) {
+              virtualGrid = {ttnnGridShape[0] * ttnnGridShape[1], 1};
+            } else {
+              virtualGrid = {1, ttnnGridShape[0] * ttnnGridShape[1]};
+            }
+            auto [forwardMap, inverseMap] =
+                ttmlir::d2m::utils::grids::createCoreVirtMaps(
+                    rewriter.getContext(), virtualGrid, ttnnGridShape);
+            metalCastOp.setVirtualGridInverseMappingAttr(
+                AffineMapAttr::get(inverseMap));
+            metalCastOp.setVirtualGridForwardMappingAttr(
+                AffineMapAttr::get(forwardMap));
           }
-          auto [forwardMap, inverseMap] =
-              ttmlir::d2m::utils::grids::createCoreVirtMaps(
-                  rewriter.getContext(), virtualGrid, ttnnGridShape);
-          metalCastOp.setVirtualGridInverseMappingAttr(
-              AffineMapAttr::get(inverseMap));
-          metalCastOp.setVirtualGridForwardMappingAttr(
-              AffineMapAttr::get(forwardMap));
         }
       }

@@ -354,10 +391,15 @@ protected:
-      ttcore::MemorySpace metalTensorMemSpace =
-          mlir::cast<ttcore::MetalLayoutAttr>(metalTensorType.getEncoding())
-              .getMemorySpace();
-      if (metalTensorMemSpace == ttcore::MemorySpace::DeviceL1) {
-        // Reblock L1 operand to unit grid to align with other operands while
-        // preserving original TTNN tensor shape. These views will be removed in
-        // GridSelection by insertTTNNDRAMViews().
+      auto metalTensorLayout =
+          mlir::cast<ttcore::MetalLayoutAttr>(metalTensorType.getEncoding());
+      ttcore::MemorySpace metalTensorMemSpace =
+          metalTensorLayout.getMemorySpace();
+      bool needsF32DRAMView =
+          metalTensorLayout.getMemoryLayout() ==
+              ttcore::TensorMemoryLayout::Interleaved &&
+          mlir::isa<Float32Type>(
+              ttcore::getOperandInnerElementType(metalValue));
+      if (metalTensorMemSpace == ttcore::MemorySpace::DeviceL1 ||
+          needsF32DRAMView) {
+        // Keep f32 DRAM backing interleaved while selecting a worker grid.
         llvm::SmallVector<int64_t> unitGrid(
             metalTensorType.getShape().size() / 2, 1);
         auto [newTensorShape, reblockMap] =
@@ -368,12 +412,21 @@ protected:
             newTensorShape, metalTensorType.getElementType(),
             metalTensorType.getEncoding());
         auto unitReblockingView = rewriter.create<d2m::ViewLayoutOp>(
-            value.getLoc(), unitGridType, metalCastOp->getResult(0), reblockMap,
+            value.getLoc(), unitGridType, metalValue, reblockMap,
             /*reinterpretLayout=*/false);
-        return unitReblockingView.getResult();
+        metalValue = unitReblockingView.getResult();
+      }
+      bool hasRequestedTiling =
+          tiled == mlir::isa<ttcore::TileType>(metalTensorType.getElementType());
+      bool hasRequestedRank = metalTensorType.getRank() == requestedMetalRank;
+      if (metalTensorMemSpace == memSpace && hasRequestedTiling &&
+          hasRequestedRank) {
+        return metalValue;
       }
-      // For DRAM operands, we can return the metal cast result directly.
-      return metalCastOp->getResult(0);
+
+      // Stage TTNN operands in the requested memory space before computation.
+      logicalType = ttnnTensorType;
+      value = metalValue;
     }

     auto tensorType = mlir::cast<mlir::RankedTensorType>(value.getType());
@@ -422,8 +473,9 @@ protected:
     llvm::SmallVector<int64_t> shardedShape =
         layout.getDeviceShape(placeholderTensorGrid, tileShape);

-    auto emptyOp = rewriter.create<d2m::EmptyOp>(value.getLoc(), shardedShape,
-                                                 elementType, layout);
+    auto layoutType = RankedTensorType::get(shardedShape, elementType, layout);
+    auto emptyOp = rewriter.create<d2m::EmptyOp>(value.getLoc(), layoutType,
+                                                 nullptr, nullptr);

     // For ND tensors (logicalShape.size() > 2), set placeholder virtual grid
     // mappings on the EmptyOp.  These will be replaced when GridSelection
@@ -1234,7 +1286,8 @@ private:

     auto [inputs, outputs] =
         toLayoutOperandsAndResults(rewriter, {origInputs, origOutputs},
-                                   /*tiled*/ true, isImplicitBcast);
+                                   /*tiled=*/true,
+                                   /*noCollapse=*/false);
     const std::size_t numInputs = inputs.size();
     const std::size_t numOutputs = outputs.size();
     const std::size_t numOperands = (numInputs + numOutputs);
@@ -1610,7 +1663,7 @@ private:

     auto [inputs, outputs] =
         toLayoutOperandsAndResults(rewriter, {origInputs, origOutputs},
-                                   /*tiled=*/true, /*noCollapse=*/true);
+                                   /*tiled=*/true, /*noCollapse=*/false);
     assert(inputs.size() == 1);
     assert(outputs.size() == 1);

@@ -2140,11 +2193,22 @@ private:
     const bool hasScaler = isFloat;
     SmallVector<mlir::Value> newInputs(origInputs.begin(), origInputs.end());
     if (hasScaler) {
       newInputs.emplace_back(this->createScaler(rewriter, loc, inputTensorType,
                                                 getScaleValue(op)));
     }
-    bool noCollapse = (inputTensorType.getRank() > 2);
-
+    mlir::ArrayAttr reductionDims = getDimArg(op);
+    bool reducesOnlyLastDim =
+        reductionDims.size() == 1 &&
+        normalizeReductionDimIndex(
+            mlir::cast<mlir::IntegerAttr>(reductionDims[0]).getInt(),
+            inputTensorType.getRank()) == inputTensorType.getRank() - 1;
+    bool noCollapse = inputTensorType.getRank() > 2 && !reducesOnlyLastDim;
+    int64_t tileWidth = ttcore::TileType::getDefaultShape()[1];
+    ttcore::OOBVal oobVal =
+        reducesOnlyLastDim && inputTensorType.getShape().back() % tileWidth == 0
+            ? ttcore::OOBVal::Undef
+            : getReductionOOBVal();
+
     auto [inputs, outputs] = toLayoutOperandsAndResults(
         rewriter, {newInputs, origOutputs},
-        /*tiled*/ true, noCollapse, getReductionOOBVal());
+        /*tiled=*/true, noCollapse, oobVal);
@@ -2156,11 +2220,12 @@ private:
     const std::size_t physicalRank =
         ttcore::getDeviceLayout(outputs[0]).getRank() / 2;
     assertPhysicalIteratorRankForReduction(physicalRank);
 
-    SmallVector<mlir::AffineMap> indexingMaps =
-        getAffineMapsArray(rewriter, op, numOperands, physicalRank, hasScaler);
-    SmallVector<mlir::Attribute> iteratorTypes =
-        getIteratorTypesArray(rewriter, op, physicalRank);
+    SmallVector<mlir::AffineMap> indexingMaps = getAffineMapsArray(
+        rewriter, op, numOperands, physicalRank, hasScaler,
+        reducesOnlyLastDim);
+    SmallVector<mlir::Attribute> iteratorTypes = getIteratorTypesArray(
+        rewriter, op, physicalRank, reducesOnlyLastDim);
 
     // Create 'd2m.generic' accepting extended operands.
     auto generic = rewriter.create<d2m::GenericOp>(
@@ -2187,14 +2252,15 @@ private:
         // Create 'linalg.generic' accepting 'blockArgs'.
 
         SmallVector<mlir::AffineMap> linalgIndexingMaps = getAffineMapsArray(
-            rewriter, op, numOperands, physicalRank, hasScaler);
+            rewriter, op, numOperands, physicalRank, hasScaler,
+            reducesOnlyLastDim);
         SmallVector<mlir::utils::IteratorType> linalgIteratorTypes =
             iteratorTypeTTIRToLinalg(rewriter, iteratorTypes);
 
         // Propagate attributes.
 
-        auto reduceDimAttr =
-            d2m::ReduceDimAttr::get(ctx, dimArgAsReduceDim(op, physicalRank));
+        auto reduceDimAttr = d2m::ReduceDimAttr::get(
+            ctx, dimArgAsReduceDim(op, physicalRank, reducesOnlyLastDim));
 
         auto linalgGeneric = rewriter.create<mlir::linalg::GenericOp>(
             loc,
@@ -2257,7 +2323,8 @@ private:
 
   static SmallVector<mlir::AffineMap>
   getAffineMapsArray(mlir::OpBuilder &builder, ConcreteOp op, std::size_t arity,
-                     std::size_t rank, bool hasScaler) {
+                     std::size_t rank, bool hasScaler,
+                     bool collapsedLastDimReduction) {
     mlir::ArrayAttr dimArg = getDimArg(op);
 
     mlir::AffineExpr zero =
@@ -2264,7 +2331,8 @@ private:
         mlir::getAffineConstantExpr(0, builder.getContext());
 
     mlir::MutableAffineMap accumulator(builder.getMultiDimIdentityMap(rank));
-    forAllDims(rank, dimArg, [&](std::size_t index, bool dropped) {
+    forAllDims(rank, dimArg, collapsedLastDimReduction,
+               [&](std::size_t index, bool dropped) {
       if (dropped) {
         accumulator.setResult(index, zero);
       }
@@ -2288,7 +2356,7 @@ private:
 
   static SmallVector<mlir::Attribute>
   getIteratorTypesArray(mlir::OpBuilder &builder, ConcreteOp op,
-                        std::size_t rank) {
+                        std::size_t rank, bool collapsedLastDimReduction) {
     mlir::ArrayAttr dimArg = getDimArg(op);
 
     auto parallel = ttcore::IteratorTypeAttr::get(
@@ -2297,7 +2365,8 @@ private:
         builder.getContext(), ttcore::IteratorType::Reduction);
 
     SmallVector<mlir::Attribute> iterators(rank, parallel);
-    forAllDims(rank, dimArg, [&](std::size_t index, bool dropped) {
+    forAllDims(rank, dimArg, collapsedLastDimReduction,
+               [&](std::size_t index, bool dropped) {
       if (dropped) {
         iterators[index] = reduction;
       }
@@ -2326,9 +2395,10 @@ private:
   /// Map `dim_arg` to a `ReduceDim` over the last two (tile C/R) dimensions.
   /// Outer-only reductions should have matched
   /// `D2MNamedAccumReductionRewriter`.
-  static d2m::ReduceDim dimArgAsReduceDim(ConcreteOp op, std::size_t rank) {
+  static d2m::ReduceDim dimArgAsReduceDim(ConcreteOp op, std::size_t rank,
+                                          bool collapsedLastDimReduction) {
     SmallVector<bool> dims(rank, false);
-    forAllDims(rank, getDimArg(op),
+    forAllDims(rank, getDimArg(op), collapsedLastDimReduction,
                [&](std::size_t index, bool dropped) { dims[index] = dropped; });
 
     bool reduceSecondToLast = dims[rank - 2]; // "C" in tile terminology
@@ -2354,13 +2424,18 @@ private:
 
   template <typename F>
-  static void forAllDims(std::size_t rank, mlir::ArrayAttr dimArg, F &&fn) {
+  static void forAllDims(std::size_t rank, mlir::ArrayAttr dimArg,
+                         bool collapsedLastDimReduction, F &&fn) {
     SmallVector<bool> dims(rank, false);
-    for (auto reduceDim : dimArg) {
-      int64_t dim = mlir::cast<IntegerAttr>(reduceDim).getInt();
-      dims[normalizeReductionDimIndex(dim, rank)] = true;
+    if (collapsedLastDimReduction) {
+      dims.back() = true;
+    } else {
+      for (auto reduceDim : dimArg) {
+        int64_t dim = mlir::cast<IntegerAttr>(reduceDim).getInt();
+        dims[normalizeReductionDimIndex(dim, rank)] = true;
+      }
     }
     for (std::size_t d = 0; d < rank; ++d) {
       std::forward<F>(fn)(d, dims[d]);
     }
   }
 };
@@ -2936,11 +3000,11 @@ public:
   D2MConcatRewriter(const TypeConverter &typeConverter, mlir::MLIRContext *ctx,
                     ttcore::MemorySpace defaultInputMemSpace,
                     ttcore::MemorySpace defaultOutputMemSpace,
-                    bool /*ttnnMode*/, bool /*collapseTensors*/,
+                    bool ttnnMode, bool /*collapseTensors*/,
                     bool enableMulticastInference)
       : OpConversionPattern<ttir::ConcatOp>(typeConverter, ctx),
         D2MNamedRewriterCommon(defaultInputMemSpace, defaultOutputMemSpace,
-                               /*ttnnMode=*/false, /*collapseTensors=*/false,
+                               ttnnMode, /*collapseTensors=*/false,
                                enableMulticastInference) {}

   LogicalResult
@@ -3042,9 +3095,12 @@ public:

     auto origOutputs = createDpsOutputs(loc, rewriter, {effectiveOutputType});

+    const bool useCollapsedTiledLayout =
+        ttnnMode && !concatRowMajor && dim == rank - 1;
     auto [inputs, outputs] = toLayoutOperandsAndResults(
         rewriter, {effectiveInputs, origOutputs},
-        /*tiled=*/!concatRowMajor, /*noCollapse=*/true);
+        /*tiled=*/!concatRowMajor,
+        /*noCollapse=*/!useCollapsedTiledLayout);

     auto compositeView = rewriter.create<d2m::CompositeViewOp>(
         loc, outputs[0].getType(), inputs, dim, logicalSizesAttr);
@@ -4695,12 +4751,13 @@ public:
                                   mlir::MLIRContext *ctx,
                                   ttcore::MemorySpace defaultInputMemSpace,
                                   ttcore::MemorySpace defaultOutputMemSpace,
-                                  bool ttnnMode, bool /*collapseTensors*/,
+                                  bool ttnnMode, bool collapseTensors,
                                   bool enableMulticastInference)
       : OpConversionPattern<TensorManipulationOp>(typeConverter, ctx),
         D2MNamedRewriterCommon(defaultInputMemSpace, defaultOutputMemSpace,
                                ttnnMode, /*collapse*/ false,
-                               enableMulticastInference) {}
+                               enableMulticastInference),
+        preferCollapsedViews(collapseTensors) {}

   LogicalResult
   matchAndRewrite(TensorManipulationOp op,
@@ -4715,6 +4772,81 @@ public:
     }

     TensorManipulationInfo info = LogicalInfoFn(op);
+    if (ttnnMode && preferCollapsedViews &&
+        (info.canBeTilized ||
+         std::is_same_v<TensorManipulationOp, ttir::ReshapeOp>)) {
+      if constexpr (std::is_same_v<TensorManipulationOp,
+                                   ttir::SliceStaticOp>) {
+        auto begins = extractFromIntegerArrayAttr<int32_t>(op.getBegins());
+        auto ends = extractFromIntegerArrayAttr<int32_t>(op.getEnds());
+        auto steps = extractFromIntegerArrayAttr<int32_t>(op.getStep());
+        auto inputType =
+            mlir::cast<RankedTensorType>(op.getInput().getType());
+        const size_t rank = inputType.getRank();
+        bool onlySlicesWidth = rank >= 2 && steps.back() == 1;
+        for (size_t dim = 0; dim + 1 < rank; ++dim) {
+          onlySlicesWidth &= begins[dim] == 0 && steps[dim] == 1 &&
+                             ends[dim] == inputType.getShape()[dim];
+        }
+        if (onlySlicesWidth) {
+          Value input = createOptimalLayoutOp(
+              adaptor.getInput(), memorySpaces[0], /*tiled=*/true,
+              /*noCollapse=*/false, rewriter);
+          auto inputMetalType =
+              mlir::cast<RankedTensorType>(input.getType());
+          auto resultType =
+              mlir::cast<RankedTensorType>(op.getResult().getType());
+          auto resultMetalType = getMetalTensorFromTTNNTensor(
+              rewriter, resultType, inputMetalType.getElementType());
+          const unsigned physicalRank = resultMetalType.getRank() / 2;
+          TT_assert(inputMetalType.getRank() == resultMetalType.getRank());
+
+          SmallVector<AffineExpr> exprs;
+          exprs.reserve(resultMetalType.getRank());
+          for (unsigned dim = 0; dim < resultMetalType.getRank(); ++dim) {
+            exprs.push_back(rewriter.getAffineDimExpr(dim));
+          }
+          constexpr auto tileShape = ttcore::TileType::getDefaultShape();
+          exprs[2 * physicalRank - 1] =
+              exprs[2 * physicalRank - 1] +
+              begins.back() / tileShape[1];
+          AffineMap remapping =
+              AffineMap::get(resultMetalType.getRank(), 0, exprs,
+                             rewriter.getContext());
+          auto view = rewriter.create<d2m::ViewLayoutOp>(
+              op.getLoc(), resultMetalType, input, remapping,
+              /*reinterpretLayout=*/false);
+          rewriter.replaceOp(op, unLayoutResult(rewriter, view.getResult(),
+                                                resultType));
+          return success();
+        }
+      }
+
+      if constexpr (std::is_same_v<TensorManipulationOp, ttir::ReshapeOp>) {
+        auto resultType =
+            mlir::cast<RankedTensorType>(op.getResult().getType());
+        Value input = createOptimalLayoutOp(
+              adaptor.getInput(), memorySpaces[0], /*tiled=*/true,
+              /*noCollapse=*/false, rewriter);
+          auto inputMetalType =
+              mlir::cast<RankedTensorType>(input.getType());
+          auto resultMetalType = getMetalTensorFromTTNNTensor(
+              rewriter, resultType, inputMetalType.getElementType());
+          if (ttmlir::utils::volume<int64_t>(inputMetalType.getShape()) ==
+              ttmlir::utils::volume<int64_t>(resultMetalType.getShape())) {
+            AffineMap remapping = ttmlir::utils::calculateReblockMap(
+                inputMetalType.getShape(), resultMetalType.getShape(),
+                rewriter.getContext());
+            auto view = rewriter.create<d2m::ViewLayoutOp>(
+                op.getLoc(), resultMetalType, input, remapping,
+                /*reinterpretLayout=*/false);
+            rewriter.replaceOp(op, unLayoutResult(rewriter, view.getResult(),
+                                                  resultType));
+            return success();
+          }
+      }
+    }
+
     AffineMap deviceMap =
         projectLogicalMapToUnitDeviceSpace(rewriter, info.map);

@@ -4797,6 +4929,9 @@ public:
     return AffineMap::get(outputDeviceRank, 0, deviceExprs,
                           builder.getContext());
   }
+
+private:
+  bool preferCollapsedViews;
 };
 } // namespace

@@ -4833,22 +4968,31 @@ static TensorManipulationInfo sliceLogicalInfo(ttir::SliceStaticOp op) {
   assert(begins.size() ==
          static_cast<size_t>(op.getResult().getType().getRank()));

-  SmallVector<AffineExpr> exprs;
-  for (size_t d = 0; d < begins.size(); d++) {
-    exprs.push_back(getAffineDimExpr(d, ctx) * step[d] + begins[d]);
-  }
-  AffineMap map = AffineMap::get(exprs.size(), 0, exprs, ctx);
   bool canBeTilized = false;
   size_t rank = begins.size();
   if (rank >= 2) {
     ArrayRef<int64_t> inputShape = op.getInput().getType().getShape();
+    constexpr auto tileShape = ttcore::TileType::getDefaultShape();
+    auto isTileAlignedOrFull = [&](size_t dim, int64_t tile) {
+      return (begins[dim] == 0 && ends[dim] == inputShape[dim]) ||
+             (begins[dim] % tile == 0 && ends[dim] % tile == 0);
+    };
     canBeTilized =
-        begins[rank - 2] == 0 &&
-        ends[rank - 2] == static_cast<int32_t>(inputShape[rank - 2]) &&
-        step[rank - 2] == 1 && begins[rank - 1] == 0 &&
-        ends[rank - 1] == static_cast<int32_t>(inputShape[rank - 1]) &&
-        step[rank - 1] == 1;
+        step[rank - 2] == 1 && step[rank - 1] == 1 &&
+        isTileAlignedOrFull(rank - 2, tileShape[0]) &&
+        isTileAlignedOrFull(rank - 1, tileShape[1]);
+  }
+
+  SmallVector<AffineExpr> exprs;
+  for (size_t d = 0; d < begins.size(); d++) {
+    int64_t offset = begins[d];
+    if (canBeTilized && d >= rank - 2) {
+      constexpr auto tileShape = ttcore::TileType::getDefaultShape();
+      offset /= tileShape[d - (rank - 2)];
+    }
+    exprs.push_back(getAffineDimExpr(d, ctx) * step[d] + offset);
   }
+  AffineMap map = AffineMap::get(exprs.size(), 0, exprs, ctx);
   return {map, canBeTilized};
 }

@@ -6029,6 +6173,70 @@ public:

     if (failed(applyPartialConversion(module, target, std::move(patterns)))) {
       signalPassFailure();
+      return;
+    }
+
+    // Internal TTNN casts are representational bridges that keep producer and
+    // consumer Metal values connected for fusion. Function results, however,
+    // must own a concrete buffer in the surrounding TTNN layout.
+    D2MNamedRewriterCommon boundaryLayout(
+        ttcore::MemorySpace::DeviceDRAM,
+        ttcore::MemorySpace::DeviceDRAM,
+        /*ttnnMode=*/true,
+        /*collapseTensors=*/true,
+        /*enableMulticastInference=*/false);
+    module.walk([&](func::ReturnOp returnOp) {
+      for (OpOperand &operand : returnOp->getOpOperands()) {
+        auto castOp =
+            operand.get().getDefiningOp<ttir::TTNNMetalLayoutCastOp>();
+        if (!castOp) {
+          continue;
+        }
+        auto sourceType =
+            mlir::dyn_cast<RankedTensorType>(castOp.getInput().getType());
+        auto resultType =
+            mlir::dyn_cast<RankedTensorType>(castOp.getResult().getType());
+        if (!sourceType || !resultType ||
+            !mlir::isa_and_nonnull<ttcore::MetalLayoutAttr>(
+                sourceType.getEncoding())) {
+          continue;
+        }
+        // F32 DRAM values retain their virtual-grid mapping across this cast.
+        if (mlir::isa<Float32Type>(
+                ttcore::getOperandInnerElementType(castOp.getInput()))) {
+          continue;
+        }
+        Type elementType = sourceType.getElementType();
+        if (!mlir::isa<ttcore::TileType>(elementType)) {
+          elementType = ttcore::TileType::get(
+              elementType, ttcore::TileType::getDefaultShape());
+        }
+        OpBuilder builder(castOp);
+        RankedTensorType boundaryType =
+            boundaryLayout.getMetalTensorFromTTNNTensor(builder, resultType,
+                                                         elementType);
+        auto output = builder.create<d2m::EmptyOp>(
+            castOp.getLoc(), boundaryType,
+            /*virtualGridInverseMapping=*/nullptr,
+            /*virtualGridForwardMapping=*/nullptr);
+        auto toLayout = builder.create<d2m::ToLayoutOp>(
+            castOp.getLoc(), castOp.getInput(), output);
+        castOp.getInputMutable().assign(toLayout.getResult(0));
+      }
+    });
+
+    // Reusing the Metal input of a TTNN-to-Metal-to-TTNN round trip can leave
+    // the outer, representational cast unused.  Drop only those dead casts;
+    // broader canonicalization may change live DRAM layouts before grid
+    // selection.
+    llvm::SmallVector<ttir::TTNNMetalLayoutCastOp> deadCasts;
+    module.walk([&](ttir::TTNNMetalLayoutCastOp castOp) {
+      if (castOp->use_empty()) {
+        deadCasts.push_back(castOp);
+      }
+    });
+    for (ttir::TTNNMetalLayoutCastOp castOp : deadCasts) {
+      castOp.erase();
     }
   }
 };
diff --git a/lib/Conversion/TTNNToTTIR/TTNNToTTIRPatterns.cpp b/lib/Conversion/TTNNToTTIR/TTNNToTTIRPatterns.cpp
--- a/lib/Conversion/TTNNToTTIR/TTNNToTTIRPatterns.cpp
+++ b/lib/Conversion/TTNNToTTIR/TTNNToTTIRPatterns.cpp
@@ -17,6 +17,8 @@
 #include "mlir/Transforms/DialectConversion.h"
 #include "llvm/ADT/STLExtras.h"

+#include <type_traits>
+
 namespace {

 template <typename SrcOp, typename DestOp>
@@ -251,6 +253,35 @@ public:
   }
 };

+template <typename SrcOp, typename DestOp>
+class TTNNToTTIRStructuralConversionPattern
+    : public mlir::OpConversionPattern<SrcOp> {
+public:
+  using mlir::OpConversionPattern<SrcOp>::OpConversionPattern;
+
+  mlir::LogicalResult
+  matchAndRewrite(SrcOp srcOp, typename SrcOp::Adaptor adaptor,
+                  mlir::ConversionPatternRewriter &rewriter) const override {
+    auto outputType = mlir::cast<mlir::RankedTensorType>(
+        this->getTypeConverter()->convertType(srcOp.getResult().getType()));
+    if constexpr (std::is_same_v<SrcOp, mlir::tt::ttnn::ConcatOp>) {
+      rewriter.replaceOpWithNewOp<DestOp>(
+          srcOp, outputType, adaptor.getInputs(), srcOp.getDimAttr());
+    } else if constexpr (std::is_same_v<SrcOp,
+                                        mlir::tt::ttnn::ReshapeOp>) {
+      rewriter.replaceOpWithNewOp<DestOp>(
+          srcOp, outputType, adaptor.getInput(), srcOp.getShapeAttr());
+    } else {
+      static_assert(
+          std::is_same_v<SrcOp, mlir::tt::ttnn::SliceStaticOp>);
+      rewriter.replaceOpWithNewOp<DestOp>(
+          srcOp, outputType, adaptor.getInput(), srcOp.getBeginsAttr(),
+          srcOp.getEndsAttr(), srcOp.getStepAttr());
+    }
+    return mlir::success();
+  }
+};
+
 static void
 addElementwiseUnaryOpsConversionPatterns(mlir::MLIRContext *ctx,
                                          mlir::RewritePatternSet &patterns,
@@ -407,6 +438,14 @@ void populateTTNNToTTIRPatterns(MLIRContext *ctx, RewritePatternSet &patterns,
   patterns.add<TTNNMatmulToTTIRMatmulConversionPattern>(typeConverter, ctx);
   patterns.add<TTNNFullOpToTTIRFullOpConversionPattern>(typeConverter, ctx);
   patterns.add<TTNNToLayoutToTTIRToLayoutConversionPattern>(typeConverter, ctx);
+  patterns.add<
+      TTNNToTTIRStructuralConversionPattern<mlir::tt::ttnn::ConcatOp,
+                                            mlir::tt::ttir::ConcatOp>,
+      TTNNToTTIRStructuralConversionPattern<mlir::tt::ttnn::ReshapeOp,
+                                            mlir::tt::ttir::ReshapeOp>,
+      TTNNToTTIRStructuralConversionPattern<mlir::tt::ttnn::SliceStaticOp,
+                                            mlir::tt::ttir::SliceStaticOp>>(
+      typeConverter, ctx);
 }

 } // namespace mlir::tt
diff --git a/include/ttmlir/Dialect/D2M/Transforms/Passes.td b/include/ttmlir/Dialect/D2M/Transforms/Passes.td
--- a/include/ttmlir/Dialect/D2M/Transforms/Passes.td
+++ b/include/ttmlir/Dialect/D2M/Transforms/Passes.td
@@ -69,6 +69,8 @@ def D2MGenericFusion : Pass<"d2m-generic-fusion", "::mlir::ModuleOp"> {
   list<Option> options = [
     Option<"enableEltwiseReductionFusion", "enable-eltwise-reduction-fusion", "bool", "false",
            "Enable fusion of an elementwise d2m.generic producer into a reduction d2m.generic consumer (single reduction dim).">,
+    Option<"enableElementwiseFusion", "enable-elementwise-fusion", "bool", "true",
+           "Enable fusion between elementwise d2m.generic operations.">,
   ];

   let dependentDialects = [
diff --git a/lib/Dialect/D2M/Analysis/GridAnalysis.cpp b/lib/Dialect/D2M/Analysis/GridAnalysis.cpp
--- a/lib/Dialect/D2M/Analysis/GridAnalysis.cpp
+++ b/lib/Dialect/D2M/Analysis/GridAnalysis.cpp
@@ -19,6 +19,12 @@ namespace mlir::tt::d2m {

 bool GridAnalysis::isTTNNOperand(Value operand) {
   while (auto view = operand.getDefiningOp<d2m::ViewLayoutOp>()) {
+    auto type = mlir::cast<RankedTensorType>(view.getType());
+    auto layout = mlir::cast<ttcore::MetalLayoutAttr>(type.getEncoding());
+    if (layout.getMemorySpace() == ttcore::MemorySpace::DeviceDRAM &&
+        mlir::isa<Float32Type>(
+            ttcore::getOperandInnerElementType(view.getResult())))
+      return false;
     operand = view.getInput();
   }
   return operand.getDefiningOp<ttir::TTNNMetalLayoutCastOp>() != nullptr;
diff --git a/lib/Dialect/D2M/IR/D2MOps.cpp b/lib/Dialect/D2M/IR/D2MOps.cpp
--- a/lib/Dialect/D2M/IR/D2MOps.cpp
+++ b/lib/Dialect/D2M/IR/D2MOps.cpp
@@ -1344,6 +1344,21 @@ LogicalResult d2m::CompositeViewOp::bufferize(
   const bool isTiled = mlir::isa<ttcore::TileType>(
       mlir::cast<RankedTensorType>(getResult().getType()).getElementType());
   const int dim = getDim();
+  int bufferDim = dim;
+  if (isTiled) {
+    auto resultTensorType =
+        mlir::cast<RankedTensorType>(getResult().getType());
+    auto resultLayout = mlir::cast<ttcore::MetalLayoutAttr>(
+        resultTensorType.getEncoding());
+    auto intervals = resultLayout.getNormalizedIntervals();
+    for (size_t interval = 0; interval < intervals.size() / 2; ++interval) {
+      if (dim >= intervals[2 * interval] &&
+          dim < intervals[2 * interval + 1]) {
+        bufferDim = static_cast<int>(interval);
+        break;
+      }
+    }
+  }
   SmallVector<int64_t> logicalSizes;
   SmallVector<Value> bufferizedInputs;
   for (Value input : getInputs()) {
@@ -1369,7 +1384,7 @@ LogicalResult d2m::CompositeViewOp::bufferize(
   auto outMemrefType = mlir::cast<MemRefType>(*outMemrefTypeOr);
   // The logicalSizes attribute is only useful for row-major concat.
   auto newOp = rewriter.create<d2m::CompositeViewOp>(
-      getLoc(), outMemrefType, bufferizedInputs, dim,
+      getLoc(), outMemrefType, bufferizedInputs, bufferDim,
       isTiled ? nullptr : rewriter.getDenseI64ArrayAttr(logicalSizes));
   mlir::bufferization::replaceOpWithBufferizedValues(rewriter, *this,
                                                      newOp.getResult());
@@ -2658,11 +2673,17 @@ static void repairParallelizedRegionTypes(d2m::GenericOp genericOp,
     } else if (auto tensorEmptyOp =
                    mlir::dyn_cast<tensor::EmptyOp>(&clonedOp)) {
       Value associatedOperand = d2m::GenericOp::findAssocOperand(tensorEmptyOp);
+      Type newEmptyType = tensorEmptyOp.getType();
-      if (associatedOperand) {
-        Type newEmptyType = d2m::utils::cloneWithShardShape(
-            associatedOperand, tensorEmptyOp.getType());
-        if (newEmptyType != tensorEmptyOp.getType()) {
-          tensorEmptyOp.getResult().setType(
-              cast<RankedTensorType>(newEmptyType));
-        }
+      if (auto blockingMapAttr =
+              tensorEmptyOp->getAttrOfType<mlir::AffineMapAttr>(
+                  "d2m.blocking_map")) {
+        newEmptyType = getTypeFromBlockingMap(
+            genericOp, blockingMapAttr.getValue(), tensorEmptyOp.getType());
+      } else if (associatedOperand) {
+        newEmptyType = d2m::utils::cloneWithShardShape(
+            associatedOperand, tensorEmptyOp.getType());
+      }
+      if (newEmptyType != tensorEmptyOp.getType()) {
+        tensorEmptyOp.getResult().setType(
+            cast<RankedTensorType>(newEmptyType));
       }
     } else if (auto remoteLoadOp =

diff --git a/lib/Dialect/D2M/Pipelines/D2MPipelines.cpp b/lib/Dialect/D2M/Pipelines/D2MPipelines.cpp
--- a/lib/Dialect/D2M/Pipelines/D2MPipelines.cpp
+++ b/lib/Dialect/D2M/Pipelines/D2MPipelines.cpp
@@ -88,7 +88,9 @@ void createD2MFrontendPipeline(OpPassManager &pm,
   pm.addPass(ttir::createTTIRDecomposeComposites());
   pm.addPass(tt::createTTIRToTTIRDecompositionPass());
   pm.addPass(ttir::createTTIRExplicateTMs());
-  pm.addPass(ttir::createTTIREraseInverseOps());
+  if (!options.ttnnMode) {
+    pm.addPass(ttir::createTTIREraseInverseOps());
+  }
   pm.addPass(ttir::createTTIRMoveReshapeToConstant());
   pm.addPass(ttir::createTTIRFoldConstantReshapeBroadcast());
   pm.addPass(ttir::createTTIRReductionForceKeepDim());
@@ -111,6 +113,14 @@ void createD2MFrontendPipeline(OpPassManager &pm,
   }
   pm.addPass(tt::createTTIRToD2MPass(toD2MOptions));
   pm.addPass(d2m::createD2MScalarizeConstTensors());
+  if (options.enableElementwiseFusion) {
+    d2m::D2MGenericFusionOptions fusionOptions;
+    fusionOptions.enableElementwiseFusion = false;
+    fusionOptions.enableEltwiseReductionFusion = true;
+    pm.addPass(d2m::createD2MGenericFusion(fusionOptions));
+    pm.addPass(mlir::createCanonicalizerPass());
+  }
+
   d2m::D2MGridSelectionOptions gridOptOptions;
   {
     gridOptOptions.overrideDeviceShape =
diff --git a/lib/Dialect/D2M/Transforms/ExpandDMAReadCompositeView.cpp b/lib/Dialect/D2M/Transforms/ExpandDMAReadCompositeView.cpp
--- a/lib/Dialect/D2M/Transforms/ExpandDMAReadCompositeView.cpp
+++ b/lib/Dialect/D2M/Transforms/ExpandDMAReadCompositeView.cpp
@@ -6,6 +6,7 @@
 #include "ttmlir/Dialect/D2M/IR/D2MOps.h"
 #include "ttmlir/Dialect/D2M/Transforms/Passes.h"
 #include "ttmlir/Dialect/D2M/Utils/Utils.h"
+#include "ttmlir/AffineMapUtils.h"

 #include "mlir/Conversion/AffineToStandard/AffineToStandard.h"
 #include "mlir/Dialect/Arith/IR/Arith.h"
@@ -17,21 +18,36 @@ namespace mlir::tt::d2m {
 #define GEN_PASS_DEF_D2MEXPANDDMAREADCOMPOSITEVIEW
 #include "ttmlir/Dialect/D2M/Transforms/Passes.h.inc"

-static LogicalResult expandCompositeDMAReadTiled(IRRewriter &rewriter,
-                                                 DMAReadOp dmaRead,
-                                                 ValueRange expandedInputs,
-                                                 const int32_t concatDim) {
+static AffineMap getCompositeSourceRemapping(Value source,
+                                             CompositeViewOp compositeView) {
+  if (source == compositeView.getResult()) {
+    auto type = mlir::cast<MemRefType>(source.getType());
+    return AffineMap::getMultiDimIdentityMap(type.getRank(),
+                                             type.getContext());
+  }
+  auto view = source.getDefiningOp<ViewLayoutOp>();
+  TT_assert(view);
+  return getCompositeSourceRemapping(view.getInput(), compositeView)
+      .compose(view.getRemapping());
+}
+
+static LogicalResult expandCompositeDMAReadTiled(
+    IRRewriter &rewriter, DMAReadOp dmaRead, ValueRange expandedInputs,
+    CompositeViewOp compositeView, AffineMap sourceRemapping,
+    const int32_t concatDim) {
   TT_assert(dmaRead.isShardLevel());
   ttcore::DeviceAttr device = ttcore::lookupDevice(dmaRead);

   int64_t gridRank = static_cast<int64_t>(dmaRead.getSrcIndices().size());
-  TT_assert((concatDim >= 0 && concatDim < gridRank));
-
-  auto compositeType = mlir::cast<MemRefType>(dmaRead.getSrc().getType());
-  TT_assert(compositeType.getRank() == 2 * gridRank);
+  auto compositeType =
+      mlir::cast<MemRefType>(compositeView.getResult().getType());
+  const int64_t compositeGridRank = compositeType.getRank() / 2;
+  TT_assert((concatDim >= 0 && concatDim < compositeGridRank));
+  TT_assert(sourceRemapping.getNumDims() >= 2 * gridRank);
+  TT_assert(sourceRemapping.getNumResults() == 2 * compositeGridRank);

   const int64_t concatGridDim = concatDim;
-  const int64_t concatShardDim = concatDim + gridRank;
+  const int64_t concatShardDim = concatDim + compositeGridRank;

   // Number of tiles in the composite view's concat dim, potentially aligned-up.
   auto compositeDeviceShape = compositeType.getShape();
@@ -55,8 +71,9 @@ static LogicalResult expandCompositeDMAReadTiled(IRRewriter &rewriter,
     inputExtents.push_back(inputExtent);
     inputMemoryMaps.push_back(
         utils::getMemoryMap(device, input, /*isRemote=*/true));
-    inputShardShapes.emplace_back(inputDeviceShape.drop_front(gridRank).begin(),
-                                  inputDeviceShape.drop_front(gridRank).end());
+    inputShardShapes.emplace_back(
+        inputDeviceShape.drop_front(compositeGridRank).begin(),
+        inputDeviceShape.drop_front(compositeGridRank).end());
   }
   TT_assert(totalInputExtent <= compositeExtent);

@@ -74,12 +91,15 @@ static LogicalResult expandCompositeDMAReadTiled(IRRewriter &rewriter,
   TT_assert(outputShardShape.size() == static_cast<size_t>(gridRank));

   auto emitDMAReadForInput = [&](OpBuilder &builder, Location innerLoc,
-                                 ValueRange shardIters, Value globalConcatIdx,
+                                 ValueRange shardIters,
+                                 ValueRange compositeFullIdx,
+                                 Value globalConcatIdx,
                                  const int inputIdx,
                                  const int64_t pieceOffset) {
     // Calculate the full index (grid x shard) of the current input tile.
-    SmallVector<Value> inputFullIdx(2 * gridRank);
-    for (int dim = 0; dim < gridRank; dim++) {
+    SmallVector<Value> inputFullIdx(2 * compositeGridRank);
+    auto compositeDeviceShape = compositeType.getShape();
+    for (int dim = 0; dim < compositeGridRank; dim++) {
       Value globalDimIdx = nullptr;
       if (dim == concatGridDim) {
         // Shift-back the output's tile index with this input's cumulative
@@ -94,14 +114,14 @@ static LogicalResult expandCompositeDMAReadTiled(IRRewriter &rewriter,
               innerLoc, globalConcatIdx, offsetVal);
         }
       } else {
-        // Otherwise it's just: grid_idx * tiles_per_shard + shard_idx.
-        Value outShardExtent = builder.create<arith::ConstantOp>(
+        Value compositeShardExtent = builder.create<arith::ConstantOp>(
             innerLoc, builder.getIndexType(),
-            builder.getIndexAttr(outputShardShape[dim]));
+            builder.getIndexAttr(
+                compositeDeviceShape[dim + compositeGridRank]));
         Value baseIdx = builder.create<arith::MulIOp>(
-            innerLoc, gridIndices[dim], outShardExtent);
-        globalDimIdx =
-            builder.create<arith::AddIOp>(innerLoc, baseIdx, shardIters[dim]);
+            innerLoc, compositeFullIdx[dim], compositeShardExtent);
+        globalDimIdx = builder.create<arith::AddIOp>(
+            innerLoc, baseIdx, compositeFullIdx[dim + compositeGridRank]);
       }

       Value inShardExtent = builder.create<arith::ConstantOp>(
@@ -115,7 +135,7 @@ static LogicalResult expandCompositeDMAReadTiled(IRRewriter &rewriter,
           builder.create<arith::RemSIOp>(innerLoc, globalDimIdx, inShardExtent);

       inputFullIdx[dim] = inputGridIdx;
-      inputFullIdx[dim + gridRank] = inputShardIdx;
+      inputFullIdx[dim + compositeGridRank] = inputShardIdx;
     }

     SmallVector<Value> remoteIndices = utils::applyMap(
@@ -137,15 +157,23 @@ static LogicalResult expandCompositeDMAReadTiled(IRRewriter &rewriter,
   scf::buildLoopNest(
       rewriter, loc, lbs, ubs, steps,
       [&](OpBuilder &loopBuilder, Location innerLoc, ValueRange shardIters) {
-        // Global index along the concat dim of a given tile:
-        // idx = grid_idx * tiles_per_shard + shard_idx
+        SmallVector<Value> sourceFullIdx;
+        sourceFullIdx.append(
+            sourceRemapping.getNumDims() - 2 * gridRank,
+            loopBuilder.create<arith::ConstantIndexOp>(innerLoc, 0));
+        sourceFullIdx.append(gridIndices.begin(), gridIndices.end());
+        sourceFullIdx.append(shardIters.begin(), shardIters.end());
+        SmallVector<Value> compositeFullIdx =
+            ttmlir::utils::fullyApplyAffineMap(
+                loopBuilder, innerLoc, sourceRemapping, sourceFullIdx);
+
         Value concatShardExtent = loopBuilder.create<arith::ConstantOp>(
-            innerLoc, loopBuilder.getIndexType(),
-            loopBuilder.getIndexAttr(outputShardShape[concatDim]));
+            innerLoc, loopBuilder.getIndexType(), loopBuilder.getIndexAttr(
+                compositeType.getShape()[concatShardDim]));
         Value baseConcatIdx = loopBuilder.create<arith::MulIOp>(
-            innerLoc, gridIndices[concatGridDim], concatShardExtent);
+            innerLoc, compositeFullIdx[concatGridDim], concatShardExtent);
         Value globalConcatIdx = loopBuilder.create<arith::AddIOp>(
-            innerLoc, baseConcatIdx, shardIters[concatDim]);
+            innerLoc, baseConcatIdx, compositeFullIdx[concatShardDim]);

         // Recursively generate the if-else chain for all inputs.
         std::function<void(OpBuilder &, const int, const int64_t)>
@@ -154,7 +182,8 @@ static LogicalResult expandCompositeDMAReadTiled(IRRewriter &rewriter,
               // Base case: reaching the end of the chain (the last 'else').
               if (inputIdx + 1 == static_cast<int>(expandedInputs.size())) {
                 emitDMAReadForInput(builder, innerLoc, shardIters,
-                                    globalConcatIdx, inputIdx, startOffset);
+                                    compositeFullIdx, globalConcatIdx, inputIdx,
+                                    startOffset);
                 return;
               }

@@ -174,7 +203,8 @@ static LogicalResult expandCompositeDMAReadTiled(IRRewriter &rewriter,

               OpBuilder thenBuilder = ifOp.getThenBodyBuilder();
               emitDMAReadForInput(thenBuilder, innerLoc, shardIters,
-                                  globalConcatIdx, inputIdx, startOffset);
+                                  compositeFullIdx, globalConcatIdx, inputIdx,
+                                  startOffset);

               OpBuilder elseBuilder = ifOp.getElseBodyBuilder();
               emitIfElseChain(elseBuilder, inputIdx + 1, boundary);
@@ -525,11 +555,26 @@ static LogicalResult expandCompositeViewsInGeneric(IRRewriter &rewriter,
   const bool isTiled =
       mlir::isa<ttcore::TileType>(compositeSourceType.getElementType());

-  const int32_t compositeDim = compositeView.getDim();
+  const int32_t originalCompositeDim = compositeView.getDim();
+  AffineMap sourceRemapping =
+      getCompositeSourceRemapping(compositeSource, compositeView);
+  int32_t compositeDim = originalCompositeDim;
+  const auto originalCompositeType =
+      mlir::cast<MemRefType>(compositeView.getResult().getType());
+  const int64_t originalGridRank = originalCompositeType.getRank() / 2;
+  const int64_t sourceGridRank = compositeSourceType.getRank() / 2;
+  if (!isTiled && sourceGridRank != originalGridRank) {
+    // Default tensor collapsing folds all leading logical dimensions into the
+    // physical row dimension and preserves the last logical dimension as the
+    // physical column dimension.
+    TT_assert(sourceGridRank == 2);
+    compositeDim = compositeDim == originalGridRank - 1 ? 1 : 0;
+  }
   LogicalResult result = success();
   if (isTiled) {
-    result = expandCompositeDMAReadTiled(rewriter, clonedCompositeRead,
-                                         expandedGenericInputs, compositeDim);
+    result = expandCompositeDMAReadTiled(
+        rewriter, clonedCompositeRead, expandedGenericInputs, compositeView,
+        sourceRemapping, originalCompositeDim);
   } else {
     // This value is only used to determine if we need the guard to skip the
     // padded portions of the output shards.
diff --git a/lib/Dialect/D2M/Transforms/GenericFusion.cpp b/lib/Dialect/D2M/Transforms/GenericFusion.cpp
--- a/lib/Dialect/D2M/Transforms/GenericFusion.cpp
+++ b/lib/Dialect/D2M/Transforms/GenericFusion.cpp
@@ -13,5 +13,6 @@
 #include "mlir/Dialect/Affine/IR/AffineOps.h"
 #include "mlir/Dialect/Linalg/IR/Linalg.h"
+#include "mlir/Dialect/Linalg/Transforms/Transforms.h"
 #include "mlir/Dialect/Tensor/IR/Tensor.h"
 #include "mlir/IR/AsmState.h"
 #include "mlir/IR/PatternMatch.h"
@@ -576,2 +577,37 @@ static FailureOr<GenericOp> fuseGenericPair(
   rewriter.setInsertionPointToEnd(&fusedBlock);
   rewriter.create<YieldOp>(fusedOp.getLoc(), fusedYields);
+  // Keep all-parallel chains as one linalg.generic. Besides producing less
+  // IR, this lets values flow directly through DST instead of round-tripping
+  // through an L1 scratch CB between elementwise operations.
+  bool changed = true;
+  while (changed) {
+    changed = false;
+    for (auto consumerOp :
+         llvm::make_early_inc_range(fusedBlock.getOps<linalg::GenericOp>())) {
+      if (consumerOp.getNumReductionLoops() != 0)
+        continue;
+      for (OpOperand &operand : consumerOp->getOpOperands()) {
+        if (!linalg::areElementwiseOpsFusable(&operand))
+          continue;
+
+        auto producerOp = operand.get().getDefiningOp<linalg::GenericOp>();
+        rewriter.setInsertionPoint(consumerOp);
+        FailureOr<linalg::ElementwiseOpFusionResult> fusion =
+            linalg::fuseElementwiseOps(rewriter, &operand);
+        if (failed(fusion))
+          continue;
+
+        for (auto [original, replacement] : fusion->replacements)
+          original.replaceAllUsesWith(replacement);
+        rewriter.eraseOp(consumerOp);
+        if (llvm::all_of(producerOp->getResults(),
+                         [](Value result) { return result.use_empty(); }))
+          rewriter.eraseOp(producerOp);
+        changed = true;
+        break;
+      }
+      if (changed)
+        break;
+    }
+  }
+
@@ -579,9 +622,14 @@ static FailureOr<GenericOp> fuseGenericPair(
   // Tag inner linalg.generic ops with their output blocking map so that the
   // allocator's reblocking can derive the correct shape for intermediate
   // buffers that have no associated generic operand.
   for (Operation &op : fusedBlock) {
     if (auto linalgOp = dyn_cast<linalg::LinalgOp>(&op)) {
       AffineMap outputMap = linalgOp.getIndexingMapsArray().back();
-      op.setAttr("d2m.blocking_map", AffineMapAttr::get(outputMap));
+      auto blockingMapAttr = AffineMapAttr::get(outputMap);
+      op.setAttr("d2m.blocking_map", blockingMapAttr);
+      for (OpOperand &init : linalgOp.getDpsInitsMutable()) {
+        if (auto emptyOp = init.get().getDefiningOp<tensor::EmptyOp>())
+          emptyOp->setAttr("d2m.blocking_map", blockingMapAttr);
+      }
     }
   }
@@ -756,7 +804,9 @@ class D2MGenericFusion
   void runOnOperation() override {
     MLIRContext *ctx = &getContext();
     RewritePatternSet patterns(ctx);
-    patterns.add<FuseD2MElementwiseOpsPattern>(ctx);
+    if (enableElementwiseFusion) {
+      patterns.add<FuseD2MElementwiseOpsPattern>(ctx);
+    }
     if (enableEltwiseReductionFusion) {
       patterns.add<FuseD2MEltwiseReductionOpsPattern>(ctx);
     }
diff --git a/lib/Dialect/D2M/Transforms/GridSelection.cpp b/lib/Dialect/D2M/Transforms/GridSelection.cpp
--- a/lib/Dialect/D2M/Transforms/GridSelection.cpp
+++ b/lib/Dialect/D2M/Transforms/GridSelection.cpp
@@ -169,6 +169,15 @@ optimizeToLayoutGrid(d2m::ToLayoutOp toLayoutOp, ArrayRef<int64_t> targetGrid,
     return;
   }

+  // Interleaved DRAM tensors always use a unit physical grid.  Let the
+  // consumer-side view select its worker grid without changing the backing
+  // tensor's layout.
+  if (oldLayout.getMemorySpace() == ttcore::MemorySpace::DeviceDRAM &&
+      oldLayout.getMemoryLayout() ==
+          ttcore::TensorMemoryLayout::Interleaved) {
+    return;
+  }
+
   bool needsOptimization = false;
   for (int64_t g : optimalGrid) {
     if (g > 1) {
@@ -234,61 +243,6 @@ optimizeToLayoutGrid(d2m::ToLayoutOp toLayoutOp, ArrayRef<int64_t> targetGrid,
   }
 }

-static void insertViewForTTNNDRAMTensor(Value operand,
-                                        ArrayRef<int64_t> optimalGrid,
-                                        OpBuilder &builder) {
-  while (auto viewOp = operand.getDefiningOp<d2m::ViewLayoutOp>()) {
-    auto originalOperand = viewOp.getInput();
-    viewOp.getResult().replaceAllUsesWith(originalOperand);
-    viewOp.erase();
-    operand = originalOperand;
-  }
-  // Do not "restream" metal -> ttnn -> metal sequences. This happens when the
-  // output of a generic is the input to another generic. The output is
-  // already streamed, but the cast back to ttnn silently erases the index
-  // map. Instead, we just forward the already streamed metal tensor to the
-  // current generic.
-  auto castOp = operand.getDefiningOp<ttir::TTNNMetalLayoutCastOp>();
-  auto producerCastOp =
-      castOp.getInput().getDefiningOp<ttir::TTNNMetalLayoutCastOp>();
-  if (producerCastOp) {
-    castOp.getResult().replaceAllUsesExcept(producerCastOp.getInput(),
-                                            producerCastOp);
-    return;
-  }
-
-  auto metalTensor = mlir::cast<mlir::RankedTensorType>(operand.getType());
-  auto baseMetalLayout =
-      mlir::cast<ttcore::MetalLayoutAttr>(metalTensor.getEncoding());
-
-  // TTNN DRAM interleaved tensors are represented as having a 1x1 grid.
-  llvm::SmallVector<int64_t> unitGridShape{1, 1};
-  llvm::SmallVector<int64_t> unShardedShapeWithGrid =
-      baseMetalLayout.getDeviceShape(unitGridShape,
-                                     ttcore::TileType::getDefaultShape());
-
-  llvm::SmallVector<int64_t> fakeShardedShape = baseMetalLayout.getDeviceShape(
-      optimalGrid, ttcore::TileType::getDefaultShape());
-
-  AffineMap reblockMap = ttmlir::utils::calculateReblockMap(
-      unShardedShapeWithGrid, fakeShardedShape, builder.getContext());
-
-  auto viewOutputLayout = ttcore::MetalLayoutAttr::get(
-      builder.getContext(), baseMetalLayout.getLogicalShape(),
-      ttcore::MemorySpace::DeviceDRAM, ttcore::TensorMemoryLayout::Interleaved,
-      baseMetalLayout.getCollapsedIntervals(),
-      baseMetalLayout.getDimAlignments());
-
-  auto viewOutputTensor = mlir::RankedTensorType::get(
-      fakeShardedShape, metalTensor.getElementType(), viewOutputLayout);
-
-  builder.setInsertionPointAfter(castOp);
-  auto viewOp = builder.create<d2m::ViewLayoutOp>(
-      castOp.getLoc(), viewOutputTensor, castOp.getResult(),
-      AffineMapAttr::get(reblockMap));
-  castOp.getResult().replaceAllUsesExcept(viewOp.getResult(), viewOp);
-}
-
 static void
 optimizeTTNNMetalLayoutCastOpGrid(ttir::TTNNMetalLayoutCastOp castOp,
                                   ArrayRef<int64_t> optimalGrid,
@@ -417,7 +371,7 @@ static void applyTTNNTensorUpdate(const OperandGridInfo &info,
   auto metalLayout =
       mlir::cast<ttcore::MetalLayoutAttr>(metalTensor.getEncoding());
   if (metalLayout.getMemorySpace() == ttcore::MemorySpace::DeviceDRAM) {
-    insertViewForTTNNDRAMTensor(ttnnOperand, info.selectedGrid, builder);
+    return;
   } else if (auto castOp =
                  ttnnOperand.getDefiningOp<ttir::TTNNMetalLayoutCastOp>()) {
     optimizeTTNNMetalLayoutCastOpGrid(castOp, info.selectedGrid, builder);
diff --git a/lib/Dialect/D2M/Transforms/LowerDMAToFullyIndexedForm.cpp b/lib/Dialect/D2M/Transforms/LowerDMAToFullyIndexedForm.cpp
--- a/lib/Dialect/D2M/Transforms/LowerDMAToFullyIndexedForm.cpp
+++ b/lib/Dialect/D2M/Transforms/LowerDMAToFullyIndexedForm.cpp
@@ -77,6 +77,18 @@ static size_t getElementSizeBytes(MemRefType memref) {
                   : elementType.getIntOrFloatBitWidth() / 8;
 }

+static void padIndicesToMapDomain(SmallVectorImpl<Value> &indices,
+                                  AffineMap map, OpBuilder &builder,
+                                  Location loc) {
+  TT_assert(indices.size() <= map.getNumDims());
+  if (indices.size() == map.getNumDims()) {
+    return;
+  }
+  Value zero = builder.create<arith::ConstantOp>(
+      loc, builder.getIndexType(), builder.getIndexAttr(0));
+  indices.insert(indices.begin(), map.getNumDims() - indices.size(), zero);
+}
+
 // Calculates coalescing factor using analytical method with sampling fallback.
 // Mirrors the logic from GenericLowerDMAs::analyzeStream.
 static size_t calculateCoalescingFactorWithFallback(
@@ -90,10 +102,17 @@ static size_t calculateCoalescingFactorWithFallback(
   SmallVector<int64_t> fullShape;
   fullShape.append(gridShape.begin(), gridShape.end());
   fullShape.append(shardShape.begin(), shardShape.end());
+  TT_assert(fullShape.size() <= memoryMap.getNumDims());
+  unsigned numGridDims = gridShape.size();
+  if (fullShape.size() != memoryMap.getNumDims()) {
+    size_t padding = memoryMap.getNumDims() - fullShape.size();
+    fullShape.insert(fullShape.begin(), padding, 1);
+    numGridDims += padding;
+  }

   // Try analytical method first
   size_t coalescingFactor = ttmlir::utils::computeCoalescingFactorAnalytically(
-      memoryMap, fullShape, gridShape.size(), elemSizeBytes);
+      memoryMap, fullShape, numGridDims, elemSizeBytes);

   // Determine if we should fallback to sampling
   size_t analyticalChunkSize = coalescingFactor * elemSizeBytes;
@@ -111,7 +130,7 @@ static size_t calculateCoalescingFactorWithFallback(
     }

     size_t sampledCoalescingFactor = ttmlir::utils::calculateCoalescingFactor(
-        memoryMap, fullShape, elemSizeBytes, gridShape.size());
+        memoryMap, fullShape, elemSizeBytes, numGridDims);

     if (debugCoalescingInference) {
       if (coalescingFactor == sampledCoalescingFactor) {
@@ -316,6 +335,7 @@ public:
               llvm::to_vector(llvm::concat<Value>(gridIndices, iters));
           SmallVector<Value> localIndices = llvm::to_vector(iters);

+          padIndicesToMapDomain(remoteIndices, remoteMemoryMap, b, l);
           remoteIndices =
               utils::applyMap(b, l, remoteMemoryMap, remoteIndices, true);
           localIndices =
@@ -416,6 +436,7 @@ public:
               llvm::to_vector(llvm::concat<Value>(gridIndices, iters));
           SmallVector<Value> localIndices = llvm::to_vector(iters);

+          padIndicesToMapDomain(remoteIndices, remoteMemoryMap, b, l);
           remoteIndices =
               utils::applyMap(b, l, remoteMemoryMap, remoteIndices, true);
           localIndices =
diff --git a/lib/Dialect/D2M/Transforms/LowerToLayout/LowerToLayout.cpp b/lib/Dialect/D2M/Transforms/LowerToLayout/LowerToLayout.cpp
--- a/lib/Dialect/D2M/Transforms/LowerToLayout/LowerToLayout.cpp
+++ b/lib/Dialect/D2M/Transforms/LowerToLayout/LowerToLayout.cpp
@@ -322,9 +322,6 @@ public:
         (!outputInfo.isDRAM() &&
          (inputInfo.getGridShape() != outputInfo.getGridShape()));

-    assert(!(isSrcDramOrReblock && outputInfo.isDRAM()) &&
-           "input and output cannot both be remote");
-
     auto buildConcreteView = [&](Value fromVal, RankedTensorType fromTy,
                                  RankedTensorType toTy) -> Value {
       auto *ctx = rewriter.getContext();
diff --git a/lib/Dialect/D2M/Transforms/LowerToLayout/Plan.cpp b/lib/Dialect/D2M/Transforms/LowerToLayout/Plan.cpp
--- a/lib/Dialect/D2M/Transforms/LowerToLayout/Plan.cpp
+++ b/lib/Dialect/D2M/Transforms/LowerToLayout/Plan.cpp
@@ -341,18 +341,38 @@ Plan canonicalize(const PlanState &src, const PlanState &tgt,
     updateStateFromOutput(current, output);
   }

-  // DRAM → DRAM materialization: stage through an equivalent L1 buffer before
-  // doing format/grid/VGM changes, then let the normal L1 → DRAM rule below
-  // write into the final target buffer.
+  // DRAM → DRAM materialization. Pure same-grid copies can write directly
+  // to the target; format and grid changes still stage through L1.
   if (needsDramBounce(current, tgt)) {
-    auto l1Type = modifyDeviceType(
-        ctx, current.type, *current.getLayout(), targetGridShape,
-        current.remapping, ttcore::MemorySpace::DeviceL1,
-        llvm::to_vector(current.getGridShape()), current.type.getElementType());
-    auto output =
-        makeOutputSpec(l1Type, current.vgmForward, current.vgmInverse);
-    plan.push_back(DRAMToL1Step{output, AffineMap()});
-    updateStateFromOutput(current, output, current.remapping);
+    const bool canRebufferDirectly =
+        current.getGridShape() == tgt.getGridShape() &&
+        current.type.getElementType() == tgt.type.getElementType() &&
+        ttcore::isTiled(current.type) == ttcore::isTiled(tgt.type);
+    if (canRebufferDirectly) {
+      auto output = makeOutputSpec(tgt.type, tgt.vgmForward, tgt.vgmInverse);
+      plan.push_back(RebufferStep{output});
+      updateStateFromOutput(current, output);
+    } else {
+      auto l1Type = modifyDeviceType(
+          ctx, current.type, *current.getLayout(), targetGridShape,
+          current.remapping, ttcore::MemorySpace::DeviceL1,
+          llvm::to_vector(current.getGridShape()),
+          current.type.getElementType());
+      if (current.getLayout()->getMemoryLayout() ==
+          ttcore::TensorMemoryLayout::Interleaved) {
+        auto layout = mlir::cast<ttcore::MetalLayoutAttr>(l1Type.getEncoding());
+        layout = ttcore::MetalLayoutAttr::get(
+            ctx, layout.getLogicalShape(), layout.getDimAlignments(),
+            layout.getCollapsedIntervals(), ttcore::MemorySpace::DeviceL1,
+            ttcore::TensorMemoryLayout::Sharded);
+        l1Type = RankedTensorType::get(
+            l1Type.getShape(), l1Type.getElementType(), layout);
+      }
+      auto output =
+          makeOutputSpec(l1Type, current.vgmForward, current.vgmInverse);
+      plan.push_back(DRAMToL1Step{output, AffineMap()});
+      updateStateFromOutput(current, output, current.remapping);
+    }
   }

   // DRAM → L1: DMA the tensor into an L1 buffer with the target's layout.
diff --git a/lib/Dialect/D2M/Transforms/InsertDstRegisterAccess/Scheduled.cpp b/lib/Dialect/D2M/Transforms/InsertDstRegisterAccess/Scheduled.cpp
--- a/lib/Dialect/D2M/Transforms/InsertDstRegisterAccess/Scheduled.cpp
+++ b/lib/Dialect/D2M/Transforms/InsertDstRegisterAccess/Scheduled.cpp
@@ -68,1 +68,9 @@ collectDstAccessesScheduled(GenericOp op, Region &region,
             getAccumClassificationOperandIndices(computeOp);
+        auto bcastLoad = mlir::isa<d2m::TileBcastOp>(computeOp)
+                             ? computeOp->getOperand(0)
+                                   .getDefiningOp<affine::AffineLoadOp>()
+                             : affine::AffineLoadOp{};
+        const bool inlineBcast =
+            bcastLoad && notDstMemspace(bcastLoad) && computeOp->hasOneUse() &&
+            (*computeOp->getUsers().begin())
+                ->hasTrait<D2MGenericRegionComputeOpTrait>();
@@ -75,4 +83,10 @@ collectDstAccessesScheduled(GenericOp op, Region &region,
-          ++numLoads;
-
           Value operand = computeOp->getOperand(operandIdx);
+          Operation *producer = operand.getDefiningOp();
+          if (!mlir::isa_and_nonnull<d2m::TileBcastOp>(producer) ||
+              dstIntermediates.contains(producer)) {
+            ++numLoads;
+          }
+          if (inlineBcast && operandIdx == 0) {
+            continue;
+          }
           if (auto affineLoad = operand.getDefiningOp<affine::AffineLoadOp>();
@@ -147,2 +161,9 @@ collectDstAccessesScheduled(GenericOp op, Region &region,
-            dstIntermediates[computeOp] = {allocatedIndex,
-                                           outermostInnerComputeLoop};
+            if (inlineBcast) {
+              recordDstAccess(
+                  bcastLoad, mlir::cast<d2m::TileBcastOp>(computeOp),
+                  copyInfos, allocatedIndex, outermostInnerComputeLoop,
+                  /*emitGuard=*/true);
+            } else {
+              dstIntermediates[computeOp] = {allocatedIndex,
+                                             outermostInnerComputeLoop};
+            }
diff --git a/lib/Dialect/D2M/Transforms/InsertDstRegisterAccess/Shared.cpp b/lib/Dialect/D2M/Transforms/InsertDstRegisterAccess/Shared.cpp
--- a/lib/Dialect/D2M/Transforms/InsertDstRegisterAccess/Shared.cpp
+++ b/lib/Dialect/D2M/Transforms/InsertDstRegisterAccess/Shared.cpp
@@ -1282,1 +1282,2 @@ void emitDstCopyNest(PatternRewriter &rewriter, Operation *loopNestOrOp,
-    const bool useInPlace = !cloneLoopNest || access.isMemref();
+    const bool useInPlace =
+        (!cloneLoopNest && access.guardIVs.empty()) || access.isMemref();
@@ -1338,2 +1339,19 @@ void emitDstCopyNest(PatternRewriter &rewriter, Operation *loopNestOrOp,
           rewriter.setInsertionPoint(copyLoop);
+        } else if (isBcastGuard) {
+          for (Value guardIV : access.guardIVs) {
+            irMapper.map(guardIV,
+                         getFirstIterationValue(rewriter, loadStoreLoc,
+                                                guardIV));
+          }
+          // Unit-trip indices are invariant even when they are omitted from
+          // the guard list. Map them before hoisting the broadcast.
+          for (Value index : access.getAffineIndices()) {
+            auto loop = dyn_cast_or_null<affine::AffineForOp>(
+                getLoopOpForIV(index));
+            if (!irMapper.contains(index) && loop &&
+                tryGetConstantAffineTripCount(loop).value_or(0) == 1) {
+              irMapper.map(index, getFirstIterationValue(
+                                      rewriter, loadStoreLoc, index));
+            }
+          }
         }
diff --git a/lib/Dialect/D2M/Utils/DstRegisterAnalysis.cpp b/lib/Dialect/D2M/Utils/DstRegisterAnalysis.cpp
--- a/lib/Dialect/D2M/Utils/DstRegisterAnalysis.cpp
+++ b/lib/Dialect/D2M/Utils/DstRegisterAnalysis.cpp
@@ -371,5 +371,20 @@ computeDSTPackingForRegion(d2m::GenericOp generic,
   if (!singleTileOutputValues.empty()) {
-    generic.emitOpError(
-        "expected all linalg.generic outputs in a region to have shard size 1");
-    return std::nullopt;
+    // A single-tile reduction can follow a multi-tile producer without shared
+    // outer tiling. Each linalg op keeps its own phase-2 packing.
+    results.numOuterLoopIters = 1;
+    results.numTilesPerResult = pendingResults.front().shardSizeTiles;
+    for (Value outputValue : singleTileOutputValues) {
+      results.perResult.try_emplace(
+          outputValue, DSTPackingPerResultInfo{/*numDstFlips=*/1,
+                                               /*numTilesPerFlip=*/1,
+                                               /*numTilesPerResult=*/1});
+    }
+    for (const PendingDSTPackingResult &pending : pendingResults) {
+      results.perResult.try_emplace(
+          pending.outputValue,
+          DSTPackingPerResultInfo{pending.numDstFlips,
+                                  pending.numTilesPerFlip,
+                                  pending.shardSizeTiles});
+    }
+    return results;
   }

diff --git a/lib/Dialect/D2M/Utils/GridSelectionUtils.cpp b/lib/Dialect/D2M/Utils/GridSelectionUtils.cpp
--- a/lib/Dialect/D2M/Utils/GridSelectionUtils.cpp
+++ b/lib/Dialect/D2M/Utils/GridSelectionUtils.cpp
@@ -228,6 +228,18 @@ static llvm::SmallVector<int64_t> computeOptimalVirtualGrid(
   return grid;
 }

+static bool needsF32DRAMGrid(mlir::RankedTensorType tensorType) {
+  auto layout =
+      mlir::cast<ttcore::MetalLayoutAttr>(tensorType.getEncoding());
+  if (layout.getMemorySpace() != ttcore::MemorySpace::DeviceDRAM ||
+      layout.getMemoryLayout() != ttcore::TensorMemoryLayout::Interleaved)
+    return false;
+  Type elementType = tensorType.getElementType();
+  if (auto tileType = mlir::dyn_cast<ttcore::TileType>(elementType))
+    elementType = tileType.getElementType();
+  return mlir::isa<Float32Type>(elementType);
+}
+
 bool shouldImplementAsVirtualGrid(mlir::RankedTensorType tensorType,
                                   ArrayRef<int64_t> physicalShape,
                                   ArrayRef<int64_t> targetGrid) {
@@ -236,7 +248,8 @@ bool shouldImplementAsVirtualGrid(mlir::RankedTensorType tensorType,
   ttcore::MetalLayoutAttr layout =
       mlir::cast<ttcore::MetalLayoutAttr>(tensorType.getEncoding());

-  if (layout.getMemoryLayout() == ttcore::TensorMemoryLayout::Interleaved) {
+  if (layout.getMemoryLayout() == ttcore::TensorMemoryLayout::Interleaved &&
+      !needsF32DRAMGrid(tensorType)) {
     return false;
   }

@@ -260,5 +273,13 @@ llvm::SmallVector<int64_t> computeOptimalGrid(
                                               ArrayRef<int64_t> physicalShape,
                                               ArrayRef<int64_t> targetGrid) {
+  if (needsF32DRAMGrid(tensorType)) {
+    llvm::SmallVector<int64_t> oneDimensionalTarget = {1, targetGrid.back()};
+    auto grid =
+        computeOptimalVirtualGrid(physicalShape, oneDimensionalTarget);
+    if (!grid.empty())
+      return grid;
+  }
+
   if (shouldImplementAsVirtualGrid(tensorType, physicalShape, targetGrid)) {
     auto virtualGrid = computeOptimalVirtualGrid(physicalShape, targetGrid);
     if (!virtualGrid.empty()) {
diff --git a/lib/Dialect/D2M/Utils/Utils.cpp b/lib/Dialect/D2M/Utils/Utils.cpp
--- a/lib/Dialect/D2M/Utils/Utils.cpp
+++ b/lib/Dialect/D2M/Utils/Utils.cpp
@@ -741,17 +741,25 @@ getMemoryMapImpl(ttcore::DeviceAttr device, MemRefType memrefType,
     }
     }
   } else if (mlir::isa<ttcore::InterleavedLayoutAttr>(memrefType.getLayout())) {
+    auto interleavedLayout =
+        mlir::cast<ttcore::InterleavedLayoutAttr>(memrefType.getLayout());

     if (view) {
+      if (affineMap.getNumDims() != view->getNumResults()) {
+        unsigned gridRank = interleavedLayout.getGridShape(memrefType).size();
+        unsigned shardRank = interleavedLayout.getShardShape(memrefType).size();
+        TT_assert(view->getNumResults() >= gridRank + shardRank);
+        SmallVector<AffineExpr> results(view->getResults().take_front(gridRank));
+        llvm::append_range(results, view->getResults().take_back(shardRank));
+        view = AffineMap::get(view->getNumDims(), view->getNumSymbols(), results,
+                              view->getContext());
+      }
       affineMap = affineMap.compose(*view);
     }

     assert(memorySpace == ttcore::MemorySpace::DeviceDRAM &&
            "interleavedLayoutAttr only supported for deviceDRAM memory space");

-    auto interleavedLayout =
-        mlir::cast<ttcore::InterleavedLayoutAttr>(memrefType.getLayout());
-
     int64_t elementSizeBytes =
         ttcore::getElementSizeBytes(memrefType.getElementType());
     pageSize = mlir::isa<ttcore::TileType>(memrefType.getElementType())
@@ -898,9 +906,9 @@ SmallVector<Value> applyMap(Builder &builder, Location loc, AffineMap map,
     // Break the map into respective gridY, gridX, offset "single result"
     // parts. AffineApply only supports single result affine maps.
     map = map.dropResults(0); // Drop the device index.
-    auto gridY = map.dropResults({1, 2});
-    auto gridX = map.dropResults({0, 2});
-    auto offset = map.dropResults({0, 1});
+    auto gridY = map.dropResults(llvm::ArrayRef<int64_t>{1, 2});
+    auto gridX = map.dropResults(llvm::ArrayRef<int64_t>{0, 2});
+    auto offset = map.dropResults(llvm::ArrayRef<int64_t>{0, 1});
     return {affineApply(gridY, index), affineApply(gridX, index),
             affineApply(offset, index)};
   }
diff --git a/lib/Dialect/TTCore/IR/Utils.cpp b/lib/Dialect/TTCore/IR/Utils.cpp
--- a/lib/Dialect/TTCore/IR/Utils.cpp
+++ b/lib/Dialect/TTCore/IR/Utils.cpp
@@ -154,8 +154,8 @@ Type getOperandInnerElementType(const mlir::Value operand) {
   auto elemType = operand.getType();
-  if (auto memRefType = mlir::dyn_cast<MemRefType>(elemType);
-      memRefType != nullptr) {
-    elemType = memRefType.getElementType();
+  if (auto shapedType = mlir::dyn_cast<ShapedType>(elemType);
+      shapedType != nullptr) {
+    elemType = shapedType.getElementType();
   }
   // We could have a memref of tiles, so this needs to be the second query.
   if (auto tileType = mlir::dyn_cast<ttcore::TileType>(elemType);
       tileType != nullptr) {
diff --git a/lib/Dialect/TTIR/Transforms/TTIRFusing.cpp b/lib/Dialect/TTIR/Transforms/TTIRFusing.cpp
--- a/lib/Dialect/TTIR/Transforms/TTIRFusing.cpp
+++ b/lib/Dialect/TTIR/Transforms/TTIRFusing.cpp
@@ -3775,18 +3775,22 @@ public:
       if (permuteSliceAfterMatmulEnabled) {
         patterns.add<PermuteSliceAfterMatmulPattern>(&getContext());
       }
-      patterns.add<RMSNormFusionPattern>(&getContext());
-      patterns.add<NormalizeRMSNormFusionPattern>(&getContext());
+      if (specializedFusionEnabled) {
+        patterns.add<RMSNormFusionPattern>(&getContext());
+        patterns.add<NormalizeRMSNormFusionPattern>(&getContext());
+      }

       patterns.add<GeluFusionPattern>(&getContext());
       patterns.add<Relu6FusionPattern>(&getContext());
       patterns.add<SiluFusionPattern>(&getContext());
       patterns.add<HardsigmoidFusionPattern>(&getContext());
       patterns.add<MishFusingPattern>(&getContext());
       patterns.add<ReshapeBroadcastReshapeToRepeatPattern>(&getContext());
-      patterns.add<fusing::RoPERotateHalfFusingPattern>(&getContext());
-      patterns.add<fusing::RoPEComplexRotationFusingPattern>(&getContext());
-      patterns.add<fusing::RoPEInterleavedPairFusingPattern>(&getContext());
+      if (specializedFusionEnabled) {
+        patterns.add<fusing::RoPERotateHalfFusingPattern>(&getContext());
+        patterns.add<fusing::RoPEComplexRotationFusingPattern>(&getContext());
+        patterns.add<fusing::RoPEInterleavedPairFusingPattern>(&getContext());
+      }
       patterns.add<fusing::SDPAFusingPattern>(&getContext());
       patterns.add<fusing::TopKFusingPattern>(&getContext());

diff --git a/lib/Dialect/TTNN/Pipelines/TTNNPipelines.cpp b/lib/Dialect/TTNN/Pipelines/TTNNPipelines.cpp
--- a/lib/Dialect/TTNN/Pipelines/TTNNPipelines.cpp
+++ b/lib/Dialect/TTNN/Pipelines/TTNNPipelines.cpp
@@ -48,7 +48,8 @@ void createTTNNPipelineTTIRPasses(
   ttir::TTIRFusingOptions fusingOptions{
       options.enableFusingConv2dWithMultiplyPattern,
       options.enablePermuteMatmulFusion,
-      options.enablePermuteSliceAfterMatmulFusion};
+      options.enablePermuteSliceAfterMatmulFusion,
+      !options.enableCreateD2MSubgraphs};
   if (options.enableFusing) {
     pm.addPass(mlir::tt::ttir::createTTIRFusing(fusingOptions));
   }
@@ -207,6 +208,12 @@ void createTTNNResolveCompositesPass(
     OpPassManager &pm, const TTIRToTTNNCommonPipelineOptions &options) {
   CompositeResolution resolution = options.compositeResolution;

+  // General fusion operates on primitive graphs. Composite decompositions
+  // provide those graphs while keeping named operations available as the
+  // fallback when D2M subgraphs are disabled.
+  if (options.enableCreateD2MSubgraphs)
+    resolution = CompositeResolution::Inline;
+
 #ifdef TTMLIR_ENABLE_OPMODEL
   const bool canValidate = options.optimizerPassEnabled;
 #else
@@ -738,6 +745,8 @@ void createTTNNPipelineD2MPass(OpPassManager &pm,
   // only works on top-level modules (doesn't run module has a parent op).
   ttmetal::D2MPipelineOptions ttmetalOptions;
   ttmetalOptions.ttnnMode = true;
+  ttmetalOptions.defaultInputMemSpace = ttcore::MemorySpace::DeviceDRAM;
+  ttmetalOptions.defaultOutputMemSpace = ttcore::MemorySpace::DeviceDRAM;
   ttmetalOptions.enableElementwiseFusion = options.enableElementwiseFusion;
   ttmetal::createD2MFrontendPipeline(pm, ttmetalOptions);
   ttmetal::createD2MBackendPipeline(pm, ttmetalOptions);
diff --git a/lib/Dialect/TTNN/Transforms/TTNNCreateD2MSubgraphs.cpp b/lib/Dialect/TTNN/Transforms/TTNNCreateD2MSubgraphs.cpp
--- a/lib/Dialect/TTNN/Transforms/TTNNCreateD2MSubgraphs.cpp
+++ b/lib/Dialect/TTNN/Transforms/TTNNCreateD2MSubgraphs.cpp
@@ -44,6 +44,8 @@ public:
   static bool isAllowedToBeFused(Operation *op) {
     if (!op) {
       return false;
     }
+    if (mlir::isa<ConcatOp, ReshapeOp, SliceStaticOp, SumOp, MeanOp>(op))
+      return true;
     // Eltwise unary ops.
     if (mlir::isa<AbsOp, CbrtOp, CeilOp, SignOp, CosOp, ExpOp, ErfOp, ErfcOp,
                   FloorOp, GeluOp, IsFiniteOp, LogicalNotOp, BitwiseNotOp,
