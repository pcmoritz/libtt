diff --git a/mlir/lib/IR/Operation.cpp b/mlir/lib/IR/Operation.cpp
index 8bcfa46..25591e7 100644
--- a/mlir/lib/IR/Operation.cpp
+++ b/mlir/lib/IR/Operation.cpp
@@ -608,26 +608,22 @@ void Operation::setSuccessor(Block *block, unsigned index) {
   getBlockOperands()[index].set(block);
 }
 
-#ifndef NDEBUG
-/// Assert that the folded results (in case of values) have the same type as
-/// the results of the given op.
-static void checkFoldResultTypes(Operation *op,
-                                 SmallVectorImpl<OpFoldResult> &results) {
+/// Return whether folded SSA values have the same type as the corresponding
+/// operation results. Rejecting an invalid fold is safer than allowing a
+/// rewrite driver to replace an operation with a differently typed value.
+static bool haveValidFoldResultTypes(
+    Operation *op, SmallVectorImpl<OpFoldResult> &results) {
   if (results.empty())
-    return;
+    return true;
 
   for (auto [ofr, opResult] : llvm::zip_equal(results, op->getResults())) {
     if (auto value = dyn_cast<Value>(ofr)) {
-      if (value.getType() != opResult.getType()) {
-        op->emitOpError() << "folder produced a value of incorrect type: "
-                          << value.getType()
-                          << ", expected: " << opResult.getType();
-        assert(false && "incorrect fold result type");
-      }
+      if (value.getType() != opResult.getType())
+        return false;
     }
   }
+  return true;
 }
-#endif // NDEBUG
 
 /// Attempt to fold this operation using the Op's registered foldHook.
 LogicalResult Operation::fold(ArrayRef<Attribute> operands,
@@ -635,9 +631,10 @@ LogicalResult Operation::fold(ArrayRef<Attribute> operands,
   // If we have a registered operation definition matching this one, use it to
   // try to constant fold the operation.
   if (succeeded(name.foldHook(this, operands, results))) {
-#ifndef NDEBUG
-    checkFoldResultTypes(this, results);
-#endif // NDEBUG
+    if (!haveValidFoldResultTypes(this, results)) {
+      results.clear();
+      return failure();
+    }
     return success();
   }
 
@@ -651,10 +648,10 @@ LogicalResult Operation::fold(ArrayRef<Attribute> operands,
     return failure();
 
   LogicalResult status = interface->fold(this, operands, results);
-#ifndef NDEBUG
-  if (succeeded(status))
-    checkFoldResultTypes(this, results);
-#endif // NDEBUG
+  if (succeeded(status) && !haveValidFoldResultTypes(this, results)) {
+    results.clear();
+    return failure();
+  }
   return status;
 }
 
