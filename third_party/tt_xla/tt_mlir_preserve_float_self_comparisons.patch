diff --git a/lib/Dialect/TTIR/IR/TTIROps.cpp b/lib/Dialect/TTIR/IR/TTIROps.cpp
--- a/lib/Dialect/TTIR/IR/TTIROps.cpp
+++ b/lib/Dialect/TTIR/IR/TTIROps.cpp
@@ -8879,5 +8879,5 @@ static bool anyZero(mlir::ElementsAttr elems) {
 
-// Fold a self-comparison `x <cmp> x` (same SSA value): `eq` is all-true and
-// `ne` is all-false. Note that for floating-point operands this assumes the
-// value is not NaN (`NaN == NaN` is false, `NaN != NaN` is true).
+// Fold an integer self-comparison `x <cmp> x` (same SSA value): `eq` is
+// all-true and `ne` is all-false. Floating-point values may be NaN, so their
+// self-comparisons cannot be folded.
 static mlir::OpFoldResult foldSelfComparison(mlir::Operation *op,
@@ -8885,3 +8885,5 @@ static mlir::OpFoldResult foldSelfComparison(mlir::Operation *op,
                                              bool foldToTrue) {
-  if (lhs != rhs) {
+  auto elementType =
+      mlir::cast<mlir::ShapedType>(lhs.getType()).getElementType();
+  if (lhs != rhs || !mlir::isa<mlir::IntegerType>(elementType)) {
     return nullptr;
