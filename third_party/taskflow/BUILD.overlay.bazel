load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "taskflow",
    hdrs = glob([
        "taskflow/**/*.h",
        "taskflow/**/*.hpp",
    ], allow_empty = True),
    includes = ["."],
)
